# Frontend Vite env example (local / restore builds)
# Copy to `.env.local` for local overrides. Default: demo OFF.

# LIVE TB Director presentation Demo View (frontend overlays only).
# Never influences Match API, analysis, or persistence.
VITE_LIVE_TB_DEMO_ENABLED=false
