/**
 * DWES frontend feature flags.
 *
 * Add new flags here when a feature must be hideable without deleting code.
 * Panel 3D model generation / demo lab was removed; VITE_ENABLE_PANEL_3D is obsolete.
 */
export {};
