/* @ds-bundle: {"namespace":"DWES","components":[{"name":"Badge","sourcePath":"components/general/Badge/Badge.jsx"},{"name":"Button","sourcePath":"components/general/Button/Button.jsx"},{"name":"ButtonHintPopover","sourcePath":"components/general/ButtonHintPopover/ButtonHintPopover.jsx"},{"name":"CableSchematic","sourcePath":"components/general/CableSchematic/CableSchematic.jsx"},{"name":"Card","sourcePath":"components/general/Card/Card.jsx"},{"name":"ComboField","sourcePath":"components/general/ComboField/ComboField.jsx"},{"name":"CompanyLogo","sourcePath":"components/general/CompanyLogo/CompanyLogo.jsx"},{"name":"CompletionReport","sourcePath":"components/general/CompletionReport/CompletionReport.jsx"},{"name":"DashboardShell","sourcePath":"components/general/DashboardShell/DashboardShell.jsx"},{"name":"DataTable","sourcePath":"components/general/DataTable/DataTable.jsx"},{"name":"EmptyState","sourcePath":"components/general/EmptyState/EmptyState.jsx"},{"name":"FormError","sourcePath":"components/general/FormError/FormError.jsx"},{"name":"FormGroup","sourcePath":"components/general/FormGroup/FormGroup.jsx"},{"name":"FormInput","sourcePath":"components/general/FormInput/FormInput.jsx"},{"name":"FormLabel","sourcePath":"components/general/FormLabel/FormLabel.jsx"},{"name":"FormSelect","sourcePath":"components/general/FormSelect/FormSelect.jsx"},{"name":"FormTextarea","sourcePath":"components/general/FormTextarea/FormTextarea.jsx"},{"name":"Icon","sourcePath":"components/general/Icon/Icon.jsx"},{"name":"Input","sourcePath":"components/general/Input/Input.jsx"},{"name":"InputField","sourcePath":"components/general/InputField/InputField.jsx"},{"name":"KpiCard","sourcePath":"components/general/KpiCard/KpiCard.jsx"},{"name":"Modal","sourcePath":"components/general/Modal/Modal.jsx"},{"name":"OverflowActionMenu","sourcePath":"components/general/OverflowActionMenu/OverflowActionMenu.jsx"},{"name":"ProjectInfoCard","sourcePath":"components/general/ProjectInfoCard/ProjectInfoCard.jsx"},{"name":"ReportPreviewModal","sourcePath":"components/general/ReportPreviewModal/ReportPreviewModal.jsx"},{"name":"SectionHeader","sourcePath":"components/general/SectionHeader/SectionHeader.jsx"},{"name":"SelectField","sourcePath":"components/general/SelectField/SelectField.jsx"},{"name":"Skeleton","sourcePath":"components/general/Skeleton/Skeleton.jsx"},{"name":"ThemeToggle","sourcePath":"components/general/ThemeToggle/ThemeToggle.jsx"},{"name":"Toast","sourcePath":"components/general/Toast/Toast.jsx"},{"name":"VerificationModal","sourcePath":"components/general/VerificationModal/VerificationModal.jsx"}],"sourceHashes":{"components/general/Badge/Badge.jsx":"59537e2c0e28","components/general/Badge/Badge.d.ts":"b954e01df0ee","components/general/Badge/Badge.prompt.md":"1d6c130f2d77","components/general/Button/Button.jsx":"a2dcbdcebde8","components/general/Button/Button.d.ts":"e4450d822c68","components/general/Button/Button.prompt.md":"e3df344f810f","components/general/ButtonHintPopover/ButtonHintPopover.jsx":"72c4046328bd","components/general/ButtonHintPopover/ButtonHintPopover.d.ts":"0a77fcb19350","components/general/ButtonHintPopover/ButtonHintPopover.prompt.md":"e409a6478cf7","components/general/CableSchematic/CableSchematic.jsx":"17823ef5b370","components/general/CableSchematic/CableSchematic.d.ts":"c3174f2ba937","components/general/CableSchematic/CableSchematic.prompt.md":"d615a893275b","components/general/Card/Card.jsx":"f9dcbd9052be","components/general/Card/Card.d.ts":"4123c191009e","components/general/Card/Card.prompt.md":"cdf95ed0acd0","components/general/ComboField/ComboField.jsx":"2ce2f8baa3f6","components/general/ComboField/ComboField.d.ts":"da7e835f4552","components/general/ComboField/ComboField.prompt.md":"ec7d56efe9be","components/general/CompanyLogo/CompanyLogo.jsx":"ec7351943486","components/general/CompanyLogo/CompanyLogo.d.ts":"f75326e5d8fc","components/general/CompanyLogo/CompanyLogo.prompt.md":"c7481f428b5d","components/general/CompletionReport/CompletionReport.jsx":"73d52711431b","components/general/CompletionReport/CompletionReport.d.ts":"439b5560c524","components/general/CompletionReport/CompletionReport.prompt.md":"c2d46dbeb38a","components/general/DashboardShell/DashboardShell.jsx":"deec0faa3334","components/general/DashboardShell/DashboardShell.d.ts":"69c857d9c115","components/general/DashboardShell/DashboardShell.prompt.md":"81b91684a89f","components/general/DataTable/DataTable.jsx":"20e286eb98fb","components/general/DataTable/DataTable.d.ts":"8471b2454f69","components/general/DataTable/DataTable.prompt.md":"91fc75e8dd08","components/general/EmptyState/EmptyState.jsx":"38dfe8c28a1d","components/general/EmptyState/EmptyState.d.ts":"a7420ef1109b","components/general/EmptyState/EmptyState.prompt.md":"2c4e401488e0","components/general/FormError/FormError.jsx":"f7ec85a89b5a","components/general/FormError/FormError.d.ts":"8673ae62073e","components/general/FormError/FormError.prompt.md":"de98c94746c5","components/general/FormGroup/FormGroup.jsx":"bc5dff74aa30","components/general/FormGroup/FormGroup.d.ts":"c1873a8b81ea","components/general/FormGroup/FormGroup.prompt.md":"1afb2f1c454e","components/general/FormInput/FormInput.jsx":"09d8ec90df6d","components/general/FormInput/FormInput.d.ts":"58f97d110610","components/general/FormInput/FormInput.prompt.md":"7e1395a53169","components/general/FormLabel/FormLabel.jsx":"c5fbeb6654c5","components/general/FormLabel/FormLabel.d.ts":"f9e4ab33bcd9","components/general/FormLabel/FormLabel.prompt.md":"11ce485c19b9","components/general/FormSelect/FormSelect.jsx":"072cdca1d1e9","components/general/FormSelect/FormSelect.d.ts":"934619394471","components/general/FormSelect/FormSelect.prompt.md":"b251278357f7","components/general/FormTextarea/FormTextarea.jsx":"ae244a6c6726","components/general/FormTextarea/FormTextarea.d.ts":"a3695b2dc9e6","components/general/FormTextarea/FormTextarea.prompt.md":"ec45ca61b6a1","components/general/Icon/Icon.jsx":"0779d347333e","components/general/Icon/Icon.d.ts":"de2035aaaeb7","components/general/Icon/Icon.prompt.md":"708c382f1c60","components/general/Input/Input.jsx":"f3d8ee957089","components/general/Input/Input.d.ts":"b3f686e71e69","components/general/Input/Input.prompt.md":"7328ff119aea","components/general/InputField/InputField.jsx":"6d4e31c9c97c","components/general/InputField/InputField.d.ts":"eccf85f6192b","components/general/InputField/InputField.prompt.md":"678773413cca","components/general/KpiCard/KpiCard.jsx":"62e661503012","components/general/KpiCard/KpiCard.d.ts":"3cac6b85b0e8","components/general/KpiCard/KpiCard.prompt.md":"41e8eefb40b3","components/general/Modal/Modal.jsx":"faff9043d49d","components/general/Modal/Modal.d.ts":"f0571024d40b","components/general/Modal/Modal.prompt.md":"557bc4fb54cc","components/general/OverflowActionMenu/OverflowActionMenu.jsx":"e60c9bfa6c1c","components/general/OverflowActionMenu/OverflowActionMenu.d.ts":"120ebc40f38d","components/general/OverflowActionMenu/OverflowActionMenu.prompt.md":"4f25dcb14116","components/general/ProjectInfoCard/ProjectInfoCard.jsx":"bed69b3e46c6","components/general/ProjectInfoCard/ProjectInfoCard.d.ts":"83206a39b0e2","components/general/ProjectInfoCard/ProjectInfoCard.prompt.md":"60a7567d0cba","components/general/ReportPreviewModal/ReportPreviewModal.jsx":"0d152165799f","components/general/ReportPreviewModal/ReportPreviewModal.d.ts":"17d7f075c885","components/general/ReportPreviewModal/ReportPreviewModal.prompt.md":"f468c4996ed8","components/general/SectionHeader/SectionHeader.jsx":"5f2989eed7cb","components/general/SectionHeader/SectionHeader.d.ts":"c15d2ee3d662","components/general/SectionHeader/SectionHeader.prompt.md":"74d740bb8446","components/general/SelectField/SelectField.jsx":"5225664a9c53","components/general/SelectField/SelectField.d.ts":"1fb73f5957b3","components/general/SelectField/SelectField.prompt.md":"c8ed40b460ea","components/general/Skeleton/Skeleton.jsx":"1ab44a8941e9","components/general/Skeleton/Skeleton.d.ts":"d7a67cb17101","components/general/Skeleton/Skeleton.prompt.md":"f72e5e71d624","components/general/ThemeToggle/ThemeToggle.jsx":"53e2d83167fe","components/general/ThemeToggle/ThemeToggle.d.ts":"4f577d8154af","components/general/ThemeToggle/ThemeToggle.prompt.md":"71317675ebdc","components/general/Toast/Toast.jsx":"f223743d5263","components/general/Toast/Toast.d.ts":"56fefe2a7767","components/general/Toast/Toast.prompt.md":"803bbc93e459","components/general/VerificationModal/VerificationModal.jsx":"5739c26e006c","components/general/VerificationModal/VerificationModal.d.ts":"360f201f3d32","components/general/VerificationModal/VerificationModal.prompt.md":"171f95fc8d01"},"inlinedExternals":["@simplewebauthn/browser","axios","cookie","react-router","react-router-dom","set-cookie-parser","zustand"],"builtBy":"cc-design-sync"} */
var DWES = (() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __esm = (fn, res, err) => function __init() {
    if (err) throw err[0];
    try {
      return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
    } catch (e) {
      throw err = [e], e;
    }
  };
  var __commonJS = (cb, mod) => function __require() {
    try {
      return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
    } catch (e) {
      throw mod = 0, e;
    }
  };
  var __export = (target, all3) => {
    for (var name in all3)
      __defProp(target, name, { get: all3[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // <define:import.meta.env>
  var define_import_meta_env_default;
  var init_define_import_meta_env = __esm({
    "<define:import.meta.env>"() {
      define_import_meta_env_default = { MODE: "development", DEV: true, PROD: false, SSR: false, BASE_URL: "/" };
    }
  });

  // shim:react-shim
  var require_react_shim = __commonJS({
    "shim:react-shim"(exports, module) {
      init_define_import_meta_env();
      var R = window.React;
      function np(p, k) {
        var o = {};
        for (var x in p) if (x !== "children") o[x] = p[x];
        if (k !== void 0) o.key = k;
        return o;
      }
      function jsx(t, p, k) {
        var c = p && p.children;
        return c === void 0 ? R.createElement(t, np(p, k)) : R.createElement(t, np(p, k), c);
      }
      function jsxs(t, p, k) {
        return R.createElement.apply(R, [t, np(p, k)].concat(p.children));
      }
      module.exports = R;
      module.exports.jsx = jsx;
      module.exports.jsxs = jsxs;
      module.exports.jsxDEV = function(t, p, k, s) {
        return (s ? jsxs : jsx)(t, p, k);
      };
      module.exports.Fragment = R.Fragment;
    }
  });

  // shim:react-dom-shim
  var require_react_dom_shim = __commonJS({
    "shim:react-dom-shim"(exports, module) {
      init_define_import_meta_env();
      var D = window.ReactDOM;
      var n = function() {
      };
      module.exports = Object.assign({ preload: n, preinit: n, preconnect: n, prefetchDNS: n, preloadModule: n, preinitModule: n }, D);
    }
  });

  // .design-sync/entry.tsx
  var entry_exports = {};
  __export(entry_exports, {
    APP_THEME: () => APP_THEME,
    Badge: () => Badge,
    Button: () => Button,
    ButtonHintPopover: () => ButtonHintPopover,
    CableSchematic: () => CableSchematic,
    Card: () => Card,
    CardBody: () => CardBody,
    CardBodySm: () => CardBodySm,
    CardContent: () => CardContent,
    CardHeader: () => CardHeader,
    CardTitle: () => CardTitle,
    ComboField: () => ComboField,
    CompanyLogo: () => CompanyLogo,
    CompletionReport: () => CompletionReport,
    DashboardShell: () => DashboardShell,
    DataTable: () => DataTable,
    EmptyState: () => EmptyState,
    EmptyStateIllustration: () => EmptyStateIllustration,
    FormError: () => FormError,
    FormGroup: () => FormGroup,
    FormInput: () => FormInput,
    FormLabel: () => FormLabel,
    FormSelect: () => FormSelect,
    FormTextarea: () => FormTextarea,
    Icon: () => Icon,
    Input: () => Input,
    InputField: () => InputField,
    KpiCard: () => KpiCard,
    MemoryRouter: () => MemoryRouter,
    Modal: () => Modal,
    OverflowActionMenu: () => OverflowActionMenu,
    ProjectInfoCard: () => ProjectInfoCard,
    ReportPreviewModal: () => ReportPreviewModal,
    SectionHeader: () => SectionHeader,
    SelectField: () => SelectField,
    Skeleton: () => Skeleton,
    SkeletonCard: () => SkeletonCard,
    SkeletonKpi: () => SkeletonKpi,
    SkeletonList: () => SkeletonList,
    SkeletonTable: () => SkeletonTable,
    ThemeProvider: () => ThemeProvider,
    ThemeToggle: () => ThemeToggle,
    Toast: () => Toast,
    VerificationModal: () => VerificationModal,
    useAuthStore: () => useAuthStore,
    useTheme: () => useTheme
  });
  init_define_import_meta_env();

  // src/components/layout/ThemeProvider.tsx
  init_define_import_meta_env();
  var import_react = __toESM(require_react_shim(), 1);
  var ThemeContext = (0, import_react.createContext)(void 0);
  var APP_THEME = "arctic";
  function useTheme() {
    const context = (0, import_react.useContext)(ThemeContext);
    if (!context) {
      throw new Error("useTheme must be used within ThemeProvider");
    }
    return context;
  }
  function ThemeProvider({ children }) {
    const [mode, setMode] = (0, import_react.useState)(() => {
      const saved = localStorage.getItem("theme-mode");
      if (saved) return saved;
      if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
        return "dark";
      }
      return "light";
    });
    (0, import_react.useEffect)(() => {
      document.documentElement.setAttribute("data-theme", APP_THEME);
      document.documentElement.setAttribute("data-mode", mode);
      localStorage.setItem("theme-mode", mode);
      return () => {
        document.documentElement.removeAttribute("data-theme");
        document.documentElement.removeAttribute("data-mode");
      };
    }, [mode]);
    const toggleMode = () => {
      setMode((prev) => prev === "light" ? "dark" : "light");
    };
    return /* @__PURE__ */ React.createElement(ThemeContext.Provider, { value: { mode, toggleMode } }, children);
  }

  // node_modules/react-router/dist/development/chunk-4ZMWKKQ3.mjs
  init_define_import_meta_env();
  var React2 = __toESM(require_react_shim(), 1);
  var React22 = __toESM(require_react_shim(), 1);
  var React3 = __toESM(require_react_shim(), 1);
  var React4 = __toESM(require_react_shim(), 1);
  var React9 = __toESM(require_react_shim(), 1);
  var React8 = __toESM(require_react_shim(), 1);
  var React7 = __toESM(require_react_shim(), 1);
  var React6 = __toESM(require_react_shim(), 1);
  var React5 = __toESM(require_react_shim(), 1);
  var React10 = __toESM(require_react_shim(), 1);
  var React11 = __toESM(require_react_shim(), 1);
  var import_meta = {};
  var ABSOLUTE_URL_REGEX = /^(?:[a-z][a-z0-9+.-]*:|[\\/]{2})/i;
  var PROTOCOL_RELATIVE_URL_REGEX = /^[\\/]{2}/;
  function normalizeProtocolRelativeUrl(url, protocol) {
    return protocol + url.replace(/\\/g, "/");
  }
  function isLocation(obj) {
    return typeof obj === "object" && obj != null && "pathname" in obj && "search" in obj && "hash" in obj && "state" in obj && "key" in obj;
  }
  function createMemoryHistory(options = {}) {
    let { initialEntries = ["/"], initialIndex, v5Compat = false } = options;
    let entries;
    entries = initialEntries.map(
      (entry, index2) => createMemoryLocation(
        entry,
        typeof entry === "string" ? null : entry.state,
        index2 === 0 ? "default" : void 0,
        typeof entry === "string" ? void 0 : entry.mask
      )
    );
    let index = clampIndex(
      initialIndex == null ? entries.length - 1 : initialIndex
    );
    let action = "POP";
    let listener = null;
    function clampIndex(n) {
      return Math.min(Math.max(n, 0), entries.length - 1);
    }
    function getCurrentLocation() {
      return entries[index];
    }
    function createMemoryLocation(to, state = null, key, mask) {
      let location = createLocation(
        entries ? getCurrentLocation().pathname : "/",
        to,
        state,
        key,
        mask
      );
      warning(
        location.pathname.charAt(0) === "/",
        `relative pathnames are not supported in memory history: ${JSON.stringify(
          to
        )}`
      );
      return location;
    }
    function createHref2(to) {
      return typeof to === "string" ? to : createPath(to);
    }
    let history = {
      get index() {
        return index;
      },
      get action() {
        return action;
      },
      get location() {
        return getCurrentLocation();
      },
      createHref: createHref2,
      createURL(to) {
        return new URL(createHref2(to), "http://localhost");
      },
      encodeLocation(to) {
        let path = typeof to === "string" ? parsePath(to) : to;
        return {
          pathname: path.pathname || "",
          search: path.search || "",
          hash: path.hash || ""
        };
      },
      push(to, state) {
        action = "PUSH";
        let nextLocation = isLocation(to) ? to : createMemoryLocation(to, state);
        index += 1;
        entries.splice(index, entries.length, nextLocation);
        if (v5Compat && listener) {
          listener({ action, location: nextLocation, delta: 1 });
        }
      },
      replace(to, state) {
        action = "REPLACE";
        let nextLocation = isLocation(to) ? to : createMemoryLocation(to, state);
        entries[index] = nextLocation;
        if (v5Compat && listener) {
          listener({ action, location: nextLocation, delta: 0 });
        }
      },
      go(delta) {
        action = "POP";
        let nextIndex = clampIndex(index + delta);
        let nextLocation = entries[nextIndex];
        index = nextIndex;
        if (listener) {
          listener({ action, location: nextLocation, delta });
        }
      },
      listen(fn) {
        listener = fn;
        return () => {
          listener = null;
        };
      }
    };
    return history;
  }
  function invariant(value, message) {
    if (value === false || value === null || typeof value === "undefined") {
      throw new Error(message);
    }
  }
  function warning(cond, message) {
    if (!cond) {
      if (typeof console !== "undefined") console.warn(message);
      try {
        throw new Error(message);
      } catch (e) {
      }
    }
  }
  function createKey() {
    return Math.random().toString(36).substring(2, 10);
  }
  function createLocation(current, to, state = null, key, mask) {
    let location = {
      pathname: typeof current === "string" ? current : current.pathname,
      search: "",
      hash: "",
      ...typeof to === "string" ? parsePath(to) : to,
      state,
      // TODO: This could be cleaned up.  push/replace should probably just take
      // full Locations now and avoid the need to run through this flow at all
      // But that's a pretty big refactor to the current test suite so going to
      // keep as is for the time being and just let any incoming keys take precedence
      key: to && to.key || key || createKey(),
      mask
    };
    return location;
  }
  function createPath({
    pathname = "/",
    search = "",
    hash = ""
  }) {
    if (search && search !== "?")
      pathname += search.charAt(0) === "?" ? search : "?" + search;
    if (hash && hash !== "#")
      pathname += hash.charAt(0) === "#" ? hash : "#" + hash;
    return pathname;
  }
  function parsePath(path) {
    let parsedPath = {};
    if (path) {
      let hashIndex = path.indexOf("#");
      if (hashIndex >= 0) {
        parsedPath.hash = path.substring(hashIndex);
        path = path.substring(0, hashIndex);
      }
      let searchIndex = path.indexOf("?");
      if (searchIndex >= 0) {
        parsedPath.search = path.substring(searchIndex);
        path = path.substring(0, searchIndex);
      }
      if (path) {
        parsedPath.pathname = path;
      }
    }
    return parsedPath;
  }
  var _map;
  _map = /* @__PURE__ */ new WeakMap();
  function matchRoutes(routes, locationArg, basename = "/") {
    return matchRoutesImpl(routes, locationArg, basename, false);
  }
  function matchRoutesImpl(routes, locationArg, basename, allowPartial, precomputedBranches) {
    let location = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
    let pathname = stripBasename(location.pathname || "/", basename);
    if (pathname == null) {
      return null;
    }
    let branches = precomputedBranches ?? flattenAndRankRoutes(routes);
    let matches = null;
    let decoded = decodePath(pathname);
    for (let i = 0; matches == null && i < branches.length; ++i) {
      matches = matchRouteBranch(
        branches[i],
        decoded,
        allowPartial
      );
    }
    return matches;
  }
  function convertRouteMatchToUiMatch(match, loaderData) {
    let { route, pathname, params } = match;
    return {
      id: route.id,
      pathname,
      params,
      data: loaderData[route.id],
      loaderData: loaderData[route.id],
      handle: route.handle
    };
  }
  function flattenAndRankRoutes(routes) {
    let branches = flattenRoutes(routes);
    rankRouteBranches(branches);
    return branches;
  }
  function flattenRoutes(routes, branches = [], parentsMeta = [], parentPath = "", _hasParentOptionalSegments = false) {
    let flattenRoute = (route, index, hasParentOptionalSegments = _hasParentOptionalSegments, relativePath) => {
      let meta = {
        relativePath: relativePath === void 0 ? route.path || "" : relativePath,
        caseSensitive: route.caseSensitive === true,
        childrenIndex: index,
        route
      };
      if (meta.relativePath.startsWith("/")) {
        if (!meta.relativePath.startsWith(parentPath) && hasParentOptionalSegments) {
          return;
        }
        invariant(
          meta.relativePath.startsWith(parentPath),
          `Absolute route path "${meta.relativePath}" nested under path "${parentPath}" is not valid. An absolute child route path must start with the combined path of all its parent routes.`
        );
        meta.relativePath = meta.relativePath.slice(parentPath.length);
      }
      let path = joinPaths([parentPath, meta.relativePath]);
      let routesMeta = parentsMeta.concat(meta);
      if (route.children && route.children.length > 0) {
        invariant(
          // Our types know better, but runtime JS may not!
          // @ts-expect-error
          route.index !== true,
          `Index routes must not have child routes. Please remove all child routes from route path "${path}".`
        );
        flattenRoutes(
          route.children,
          branches,
          routesMeta,
          path,
          hasParentOptionalSegments
        );
      }
      if (route.path == null && !route.index) {
        return;
      }
      branches.push({
        path,
        score: computeScore(path, route.index),
        routesMeta: routesMeta.map((meta2, i) => {
          let [matcher, params] = compilePath(
            meta2.relativePath,
            meta2.caseSensitive,
            i === routesMeta.length - 1
          );
          return {
            ...meta2,
            matcher,
            compiledParams: params
          };
        })
      });
    };
    routes.forEach((route, index) => {
      if (route.path === "" || !route.path?.includes("?")) {
        flattenRoute(route, index);
      } else {
        for (let exploded of explodeOptionalSegments(route.path)) {
          flattenRoute(route, index, true, exploded);
        }
      }
    });
    return branches;
  }
  function explodeOptionalSegments(path) {
    let segments = path.split("/");
    if (segments.length === 0) return [];
    let [first, ...rest] = segments;
    let isOptional = first.endsWith("?");
    let required = first.replace(/\?$/, "");
    if (rest.length === 0) {
      return isOptional ? [required, ""] : [required];
    }
    let restExploded = explodeOptionalSegments(rest.join("/"));
    let result = [];
    result.push(
      ...restExploded.map(
        (subpath) => subpath === "" ? required : [required, subpath].join("/")
      )
    );
    if (isOptional) {
      result.push(...restExploded);
    }
    return result.map(
      (exploded) => path.startsWith("/") && exploded === "" ? "/" : exploded
    );
  }
  function rankRouteBranches(branches) {
    branches.sort(
      (a, b) => a.score !== b.score ? b.score - a.score : compareIndexes(
        a.routesMeta.map((meta) => meta.childrenIndex),
        b.routesMeta.map((meta) => meta.childrenIndex)
      )
    );
  }
  var paramRe = /^:[\w-]+$/;
  var dynamicSegmentValue = 3;
  var indexRouteValue = 2;
  var emptySegmentValue = 1;
  var staticSegmentValue = 10;
  var splatPenalty = -2;
  var isSplat = (s) => s === "*";
  function computeScore(path, index) {
    let segments = path.split("/");
    let initialScore = segments.length;
    if (segments.some(isSplat)) {
      initialScore += splatPenalty;
    }
    if (index) {
      initialScore += indexRouteValue;
    }
    return segments.filter((s) => !isSplat(s)).reduce(
      (score, segment) => score + (paramRe.test(segment) ? dynamicSegmentValue : segment === "" ? emptySegmentValue : staticSegmentValue),
      initialScore
    );
  }
  function compareIndexes(a, b) {
    let siblings = a.length === b.length && a.slice(0, -1).every((n, i) => n === b[i]);
    return siblings ? (
      // If two routes are siblings, we should try to match the earlier sibling
      // first. This allows people to have fine-grained control over the matching
      // behavior by simply putting routes with identical paths in the order they
      // want them tried.
      a[a.length - 1] - b[b.length - 1]
    ) : (
      // Otherwise, it doesn't really make sense to rank non-siblings by index,
      // so they sort equally.
      0
    );
  }
  function matchRouteBranch(branch, pathname, allowPartial = false) {
    let { routesMeta } = branch;
    let matchedParams = {};
    let matchedPathname = "/";
    let matches = [];
    for (let i = 0; i < routesMeta.length; ++i) {
      let meta = routesMeta[i];
      let end = i === routesMeta.length - 1;
      let remainingPathname = matchedPathname === "/" ? pathname : pathname.slice(matchedPathname.length) || "/";
      let pattern = {
        path: meta.relativePath,
        caseSensitive: meta.caseSensitive,
        end
      };
      let match = (
        // Use precomputed matcher if it exists
        meta.matcher && meta.compiledParams ? matchPathImpl(
          pattern,
          remainingPathname,
          meta.matcher,
          meta.compiledParams
        ) : matchPath(pattern, remainingPathname)
      );
      let route = meta.route;
      if (!match && end && allowPartial && !routesMeta[routesMeta.length - 1].route.index) {
        match = matchPath(
          {
            path: meta.relativePath,
            caseSensitive: meta.caseSensitive,
            end: false
          },
          remainingPathname
        );
      }
      if (!match) {
        return null;
      }
      Object.assign(matchedParams, match.params);
      matches.push({
        // TODO: Can this as be avoided?
        params: matchedParams,
        pathname: joinPaths([matchedPathname, match.pathname]),
        pathnameBase: normalizePathname(
          joinPaths([matchedPathname, match.pathnameBase])
        ),
        route
      });
      if (match.pathnameBase !== "/") {
        matchedPathname = joinPaths([matchedPathname, match.pathnameBase]);
      }
    }
    return matches;
  }
  function matchPath(pattern, pathname) {
    if (typeof pattern === "string") {
      pattern = { path: pattern, caseSensitive: false, end: true };
    }
    let [matcher, compiledParams] = compilePath(
      pattern.path,
      pattern.caseSensitive,
      pattern.end
    );
    return matchPathImpl(pattern, pathname, matcher, compiledParams);
  }
  function matchPathImpl(pattern, pathname, matcher, compiledParams) {
    let match = pathname.match(matcher);
    if (!match) return null;
    let matchedPathname = match[0];
    let pathnameBase = matchedPathname.replace(/(.)\/+$/, "$1");
    let captureGroups = match.slice(1);
    let params = compiledParams.reduce(
      (memo2, { paramName, isOptional }, index) => {
        if (paramName === "*") {
          let splatValue = captureGroups[index] || "";
          pathnameBase = matchedPathname.slice(0, matchedPathname.length - splatValue.length).replace(/(.)\/+$/, "$1");
        }
        const value = captureGroups[index];
        if (isOptional && !value) {
          memo2[paramName] = void 0;
        } else {
          memo2[paramName] = (value || "").replace(/%2F/g, "/");
        }
        return memo2;
      },
      {}
    );
    return {
      params,
      pathname: matchedPathname,
      pathnameBase,
      pattern
    };
  }
  function compilePath(path, caseSensitive = false, end = true) {
    warning(
      path === "*" || !path.endsWith("*") || path.endsWith("/*"),
      `Route path "${path}" will be treated as if it were "${path.replace(/\*$/, "/*")}" because the \`*\` character must always follow a \`/\` in the pattern. To get rid of this warning, please change the route path to "${path.replace(/\*$/, "/*")}".`
    );
    let params = [];
    let regexpSource = "^" + path.replace(/\/*\*?$/, "").replace(/^\/*/, "/").replace(/[\\.*+^${}|()[\]]/g, "\\$&").replace(
      /\/:([\w-]+)(\?)?/g,
      (match, paramName, isOptional, index, str) => {
        params.push({ paramName, isOptional: isOptional != null });
        if (isOptional) {
          let nextChar = str.charAt(index + match.length);
          if (nextChar && nextChar !== "/") {
            return "/([^\\/]*)";
          }
          return "(?:/([^\\/]*))?";
        }
        return "/([^\\/]+)";
      }
    ).replace(/\/([\w-]+)\?(\/|$)/g, "(/$1)?$2");
    if (path.endsWith("*")) {
      params.push({ paramName: "*" });
      regexpSource += path === "*" || path === "/*" ? "(.*)$" : "(?:\\/(.+)|\\/*)$";
    } else if (end) {
      regexpSource += "\\/*$";
    } else if (path !== "" && path !== "/") {
      regexpSource += "(?:(?=\\/|$))";
    } else {
    }
    let matcher = new RegExp(regexpSource, caseSensitive ? void 0 : "i");
    return [matcher, params];
  }
  function decodePath(value) {
    try {
      return value.split("/").map((v) => decodeURIComponent(v).replace(/\//g, "%2F")).join("/");
    } catch (error) {
      warning(
        false,
        `The URL path "${value}" could not be decoded because it is a malformed URL segment. This is probably due to a bad percent encoding (${error}).`
      );
      return value;
    }
  }
  function stripBasename(pathname, basename) {
    if (basename === "/") return pathname;
    if (!pathname.toLowerCase().startsWith(basename.toLowerCase())) {
      return null;
    }
    let startIndex = basename.endsWith("/") ? basename.length - 1 : basename.length;
    let nextChar = pathname.charAt(startIndex);
    if (nextChar && nextChar !== "/") {
      return null;
    }
    return pathname.slice(startIndex) || "/";
  }
  function resolvePath(to, fromPathname = "/") {
    let {
      pathname: toPathname,
      search = "",
      hash = ""
    } = typeof to === "string" ? parsePath(to) : to;
    let pathname;
    if (toPathname) {
      toPathname = removeDoubleSlashes(toPathname);
      if (toPathname.startsWith("/")) {
        pathname = resolvePathname(toPathname.substring(1), "/");
      } else {
        pathname = resolvePathname(toPathname, fromPathname);
      }
    } else {
      pathname = fromPathname;
    }
    return {
      pathname,
      search: normalizeSearch(search),
      hash: normalizeHash(hash)
    };
  }
  function resolvePathname(relativePath, fromPathname) {
    let segments = removeTrailingSlash(fromPathname).split("/");
    let relativeSegments = relativePath.split("/");
    relativeSegments.forEach((segment) => {
      if (segment === "..") {
        if (segments.length > 1) segments.pop();
      } else if (segment !== ".") {
        segments.push(segment);
      }
    });
    return segments.length > 1 ? segments.join("/") : "/";
  }
  function getInvalidPathError(char, field, dest, path) {
    return `Cannot include a '${char}' character in a manually specified \`to.${field}\` field [${JSON.stringify(
      path
    )}].  Please separate it out to the \`to.${dest}\` field. Alternatively you may provide the full path as a string in <Link to="..."> and the router will parse it for you.`;
  }
  function getPathContributingMatches(matches) {
    return matches.filter(
      (match, index) => index === 0 || match.route.path && match.route.path.length > 0
    );
  }
  function getResolveToMatches(matches) {
    let pathMatches = getPathContributingMatches(matches);
    return pathMatches.map(
      (match, idx) => idx === pathMatches.length - 1 ? match.pathname : match.pathnameBase
    );
  }
  function resolveTo(toArg, routePathnames, locationPathname, isPathRelative = false) {
    let to;
    if (typeof toArg === "string") {
      to = parsePath(toArg);
    } else {
      to = { ...toArg };
      invariant(
        !to.pathname || !to.pathname.includes("?"),
        getInvalidPathError("?", "pathname", "search", to)
      );
      invariant(
        !to.pathname || !to.pathname.includes("#"),
        getInvalidPathError("#", "pathname", "hash", to)
      );
      invariant(
        !to.search || !to.search.includes("#"),
        getInvalidPathError("#", "search", "hash", to)
      );
    }
    let isEmptyPath = toArg === "" || to.pathname === "";
    let toPathname = isEmptyPath ? "/" : to.pathname;
    let from;
    if (toPathname == null) {
      from = locationPathname;
    } else {
      let routePathnameIndex = routePathnames.length - 1;
      if (!isPathRelative && toPathname.startsWith("..")) {
        let toSegments = toPathname.split("/");
        while (toSegments[0] === "..") {
          toSegments.shift();
          routePathnameIndex -= 1;
        }
        to.pathname = toSegments.join("/");
      }
      from = routePathnameIndex >= 0 ? routePathnames[routePathnameIndex] : "/";
    }
    let path = resolvePath(to, from);
    let hasExplicitTrailingSlash = toPathname && toPathname !== "/" && toPathname.endsWith("/");
    let hasCurrentTrailingSlash = (isEmptyPath || toPathname === ".") && locationPathname.endsWith("/");
    if (!path.pathname.endsWith("/") && (hasExplicitTrailingSlash || hasCurrentTrailingSlash)) {
      path.pathname += "/";
    }
    return path;
  }
  var removeDoubleSlashes = (path) => path.replace(/[\\/]{2,}/g, "/");
  var joinPaths = (paths) => removeDoubleSlashes(paths.join("/"));
  var removeTrailingSlash = (path) => path.replace(/\/+$/, "");
  var normalizePathname = (pathname) => removeTrailingSlash(pathname).replace(/^\/*/, "/");
  var normalizeSearch = (search) => !search || search === "?" ? "" : search.startsWith("?") ? search : "?" + search;
  var normalizeHash = (hash) => !hash || hash === "#" ? "" : hash.startsWith("#") ? hash : "#" + hash;
  var ErrorResponseImpl = class {
    constructor(status, statusText, data2, internal = false) {
      this.status = status;
      this.statusText = statusText || "";
      this.internal = internal;
      if (data2 instanceof Error) {
        this.data = data2.toString();
        this.error = data2;
      } else {
        this.data = data2;
      }
    }
  };
  function isRouteErrorResponse(error) {
    return error != null && typeof error.status === "number" && typeof error.statusText === "string" && typeof error.internal === "boolean" && "data" in error;
  }
  function getRoutePattern(matches) {
    let parts = matches.map((m) => m.route.path).filter(Boolean);
    return joinPaths(parts) || "/";
  }
  var isBrowser = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
  function parseToInfo(_to, basename) {
    let to = _to;
    if (typeof to !== "string" || !ABSOLUTE_URL_REGEX.test(to)) {
      return {
        absoluteURL: void 0,
        isExternal: false,
        to
      };
    }
    let absoluteURL = to;
    let isExternal = false;
    if (isBrowser) {
      try {
        let currentUrl = new URL(window.location.href);
        let targetUrl = PROTOCOL_RELATIVE_URL_REGEX.test(to) ? new URL(normalizeProtocolRelativeUrl(to, currentUrl.protocol)) : new URL(to);
        let path = stripBasename(targetUrl.pathname, basename);
        if (targetUrl.origin === currentUrl.origin && path != null) {
          to = path + targetUrl.search + targetUrl.hash;
        } else {
          isExternal = true;
        }
      } catch (e) {
        warning(
          false,
          `<Link to="${to}"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.`
        );
      }
    }
    return {
      absoluteURL,
      isExternal,
      to
    };
  }
  var objectProtoNames = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
  var validMutationMethodsArr = [
    "POST",
    "PUT",
    "PATCH",
    "DELETE"
  ];
  var validMutationMethods = new Set(
    validMutationMethodsArr
  );
  var validRequestMethodsArr = [
    "GET",
    ...validMutationMethodsArr
  ];
  var validRequestMethods = new Set(validRequestMethodsArr);
  var _routes;
  var _branches;
  var _hmrRoutes;
  var _hmrBranches;
  _routes = /* @__PURE__ */ new WeakMap();
  _branches = /* @__PURE__ */ new WeakMap();
  _hmrRoutes = /* @__PURE__ */ new WeakMap();
  _hmrBranches = /* @__PURE__ */ new WeakMap();
  var invalidProtocols = [
    "about:",
    "blob:",
    "chrome:",
    "chrome-untrusted:",
    "content:",
    "data:",
    "devtools:",
    "file:",
    "filesystem:",
    // eslint-disable-next-line no-script-url
    "javascript:"
  ];
  function hasInvalidProtocol(location) {
    try {
      return invalidProtocols.includes(new URL(location).protocol);
    } catch {
      return false;
    }
  }
  var DataRouterContext = React2.createContext(null);
  DataRouterContext.displayName = "DataRouter";
  var DataRouterStateContext = React2.createContext(null);
  DataRouterStateContext.displayName = "DataRouterState";
  var RSCRouterContext = React2.createContext(false);
  function useIsRSCRouterContext() {
    return React2.useContext(RSCRouterContext);
  }
  var ViewTransitionContext = React2.createContext({
    isTransitioning: false
  });
  ViewTransitionContext.displayName = "ViewTransition";
  var FetchersContext = React2.createContext(
    /* @__PURE__ */ new Map()
  );
  FetchersContext.displayName = "Fetchers";
  var AwaitContext = React2.createContext(null);
  AwaitContext.displayName = "Await";
  var NavigationContext = React2.createContext(
    null
  );
  NavigationContext.displayName = "Navigation";
  var LocationContext = React2.createContext(
    null
  );
  LocationContext.displayName = "Location";
  var RouteContext = React2.createContext({
    outlet: null,
    matches: [],
    isDataRoute: false
  });
  RouteContext.displayName = "Route";
  var RouteErrorContext = React2.createContext(null);
  RouteErrorContext.displayName = "RouteError";
  var ENABLE_DEV_WARNINGS = true;
  var ERROR_DIGEST_BASE = "REACT_ROUTER_ERROR";
  var ERROR_DIGEST_REDIRECT = "REDIRECT";
  var ERROR_DIGEST_ROUTE_ERROR_RESPONSE = "ROUTE_ERROR_RESPONSE";
  function decodeRedirectErrorDigest(digest) {
    if (digest.startsWith(`${ERROR_DIGEST_BASE}:${ERROR_DIGEST_REDIRECT}:{`)) {
      try {
        let parsed = JSON.parse(digest.slice(28));
        if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string" && typeof parsed.location === "string" && typeof parsed.reloadDocument === "boolean" && typeof parsed.replace === "boolean") {
          return parsed;
        }
      } catch {
      }
    }
  }
  function decodeRouteErrorResponseDigest(digest) {
    if (digest.startsWith(
      `${ERROR_DIGEST_BASE}:${ERROR_DIGEST_ROUTE_ERROR_RESPONSE}:{`
    )) {
      try {
        let parsed = JSON.parse(digest.slice(40));
        if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string") {
          return new ErrorResponseImpl(
            parsed.status,
            parsed.statusText,
            parsed.data
          );
        }
      } catch {
      }
    }
  }
  function useHref(to, { relative } = {}) {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useHref() may be used only in the context of a <Router> component.`
    );
    let { basename, navigator: navigator2 } = React22.useContext(NavigationContext);
    let { hash, pathname, search } = useResolvedPath(to, { relative });
    let joinedPathname = pathname;
    if (basename !== "/") {
      joinedPathname = pathname === "/" ? basename : joinPaths([basename, pathname]);
    }
    return navigator2.createHref({ pathname: joinedPathname, search, hash });
  }
  function useInRouterContext() {
    return React22.useContext(LocationContext) != null;
  }
  function useLocation() {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useLocation() may be used only in the context of a <Router> component.`
    );
    return React22.useContext(LocationContext).location;
  }
  var navigateEffectWarning = `You should call navigate() in a React.useEffect(), not when your component is first rendered.`;
  function useIsomorphicLayoutEffect(cb) {
    let isStatic = React22.useContext(NavigationContext).static;
    if (!isStatic) {
      React22.useLayoutEffect(cb);
    }
  }
  function useNavigate() {
    let { isDataRoute } = React22.useContext(RouteContext);
    return isDataRoute ? useNavigateStable() : useNavigateUnstable();
  }
  function useNavigateUnstable() {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useNavigate() may be used only in the context of a <Router> component.`
    );
    let dataRouterContext = React22.useContext(DataRouterContext);
    let { basename, navigator: navigator2 } = React22.useContext(NavigationContext);
    let { matches } = React22.useContext(RouteContext);
    let { pathname: locationPathname } = useLocation();
    let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
    let activeRef = React22.useRef(false);
    useIsomorphicLayoutEffect(() => {
      activeRef.current = true;
    });
    let navigate = React22.useCallback(
      (to, options = {}) => {
        warning(activeRef.current, navigateEffectWarning);
        if (!activeRef.current) return;
        if (typeof to === "number") {
          navigator2.go(to);
          return;
        }
        let path = resolveTo(
          to,
          JSON.parse(routePathnamesJson),
          locationPathname,
          options.relative === "path"
        );
        if (dataRouterContext == null && basename !== "/") {
          path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
        }
        (!!options.replace ? navigator2.replace : navigator2.push)(
          path,
          options.state,
          options
        );
      },
      [
        basename,
        navigator2,
        routePathnamesJson,
        locationPathname,
        dataRouterContext
      ]
    );
    return navigate;
  }
  var OutletContext = React22.createContext(null);
  function useResolvedPath(to, { relative } = {}) {
    let { matches } = React22.useContext(RouteContext);
    let { pathname: locationPathname } = useLocation();
    let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
    return React22.useMemo(
      () => resolveTo(
        to,
        JSON.parse(routePathnamesJson),
        locationPathname,
        relative === "path"
      ),
      [to, routePathnamesJson, locationPathname, relative]
    );
  }
  function useRoutesImpl(routes, locationArg, dataRouterOpts) {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useRoutes() may be used only in the context of a <Router> component.`
    );
    let { navigator: navigator2 } = React22.useContext(NavigationContext);
    let { matches: parentMatches } = React22.useContext(RouteContext);
    let routeMatch = parentMatches[parentMatches.length - 1];
    let parentParams = routeMatch ? routeMatch.params : {};
    let parentPathname = routeMatch ? routeMatch.pathname : "/";
    let parentPathnameBase = routeMatch ? routeMatch.pathnameBase : "/";
    let parentRoute = routeMatch && routeMatch.route;
    if (ENABLE_DEV_WARNINGS) {
      let parentPath = parentRoute && parentRoute.path || "";
      warningOnce(
        parentPathname,
        !parentRoute || parentPath.endsWith("*") || parentPath.endsWith("*?"),
        `You rendered descendant <Routes> (or called \`useRoutes()\`) at "${parentPathname}" (under <Route path="${parentPath}">) but the parent route path has no trailing "*". This means if you navigate deeper, the parent won't match anymore and therefore the child routes will never render.

Please change the parent <Route path="${parentPath}"> to <Route path="${parentPath === "/" ? "*" : `${parentPath}/*`}">.`
      );
    }
    let locationFromContext = useLocation();
    let location;
    if (locationArg) {
      let parsedLocationArg = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
      invariant(
        parentPathnameBase === "/" || parsedLocationArg.pathname?.startsWith(parentPathnameBase),
        `When overriding the location using \`<Routes location>\` or \`useRoutes(routes, location)\`, the location pathname must begin with the portion of the URL pathname that was matched by all parent routes. The current pathname base is "${parentPathnameBase}" but pathname "${parsedLocationArg.pathname}" was given in the \`location\` prop.`
      );
      location = parsedLocationArg;
    } else {
      location = locationFromContext;
    }
    let pathname = location.pathname || "/";
    let remainingPathname = pathname;
    if (parentPathnameBase !== "/") {
      let parentSegments = parentPathnameBase.replace(/^\//, "").split("/");
      let segments = pathname.replace(/^\//, "").split("/");
      remainingPathname = "/" + segments.slice(parentSegments.length).join("/");
    }
    let matches = dataRouterOpts && dataRouterOpts.state.matches.length ? (
      // If we're in a data router, use the matches we've already identified but ensure
      // we have the latest route instances from the manifest in case elements have changed
      dataRouterOpts.state.matches.map(
        (m) => Object.assign(m, {
          route: dataRouterOpts.manifest[m.route.id] || m.route
        })
      )
    ) : matchRoutes(routes, { pathname: remainingPathname });
    if (ENABLE_DEV_WARNINGS) {
      warning(
        parentRoute || matches != null,
        `No routes matched location "${location.pathname}${location.search}${location.hash}" `
      );
      warning(
        matches == null || matches[matches.length - 1].route.element !== void 0 || matches[matches.length - 1].route.Component !== void 0 || matches[matches.length - 1].route.lazy !== void 0,
        `Matched leaf route at location "${location.pathname}${location.search}${location.hash}" does not have an element or Component. This means it will render an <Outlet /> with a null value by default resulting in an "empty" page.`
      );
    }
    let renderedMatches = _renderMatches(
      matches && matches.map(
        (match) => Object.assign({}, match, {
          params: Object.assign({}, parentParams, match.params),
          pathname: joinPaths([
            parentPathnameBase,
            // Re-encode pathnames that were decoded inside matchRoutes.
            // Pre-encode `%`, `?` and `#` ahead of `encodeLocation` because it uses
            // `new URL()` internally and we need to prevent it from treating
            // them as separators
            navigator2.encodeLocation ? navigator2.encodeLocation(
              match.pathname.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")
            ).pathname : match.pathname
          ]),
          pathnameBase: match.pathnameBase === "/" ? parentPathnameBase : joinPaths([
            parentPathnameBase,
            // Re-encode pathnames that were decoded inside matchRoutes
            // Pre-encode `%`, `?` and `#` ahead of `encodeLocation` because it uses
            // `new URL()` internally and we need to prevent it from treating
            // them as separators
            navigator2.encodeLocation ? navigator2.encodeLocation(
              match.pathnameBase.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")
            ).pathname : match.pathnameBase
          ])
        })
      ),
      parentMatches,
      dataRouterOpts
    );
    if (locationArg && renderedMatches) {
      return /* @__PURE__ */ React22.createElement(
        LocationContext.Provider,
        {
          value: {
            location: {
              pathname: "/",
              search: "",
              hash: "",
              state: null,
              key: "default",
              mask: void 0,
              ...location
            },
            navigationType: "POP"
            /* Pop */
          }
        },
        renderedMatches
      );
    }
    return renderedMatches;
  }
  function DefaultErrorComponent() {
    let error = useRouteError();
    let message = isRouteErrorResponse(error) ? `${error.status} ${error.statusText}` : error instanceof Error ? error.message : JSON.stringify(error);
    let stack = error instanceof Error ? error.stack : null;
    let lightgrey = "rgba(200,200,200, 0.5)";
    let preStyles = { padding: "0.5rem", backgroundColor: lightgrey };
    let codeStyles = { padding: "2px 4px", backgroundColor: lightgrey };
    let devInfo = null;
    if (ENABLE_DEV_WARNINGS) {
      console.error(
        "Error handled by React Router default ErrorBoundary:",
        error
      );
      devInfo = /* @__PURE__ */ React22.createElement(React22.Fragment, null, /* @__PURE__ */ React22.createElement("p", null, "\u{1F4BF} Hey developer \u{1F44B}"), /* @__PURE__ */ React22.createElement("p", null, "You can provide a way better UX than this when your app throws errors by providing your own ", /* @__PURE__ */ React22.createElement("code", { style: codeStyles }, "ErrorBoundary"), " or", " ", /* @__PURE__ */ React22.createElement("code", { style: codeStyles }, "errorElement"), " prop on your route."));
    }
    return /* @__PURE__ */ React22.createElement(React22.Fragment, null, /* @__PURE__ */ React22.createElement("h2", null, "Unexpected Application Error!"), /* @__PURE__ */ React22.createElement("h3", { style: { fontStyle: "italic" } }, message), stack ? /* @__PURE__ */ React22.createElement("pre", { style: preStyles }, stack) : null, devInfo);
  }
  var defaultErrorElement = /* @__PURE__ */ React22.createElement(DefaultErrorComponent, null);
  var RenderErrorBoundary = class extends React22.Component {
    constructor(props) {
      super(props);
      this.state = {
        location: props.location,
        revalidation: props.revalidation,
        error: props.error
      };
    }
    static getDerivedStateFromError(error) {
      return { error };
    }
    static getDerivedStateFromProps(props, state) {
      if (state.location !== props.location || state.revalidation !== "idle" && props.revalidation === "idle") {
        return {
          error: props.error,
          location: props.location,
          revalidation: props.revalidation
        };
      }
      return {
        error: props.error !== void 0 ? props.error : state.error,
        location: state.location,
        revalidation: props.revalidation || state.revalidation
      };
    }
    componentDidCatch(error, errorInfo) {
      if (this.props.onError) {
        this.props.onError(error, errorInfo);
      } else {
        console.error(
          "React Router caught the following error during render",
          error
        );
      }
    }
    render() {
      let error = this.state.error;
      if (this.context && typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
        const decoded = decodeRouteErrorResponseDigest(error.digest);
        if (decoded) error = decoded;
      }
      let result = error !== void 0 ? /* @__PURE__ */ React22.createElement(RouteContext.Provider, { value: this.props.routeContext }, /* @__PURE__ */ React22.createElement(
        RouteErrorContext.Provider,
        {
          value: error,
          children: this.props.component
        }
      )) : this.props.children;
      if (this.context) {
        return /* @__PURE__ */ React22.createElement(RSCErrorHandler, { error }, result);
      }
      return result;
    }
  };
  RenderErrorBoundary.contextType = RSCRouterContext;
  var errorRedirectHandledMap = /* @__PURE__ */ new WeakMap();
  function RSCErrorHandler({
    children,
    error
  }) {
    let { basename } = React22.useContext(NavigationContext);
    if (typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
      let redirect2 = decodeRedirectErrorDigest(error.digest);
      if (redirect2) {
        let existingRedirect = errorRedirectHandledMap.get(error);
        if (existingRedirect) throw existingRedirect;
        let parsed = parseToInfo(redirect2.location, basename);
        let target = parsed.absoluteURL || parsed.to;
        if (hasInvalidProtocol(target)) {
          throw new Error("Invalid redirect location");
        }
        if (isBrowser && !errorRedirectHandledMap.get(error)) {
          if (parsed.isExternal || redirect2.reloadDocument) {
            window.location.href = target;
          } else {
            const redirectPromise = Promise.resolve().then(
              () => window.__reactRouterDataRouter.navigate(parsed.to, {
                replace: redirect2.replace
              })
            );
            errorRedirectHandledMap.set(error, redirectPromise);
            throw redirectPromise;
          }
        }
        return /* @__PURE__ */ React22.createElement("meta", { httpEquiv: "refresh", content: `0;url=${target}` });
      }
    }
    return children;
  }
  function RenderedRoute({ routeContext, match, children }) {
    let dataRouterContext = React22.useContext(DataRouterContext);
    if (dataRouterContext && dataRouterContext.static && dataRouterContext.staticContext && (match.route.errorElement || match.route.ErrorBoundary)) {
      dataRouterContext.staticContext._deepestRenderedBoundaryId = match.route.id;
    }
    return /* @__PURE__ */ React22.createElement(RouteContext.Provider, { value: routeContext }, children);
  }
  function _renderMatches(matches, parentMatches = [], dataRouterOpts) {
    let dataRouterState = dataRouterOpts?.state;
    if (matches == null) {
      if (!dataRouterState) {
        return null;
      }
      if (dataRouterState.errors) {
        matches = dataRouterState.matches;
      } else if (parentMatches.length === 0 && !dataRouterState.initialized && dataRouterState.matches.length > 0) {
        matches = dataRouterState.matches;
      } else {
        return null;
      }
    }
    let renderedMatches = matches;
    let errors = dataRouterState?.errors;
    if (errors != null) {
      let errorIndex = renderedMatches.findIndex(
        (m) => m.route.id && errors?.[m.route.id] !== void 0
      );
      invariant(
        errorIndex >= 0,
        `Could not find a matching route for errors on route IDs: ${Object.keys(
          errors
        ).join(",")}`
      );
      renderedMatches = renderedMatches.slice(
        0,
        Math.min(renderedMatches.length, errorIndex + 1)
      );
    }
    let renderFallback = false;
    let fallbackIndex = -1;
    if (dataRouterOpts && dataRouterState) {
      renderFallback = dataRouterState.renderFallback;
      for (let i = 0; i < renderedMatches.length; i++) {
        let match = renderedMatches[i];
        if (match.route.HydrateFallback || match.route.hydrateFallbackElement) {
          fallbackIndex = i;
        }
        if (match.route.id) {
          let { loaderData, errors: errors2 } = dataRouterState;
          let needsToRunLoader = match.route.loader && !loaderData.hasOwnProperty(match.route.id) && (!errors2 || errors2[match.route.id] === void 0);
          if (match.route.lazy || needsToRunLoader) {
            if (dataRouterOpts.isStatic) {
              renderFallback = true;
            }
            if (fallbackIndex >= 0) {
              renderedMatches = renderedMatches.slice(0, fallbackIndex + 1);
            } else {
              renderedMatches = [renderedMatches[0]];
            }
            break;
          }
        }
      }
    }
    let onErrorHandler = dataRouterOpts?.onError;
    let onError = dataRouterState && onErrorHandler ? (error, errorInfo) => {
      onErrorHandler(error, {
        location: dataRouterState.location,
        params: dataRouterState.matches?.[0]?.params ?? {},
        pattern: getRoutePattern(dataRouterState.matches),
        errorInfo
      });
    } : void 0;
    return renderedMatches.reduceRight(
      (outlet, match, index) => {
        let error;
        let shouldRenderHydrateFallback = false;
        let errorElement = null;
        let hydrateFallbackElement = null;
        if (dataRouterState) {
          error = errors && match.route.id ? errors[match.route.id] : void 0;
          errorElement = match.route.errorElement || defaultErrorElement;
          if (renderFallback) {
            if (fallbackIndex < 0 && index === 0) {
              warningOnce(
                "route-fallback",
                false,
                "No `HydrateFallback` element provided to render during initial hydration"
              );
              shouldRenderHydrateFallback = true;
              hydrateFallbackElement = null;
            } else if (fallbackIndex === index) {
              shouldRenderHydrateFallback = true;
              hydrateFallbackElement = match.route.hydrateFallbackElement || null;
            }
          }
        }
        let matches2 = parentMatches.concat(renderedMatches.slice(0, index + 1));
        let getChildren = () => {
          let children;
          if (error) {
            children = errorElement;
          } else if (shouldRenderHydrateFallback) {
            children = hydrateFallbackElement;
          } else if (match.route.Component) {
            children = /* @__PURE__ */ React22.createElement(match.route.Component, null);
          } else if (match.route.element) {
            children = match.route.element;
          } else {
            children = outlet;
          }
          return /* @__PURE__ */ React22.createElement(
            RenderedRoute,
            {
              match,
              routeContext: {
                outlet,
                matches: matches2,
                isDataRoute: dataRouterState != null
              },
              children
            }
          );
        };
        return dataRouterState && (match.route.ErrorBoundary || match.route.errorElement || index === 0) ? /* @__PURE__ */ React22.createElement(
          RenderErrorBoundary,
          {
            location: dataRouterState.location,
            revalidation: dataRouterState.revalidation,
            component: errorElement,
            error,
            children: getChildren(),
            routeContext: { outlet: null, matches: matches2, isDataRoute: true },
            onError
          }
        ) : getChildren();
      },
      null
    );
  }
  function getDataRouterConsoleError(hookName) {
    return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
  }
  function useDataRouterContext(hookName) {
    let ctx = React22.useContext(DataRouterContext);
    invariant(ctx, getDataRouterConsoleError(hookName));
    return ctx;
  }
  function useDataRouterState(hookName) {
    let state = React22.useContext(DataRouterStateContext);
    invariant(state, getDataRouterConsoleError(hookName));
    return state;
  }
  function useRouteContext(hookName) {
    let route = React22.useContext(RouteContext);
    invariant(route, getDataRouterConsoleError(hookName));
    return route;
  }
  function useCurrentRouteId(hookName) {
    let route = useRouteContext(hookName);
    let thisRoute = route.matches[route.matches.length - 1];
    invariant(
      thisRoute.route.id,
      `${hookName} can only be used on routes that contain a unique "id"`
    );
    return thisRoute.route.id;
  }
  function useRouteId() {
    return useCurrentRouteId(
      "useRouteId"
      /* UseRouteId */
    );
  }
  function useNavigation() {
    let state = useDataRouterState(
      "useNavigation"
      /* UseNavigation */
    );
    return React22.useMemo(() => {
      let { matches, historyAction, ...rest } = state.navigation;
      return rest;
    }, [state.navigation]);
  }
  function useMatches() {
    let { matches, loaderData } = useDataRouterState(
      "useMatches"
      /* UseMatches */
    );
    return React22.useMemo(
      () => matches.map((m) => convertRouteMatchToUiMatch(m, loaderData)),
      [matches, loaderData]
    );
  }
  function useRouteError() {
    let error = React22.useContext(RouteErrorContext);
    let state = useDataRouterState(
      "useRouteError"
      /* UseRouteError */
    );
    let routeId = useCurrentRouteId(
      "useRouteError"
      /* UseRouteError */
    );
    if (error !== void 0) {
      return error;
    }
    return state.errors?.[routeId];
  }
  function useNavigateStable() {
    let { router } = useDataRouterContext(
      "useNavigate"
      /* UseNavigateStable */
    );
    let id = useCurrentRouteId(
      "useNavigate"
      /* UseNavigateStable */
    );
    let activeRef = React22.useRef(false);
    useIsomorphicLayoutEffect(() => {
      activeRef.current = true;
    });
    let navigate = React22.useCallback(
      async (to, options = {}) => {
        warning(activeRef.current, navigateEffectWarning);
        if (!activeRef.current) return;
        if (typeof to === "number") {
          await router.navigate(to);
        } else {
          await router.navigate(to, { fromRouteId: id, ...options });
        }
      },
      [router, id]
    );
    return navigate;
  }
  var alreadyWarned = {};
  function warningOnce(key, cond, message) {
    if (!cond && !alreadyWarned[key]) {
      alreadyWarned[key] = true;
      warning(false, message);
    }
  }
  var USE_OPTIMISTIC = "useOptimistic";
  var useOptimisticImpl = React3[USE_OPTIMISTIC];
  var MemoizedDataRoutes = React3.memo(DataRoutes2);
  function DataRoutes2({
    routes,
    manifest,
    future,
    state,
    isStatic,
    onError
  }) {
    return useRoutesImpl(routes, void 0, {
      manifest,
      state,
      isStatic,
      onError,
      future
    });
  }
  function MemoryRouter({
    basename,
    children,
    initialEntries,
    initialIndex,
    useTransitions
  }) {
    let historyRef = React3.useRef();
    if (historyRef.current == null) {
      historyRef.current = createMemoryHistory({
        initialEntries,
        initialIndex,
        v5Compat: true
      });
    }
    let history = historyRef.current;
    let [state, setStateImpl] = React3.useState({
      action: history.action,
      location: history.location
    });
    let setState = React3.useCallback(
      (newState) => {
        if (useTransitions === false) {
          setStateImpl(newState);
        } else {
          React3.startTransition(() => setStateImpl(newState));
        }
      },
      [useTransitions]
    );
    React3.useLayoutEffect(() => history.listen(setState), [history, setState]);
    return /* @__PURE__ */ React3.createElement(
      Router,
      {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        useTransitions
      }
    );
  }
  function Router({
    basename: basenameProp = "/",
    children = null,
    location: locationProp,
    navigationType = "POP",
    navigator: navigator2,
    static: staticProp = false,
    useTransitions
  }) {
    invariant(
      !useInRouterContext(),
      `You cannot render a <Router> inside another <Router>. You should never have more than one in your app.`
    );
    let basename = basenameProp.replace(/^\/*/, "/");
    let navigationContext = React3.useMemo(
      () => ({
        basename,
        navigator: navigator2,
        static: staticProp,
        useTransitions,
        future: {}
      }),
      [basename, navigator2, staticProp, useTransitions]
    );
    if (typeof locationProp === "string") {
      locationProp = parsePath(locationProp);
    }
    let {
      pathname = "/",
      search = "",
      hash = "",
      state = null,
      key = "default",
      mask
    } = locationProp;
    let locationContext = React3.useMemo(() => {
      let trailingPathname = stripBasename(pathname, basename);
      if (trailingPathname == null) {
        return null;
      }
      return {
        location: {
          pathname: trailingPathname,
          search,
          hash,
          state,
          key,
          mask
        },
        navigationType
      };
    }, [basename, pathname, search, hash, state, key, navigationType, mask]);
    warning(
      locationContext != null,
      `<Router basename="${basename}"> is not able to match the URL "${pathname}${search}${hash}" because it does not start with the basename, so the <Router> won't render anything.`
    );
    if (locationContext == null) {
      return null;
    }
    return /* @__PURE__ */ React3.createElement(NavigationContext.Provider, { value: navigationContext }, /* @__PURE__ */ React3.createElement(LocationContext.Provider, { children, value: locationContext }));
  }
  var defaultMethod = "get";
  var defaultEncType = "application/x-www-form-urlencoded";
  function isHtmlElement(object) {
    return typeof HTMLElement !== "undefined" && object instanceof HTMLElement;
  }
  function isButtonElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "button";
  }
  function isFormElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "form";
  }
  function isInputElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "input";
  }
  function isModifiedEvent(event) {
    return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
  }
  function shouldProcessLinkClick(event, target) {
    return event.button === 0 && // Ignore everything but left clicks
    (!target || target === "_self") && // Let browser handle "target=_blank" etc.
    !isModifiedEvent(event);
  }
  var _formDataSupportsSubmitter = null;
  function isFormDataSubmitterSupported() {
    if (_formDataSupportsSubmitter === null) {
      try {
        new FormData(
          document.createElement("form"),
          // @ts-expect-error if FormData supports the submitter parameter, this will throw
          0
        );
        _formDataSupportsSubmitter = false;
      } catch (e) {
        _formDataSupportsSubmitter = true;
      }
    }
    return _formDataSupportsSubmitter;
  }
  var supportedFormEncTypes = /* @__PURE__ */ new Set([
    "application/x-www-form-urlencoded",
    "multipart/form-data",
    "text/plain"
  ]);
  function getFormEncType(encType) {
    if (encType != null && !supportedFormEncTypes.has(encType)) {
      warning(
        false,
        `"${encType}" is not a valid \`encType\` for \`<Form>\`/\`<fetcher.Form>\` and will default to "${defaultEncType}"`
      );
      return null;
    }
    return encType;
  }
  function getFormSubmissionInfo(target, basename) {
    let method;
    let action;
    let encType;
    let formData;
    let body;
    if (isFormElement(target)) {
      let attr = target.getAttribute("action");
      action = attr ? stripBasename(attr, basename) : null;
      method = target.getAttribute("method") || defaultMethod;
      encType = getFormEncType(target.getAttribute("enctype")) || defaultEncType;
      formData = new FormData(target);
    } else if (isButtonElement(target) || isInputElement(target) && (target.type === "submit" || target.type === "image")) {
      let form = target.form;
      if (form == null) {
        throw new Error(
          `Cannot submit a <button> or <input type="submit"> without a <form>`
        );
      }
      let attr = target.getAttribute("formaction") || form.getAttribute("action");
      action = attr ? stripBasename(attr, basename) : null;
      method = target.getAttribute("formmethod") || form.getAttribute("method") || defaultMethod;
      encType = getFormEncType(target.getAttribute("formenctype")) || getFormEncType(form.getAttribute("enctype")) || defaultEncType;
      formData = new FormData(form, target);
      if (!isFormDataSubmitterSupported()) {
        let { name, type, value } = target;
        if (type === "image") {
          let prefix = name ? `${name}.` : "";
          formData.append(`${prefix}x`, "0");
          formData.append(`${prefix}y`, "0");
        } else if (name) {
          formData.append(name, value);
        }
      }
    } else if (isHtmlElement(target)) {
      throw new Error(
        `Cannot submit element that is not <form>, <button>, or <input type="submit|image">`
      );
    } else {
      method = defaultMethod;
      action = null;
      encType = defaultEncType;
      body = target;
    }
    if (formData && encType === "text/plain") {
      body = formData;
      formData = void 0;
    }
    return { action, method: method.toLowerCase(), encType, formData, body };
  }
  var objectProtoNames2 = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
  var ESCAPE_LOOKUP = {
    "&": "\\u0026",
    ">": "\\u003e",
    "<": "\\u003c",
    "\u2028": "\\u2028",
    "\u2029": "\\u2029"
  };
  var ESCAPE_REGEX = /[&><\u2028\u2029]/g;
  function escapeHtml(html) {
    return html.replace(ESCAPE_REGEX, (match) => ESCAPE_LOOKUP[match]);
  }
  function invariant2(value, message) {
    if (value === false || value === null || typeof value === "undefined") {
      throw new Error(message);
    }
  }
  function singleFetchUrl(reqUrl, basename, trailingSlashAware, extension) {
    let url = typeof reqUrl === "string" ? new URL(
      reqUrl,
      // This can be called during the SSR flow via PrefetchPageLinksImpl so
      // don't assume window is available
      typeof window === "undefined" ? "server://singlefetch/" : window.location.origin
    ) : reqUrl;
    if (trailingSlashAware) {
      if (url.pathname.endsWith("/")) {
        url.pathname = `${url.pathname}_.${extension}`;
      } else {
        url.pathname = `${url.pathname}.${extension}`;
      }
    } else {
      if (url.pathname === "/") {
        url.pathname = `_root.${extension}`;
      } else if (basename && stripBasename(url.pathname, basename) === "/") {
        url.pathname = `${removeTrailingSlash(basename)}/_root.${extension}`;
      } else {
        url.pathname = `${removeTrailingSlash(url.pathname)}.${extension}`;
      }
    }
    return url;
  }
  async function loadRouteModule(route, routeModulesCache) {
    if (route.id in routeModulesCache) {
      return routeModulesCache[route.id];
    }
    try {
      let routeModule = await import(
        /* @vite-ignore */
        /* webpackIgnore: true */
        route.module
      );
      routeModulesCache[route.id] = routeModule;
      return routeModule;
    } catch (error) {
      console.error(
        `Error loading route module \`${route.module}\`, reloading page...`
      );
      console.error(error);
      if (window.__reactRouterContext && window.__reactRouterContext.isSpaMode && // @ts-expect-error
      import_meta.hot) {
        throw error;
      }
      window.location.reload();
      return new Promise(() => {
      });
    }
  }
  function isPageLinkDescriptor(object) {
    return object != null && typeof object.page === "string";
  }
  function isHtmlLinkDescriptor(object) {
    if (object == null) {
      return false;
    }
    if (object.href == null) {
      return object.rel === "preload" && typeof object.imageSrcSet === "string" && typeof object.imageSizes === "string";
    }
    return typeof object.rel === "string" && typeof object.href === "string";
  }
  async function getKeyedPrefetchLinks(matches, manifest, routeModules) {
    let links = await Promise.all(
      matches.map(async (match) => {
        let route = manifest.routes[match.route.id];
        if (route) {
          let mod = await loadRouteModule(route, routeModules);
          return mod.links ? mod.links() : [];
        }
        return [];
      })
    );
    return dedupeLinkDescriptors(
      links.flat(1).filter(isHtmlLinkDescriptor).filter((link) => link.rel === "stylesheet" || link.rel === "preload").map(
        (link) => link.rel === "stylesheet" ? { ...link, rel: "prefetch", as: "style" } : { ...link, rel: "prefetch" }
      )
    );
  }
  function getNewMatchesForLinks(page, nextMatches, currentMatches, manifest, location, mode) {
    let isNew = (match, index) => {
      if (!currentMatches[index]) return true;
      return match.route.id !== currentMatches[index].route.id;
    };
    let matchPathChanged = (match, index) => {
      return (
        // param change, /users/123 -> /users/456
        currentMatches[index].pathname !== match.pathname || // splat param changed, which is not present in match.path
        // e.g. /files/images/avatar.jpg -> files/finances.xls
        currentMatches[index].route.path?.endsWith("*") && currentMatches[index].params["*"] !== match.params["*"]
      );
    };
    if (mode === "assets") {
      return nextMatches.filter(
        (match, index) => isNew(match, index) || matchPathChanged(match, index)
      );
    }
    if (mode === "data") {
      return nextMatches.filter((match, index) => {
        let manifestRoute = manifest.routes[match.route.id];
        if (!manifestRoute || !manifestRoute.hasLoader) {
          return false;
        }
        if (isNew(match, index) || matchPathChanged(match, index)) {
          return true;
        }
        if (match.route.shouldRevalidate) {
          let routeChoice = match.route.shouldRevalidate({
            currentUrl: new URL(
              location.pathname + location.search + location.hash,
              window.origin
            ),
            currentParams: currentMatches[0]?.params || {},
            nextUrl: new URL(page, window.origin),
            nextParams: match.params,
            defaultShouldRevalidate: true
          });
          if (typeof routeChoice === "boolean") {
            return routeChoice;
          }
        }
        return true;
      });
    }
    return [];
  }
  function getModuleLinkHrefs(matches, manifest, { includeHydrateFallback } = {}) {
    return dedupeHrefs(
      matches.map((match) => {
        let route = manifest.routes[match.route.id];
        if (!route) return [];
        let hrefs = [route.module];
        if (route.clientActionModule) {
          hrefs = hrefs.concat(route.clientActionModule);
        }
        if (route.clientLoaderModule) {
          hrefs = hrefs.concat(route.clientLoaderModule);
        }
        if (includeHydrateFallback && route.hydrateFallbackModule) {
          hrefs = hrefs.concat(route.hydrateFallbackModule);
        }
        if (route.imports) {
          hrefs = hrefs.concat(route.imports);
        }
        return hrefs;
      }).flat(1)
    );
  }
  function dedupeHrefs(hrefs) {
    return [...new Set(hrefs)];
  }
  function sortKeys(obj) {
    let sorted = {};
    let keys = Object.keys(obj).sort();
    for (let key of keys) {
      sorted[key] = obj[key];
    }
    return sorted;
  }
  function dedupeLinkDescriptors(descriptors, preloads) {
    let set = /* @__PURE__ */ new Set();
    let preloadsSet = new Set(preloads);
    return descriptors.reduce((deduped, descriptor) => {
      let alreadyModulePreload = preloads && !isPageLinkDescriptor(descriptor) && descriptor.as === "script" && descriptor.href && preloadsSet.has(descriptor.href);
      if (alreadyModulePreload) {
        return deduped;
      }
      let key = JSON.stringify(sortKeys(descriptor));
      if (!set.has(key)) {
        set.add(key);
        deduped.push({ key, link: descriptor });
      }
      return deduped;
    }, []);
  }
  function useDataRouterContext2() {
    let context = React8.useContext(DataRouterContext);
    invariant2(
      context,
      "You must render this element inside a <DataRouterContext.Provider> element"
    );
    return context;
  }
  function useDataRouterStateContext() {
    let context = React8.useContext(DataRouterStateContext);
    invariant2(
      context,
      "You must render this element inside a <DataRouterStateContext.Provider> element"
    );
    return context;
  }
  var FrameworkContext = React8.createContext(void 0);
  FrameworkContext.displayName = "FrameworkContext";
  function useFrameworkContext() {
    let context = React8.useContext(FrameworkContext);
    invariant2(
      context,
      "You must render this element inside a <HydratedRouter> element"
    );
    return context;
  }
  function usePrefetchBehavior(prefetch, theirElementProps) {
    let frameworkContext = React8.useContext(FrameworkContext);
    let [maybePrefetch, setMaybePrefetch] = React8.useState(false);
    let [shouldPrefetch, setShouldPrefetch] = React8.useState(false);
    let { onFocus, onBlur, onMouseEnter, onMouseLeave, onTouchStart } = theirElementProps;
    let ref = React8.useRef(null);
    React8.useEffect(() => {
      if (prefetch === "render") {
        setShouldPrefetch(true);
      }
      if (prefetch === "viewport") {
        let callback = (entries) => {
          entries.forEach((entry) => {
            setShouldPrefetch(entry.isIntersecting);
          });
        };
        let observer = new IntersectionObserver(callback, { threshold: 0.5 });
        if (ref.current) observer.observe(ref.current);
        return () => {
          observer.disconnect();
        };
      }
    }, [prefetch]);
    React8.useEffect(() => {
      if (maybePrefetch) {
        let id = setTimeout(() => {
          setShouldPrefetch(true);
        }, 100);
        return () => {
          clearTimeout(id);
        };
      }
    }, [maybePrefetch]);
    let setIntent = () => {
      setMaybePrefetch(true);
    };
    let cancelIntent = () => {
      setMaybePrefetch(false);
      setShouldPrefetch(false);
    };
    if (!frameworkContext) {
      return [false, ref, {}];
    }
    if (prefetch !== "intent") {
      return [shouldPrefetch, ref, {}];
    }
    return [
      shouldPrefetch,
      ref,
      {
        onFocus: composeEventHandlers(onFocus, setIntent),
        onBlur: composeEventHandlers(onBlur, cancelIntent),
        onMouseEnter: composeEventHandlers(onMouseEnter, setIntent),
        onMouseLeave: composeEventHandlers(onMouseLeave, cancelIntent),
        onTouchStart: composeEventHandlers(onTouchStart, setIntent)
      }
    ];
  }
  function composeEventHandlers(theirHandler, ourHandler) {
    return (event) => {
      theirHandler && theirHandler(event);
      if (!event.defaultPrevented) {
        ourHandler(event);
      }
    };
  }
  function PrefetchPageLinks({ page, ...linkProps }) {
    let rsc = useIsRSCRouterContext();
    let { nonce: contextNonce } = useFrameworkContext();
    let { router } = useDataRouterContext2();
    let matches = React8.useMemo(
      () => matchRoutes(router.routes, page, router.basename),
      [router.routes, page, router.basename]
    );
    if (!matches) {
      return null;
    }
    if (linkProps.nonce == null && contextNonce) {
      linkProps = { ...linkProps, nonce: contextNonce };
    }
    if (rsc) {
      return /* @__PURE__ */ React8.createElement(RSCPrefetchPageLinksImpl, { page, matches, ...linkProps });
    }
    return /* @__PURE__ */ React8.createElement(PrefetchPageLinksImpl, { page, matches, ...linkProps });
  }
  function useKeyedPrefetchLinks(matches) {
    let { manifest, routeModules } = useFrameworkContext();
    let [keyedPrefetchLinks, setKeyedPrefetchLinks] = React8.useState([]);
    React8.useEffect(() => {
      let interrupted = false;
      void getKeyedPrefetchLinks(matches, manifest, routeModules).then(
        (links) => {
          if (!interrupted) {
            setKeyedPrefetchLinks(links);
          }
        }
      );
      return () => {
        interrupted = true;
      };
    }, [matches, manifest, routeModules]);
    return keyedPrefetchLinks;
  }
  function RSCPrefetchPageLinksImpl({
    page,
    matches: nextMatches,
    ...linkProps
  }) {
    let location = useLocation();
    let { future } = useFrameworkContext();
    let { basename } = useDataRouterContext2();
    let dataHrefs = React8.useMemo(() => {
      if (page === location.pathname + location.search + location.hash) {
        return [];
      }
      let url = singleFetchUrl(
        page,
        basename,
        future.v8_trailingSlashAwareDataRequests,
        "rsc"
      );
      let hasSomeRoutesWithShouldRevalidate = false;
      let targetRoutes = [];
      for (let match of nextMatches) {
        if (typeof match.route.shouldRevalidate === "function") {
          hasSomeRoutesWithShouldRevalidate = true;
        } else {
          targetRoutes.push(match.route.id);
        }
      }
      if (hasSomeRoutesWithShouldRevalidate && targetRoutes.length > 0) {
        url.searchParams.set("_routes", targetRoutes.join(","));
      }
      return [url.pathname + url.search];
    }, [
      basename,
      future.v8_trailingSlashAwareDataRequests,
      page,
      location,
      nextMatches
    ]);
    return /* @__PURE__ */ React8.createElement(React8.Fragment, null, dataHrefs.map((href) => /* @__PURE__ */ React8.createElement("link", { key: href, rel: "prefetch", as: "fetch", href, ...linkProps })));
  }
  function PrefetchPageLinksImpl({
    page,
    matches: nextMatches,
    ...linkProps
  }) {
    let location = useLocation();
    let { future, manifest, routeModules } = useFrameworkContext();
    let { basename } = useDataRouterContext2();
    let { loaderData, matches } = useDataRouterStateContext();
    let newMatchesForData = React8.useMemo(
      () => getNewMatchesForLinks(
        page,
        nextMatches,
        matches,
        manifest,
        location,
        "data"
      ),
      [page, nextMatches, matches, manifest, location]
    );
    let newMatchesForAssets = React8.useMemo(
      () => getNewMatchesForLinks(
        page,
        nextMatches,
        matches,
        manifest,
        location,
        "assets"
      ),
      [page, nextMatches, matches, manifest, location]
    );
    let dataHrefs = React8.useMemo(() => {
      if (page === location.pathname + location.search + location.hash) {
        return [];
      }
      let routesParams = /* @__PURE__ */ new Set();
      let foundOptOutRoute = false;
      nextMatches.forEach((m) => {
        let manifestRoute = manifest.routes[m.route.id];
        if (!manifestRoute || !manifestRoute.hasLoader) {
          return;
        }
        if (!newMatchesForData.some((m2) => m2.route.id === m.route.id) && m.route.id in loaderData && routeModules[m.route.id]?.shouldRevalidate) {
          foundOptOutRoute = true;
        } else if (manifestRoute.hasClientLoader) {
          foundOptOutRoute = true;
        } else {
          routesParams.add(m.route.id);
        }
      });
      if (routesParams.size === 0) {
        return [];
      }
      let url = singleFetchUrl(
        page,
        basename,
        future.v8_trailingSlashAwareDataRequests,
        "data"
      );
      if (foundOptOutRoute && routesParams.size > 0) {
        url.searchParams.set(
          "_routes",
          nextMatches.filter((m) => routesParams.has(m.route.id)).map((m) => m.route.id).join(",")
        );
      }
      return [url.pathname + url.search];
    }, [
      basename,
      future.v8_trailingSlashAwareDataRequests,
      loaderData,
      location,
      manifest,
      newMatchesForData,
      nextMatches,
      page,
      routeModules
    ]);
    let moduleHrefs = React8.useMemo(
      () => getModuleLinkHrefs(newMatchesForAssets, manifest),
      [newMatchesForAssets, manifest]
    );
    let keyedPrefetchLinks = useKeyedPrefetchLinks(newMatchesForAssets);
    return /* @__PURE__ */ React8.createElement(React8.Fragment, null, dataHrefs.map((href) => /* @__PURE__ */ React8.createElement("link", { key: href, rel: "prefetch", as: "fetch", href, ...linkProps })), moduleHrefs.map((href) => /* @__PURE__ */ React8.createElement("link", { key: href, rel: "modulepreload", href, ...linkProps })), keyedPrefetchLinks.map(({ key, link }) => (
      // these don't spread `linkProps` because they are full link descriptors
      // already with their own props
      /* @__PURE__ */ React8.createElement(
        "link",
        {
          key,
          nonce: linkProps.nonce,
          ...link,
          crossOrigin: link.crossOrigin ?? linkProps.crossOrigin
        }
      )
    )));
  }
  function mergeRefs(...refs) {
    return (value) => {
      refs.forEach((ref) => {
        if (typeof ref === "function") {
          ref(value);
        } else if (ref != null) {
          ref.current = value;
        }
      });
    };
  }
  var isBrowser2 = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
  try {
    if (isBrowser2) {
      window.__reactRouterVersion = // @ts-expect-error
      "7.18.0";
    }
  } catch (e) {
  }
  function HistoryRouter({
    basename,
    children,
    history,
    useTransitions
  }) {
    let [state, setStateImpl] = React10.useState({
      action: history.action,
      location: history.location
    });
    let setState = React10.useCallback(
      (newState) => {
        if (useTransitions === false) {
          setStateImpl(newState);
        } else {
          React10.startTransition(() => setStateImpl(newState));
        }
      },
      [useTransitions]
    );
    React10.useLayoutEffect(() => history.listen(setState), [history, setState]);
    return /* @__PURE__ */ React10.createElement(
      Router,
      {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        useTransitions
      }
    );
  }
  HistoryRouter.displayName = "unstable_HistoryRouter";
  var Link = React10.forwardRef(
    function LinkWithRef({
      onClick,
      discover = "render",
      prefetch = "none",
      relative,
      reloadDocument,
      replace: replace2,
      mask,
      state,
      target,
      to,
      preventScrollReset,
      viewTransition,
      defaultShouldRevalidate,
      ...rest
    }, forwardedRef) {
      let { basename, navigator: navigator2, useTransitions } = React10.useContext(NavigationContext);
      let isAbsolute = typeof to === "string" && ABSOLUTE_URL_REGEX.test(to);
      let parsed = parseToInfo(to, basename);
      to = parsed.to;
      let href = useHref(to, { relative });
      let location = useLocation();
      let maskedHref = null;
      if (mask) {
        let resolved = resolveTo(
          mask,
          [],
          location.mask ? location.mask.pathname : "/",
          true
        );
        if (basename !== "/") {
          resolved.pathname = resolved.pathname === "/" ? basename : joinPaths([basename, resolved.pathname]);
        }
        maskedHref = navigator2.createHref(resolved);
      }
      let [shouldPrefetch, prefetchRef, prefetchHandlers] = usePrefetchBehavior(
        prefetch,
        rest
      );
      let internalOnClick = useLinkClickHandler(to, {
        replace: replace2,
        mask,
        state,
        target,
        preventScrollReset,
        relative,
        viewTransition,
        defaultShouldRevalidate,
        useTransitions
      });
      function handleClick(event) {
        if (onClick) onClick(event);
        if (!event.defaultPrevented) {
          internalOnClick(event);
        }
      }
      let isSpaLink = !(parsed.isExternal || reloadDocument);
      let link = (
        // eslint-disable-next-line jsx-a11y/anchor-has-content
        /* @__PURE__ */ React10.createElement(
          "a",
          {
            ...rest,
            ...prefetchHandlers,
            href: (isSpaLink ? maskedHref : void 0) || parsed.absoluteURL || href,
            onClick: isSpaLink ? handleClick : onClick,
            ref: mergeRefs(forwardedRef, prefetchRef),
            target,
            "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
          }
        )
      );
      return shouldPrefetch && !isAbsolute ? /* @__PURE__ */ React10.createElement(React10.Fragment, null, link, /* @__PURE__ */ React10.createElement(PrefetchPageLinks, { page: href })) : link;
    }
  );
  Link.displayName = "Link";
  var NavLink = React10.forwardRef(
    function NavLinkWithRef({
      "aria-current": ariaCurrentProp = "page",
      caseSensitive = false,
      className: classNameProp = "",
      end = false,
      style: styleProp,
      to,
      viewTransition,
      children,
      ...rest
    }, ref) {
      let path = useResolvedPath(to, { relative: rest.relative });
      let location = useLocation();
      let routerState = React10.useContext(DataRouterStateContext);
      let { navigator: navigator2, basename } = React10.useContext(NavigationContext);
      let isTransitioning = routerState != null && // Conditional usage is OK here because the usage of a data router is static
      // eslint-disable-next-line react-hooks/rules-of-hooks
      useViewTransitionState(path) && viewTransition === true;
      let toPathname = navigator2.encodeLocation ? navigator2.encodeLocation(path).pathname : path.pathname;
      let locationPathname = location.pathname;
      let nextLocationPathname = routerState && routerState.navigation && routerState.navigation.location ? routerState.navigation.location.pathname : null;
      if (!caseSensitive) {
        locationPathname = locationPathname.toLowerCase();
        nextLocationPathname = nextLocationPathname ? nextLocationPathname.toLowerCase() : null;
        toPathname = toPathname.toLowerCase();
      }
      if (nextLocationPathname && basename) {
        nextLocationPathname = stripBasename(nextLocationPathname, basename) || nextLocationPathname;
      }
      const endSlashPosition = toPathname !== "/" && toPathname.endsWith("/") ? toPathname.length - 1 : toPathname.length;
      let isActive = locationPathname === toPathname || !end && locationPathname.startsWith(toPathname) && locationPathname.charAt(endSlashPosition) === "/";
      let isPending = nextLocationPathname != null && (nextLocationPathname === toPathname || !end && nextLocationPathname.startsWith(toPathname) && nextLocationPathname.charAt(toPathname.length) === "/");
      let renderProps = {
        isActive,
        isPending,
        isTransitioning
      };
      let ariaCurrent = isActive ? ariaCurrentProp : void 0;
      let className;
      if (typeof classNameProp === "function") {
        className = classNameProp(renderProps);
      } else {
        className = [
          classNameProp,
          isActive ? "active" : null,
          isPending ? "pending" : null,
          isTransitioning ? "transitioning" : null
        ].filter(Boolean).join(" ");
      }
      let style = typeof styleProp === "function" ? styleProp(renderProps) : styleProp;
      return /* @__PURE__ */ React10.createElement(
        Link,
        {
          ...rest,
          "aria-current": ariaCurrent,
          className,
          ref,
          style,
          to,
          viewTransition
        },
        typeof children === "function" ? children(renderProps) : children
      );
    }
  );
  NavLink.displayName = "NavLink";
  var Form = React10.forwardRef(
    ({
      discover = "render",
      fetcherKey,
      navigate,
      reloadDocument,
      replace: replace2,
      state,
      method = defaultMethod,
      action,
      onSubmit,
      relative,
      preventScrollReset,
      viewTransition,
      defaultShouldRevalidate,
      ...props
    }, forwardedRef) => {
      let { useTransitions } = React10.useContext(NavigationContext);
      let submit = useSubmit();
      let formAction = useFormAction(action, { relative });
      let formMethod = method.toLowerCase() === "get" ? "get" : "post";
      let isAbsolute = typeof action === "string" && ABSOLUTE_URL_REGEX.test(action);
      let submitHandler = (event) => {
        onSubmit && onSubmit(event);
        if (event.defaultPrevented) return;
        event.preventDefault();
        let submitter = event.nativeEvent.submitter;
        let submitMethod = submitter?.getAttribute("formmethod") || method;
        let doSubmit = () => submit(submitter || event.currentTarget, {
          fetcherKey,
          method: submitMethod,
          navigate,
          replace: replace2,
          state,
          relative,
          preventScrollReset,
          viewTransition,
          defaultShouldRevalidate
        });
        if (useTransitions && navigate !== false) {
          React10.startTransition(() => doSubmit());
        } else {
          doSubmit();
        }
      };
      return /* @__PURE__ */ React10.createElement(
        "form",
        {
          ref: forwardedRef,
          method: formMethod,
          action: formAction,
          onSubmit: reloadDocument ? onSubmit : submitHandler,
          ...props,
          "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
        }
      );
    }
  );
  Form.displayName = "Form";
  function ScrollRestoration({
    getKey,
    storageKey,
    ...props
  }) {
    let remixContext = React10.useContext(FrameworkContext);
    let { basename } = React10.useContext(NavigationContext);
    let location = useLocation();
    let matches = useMatches();
    useScrollRestoration({ getKey, storageKey });
    let ssrKey = React10.useMemo(
      () => {
        if (!remixContext || !getKey) return null;
        let userKey = getScrollRestorationKey(
          location,
          matches,
          basename,
          getKey
        );
        return userKey !== location.key ? userKey : null;
      },
      // Nah, we only need this the first time for the SSR render
      // eslint-disable-next-line react-hooks/exhaustive-deps
      []
    );
    if (!remixContext || remixContext.isSpaMode) {
      return null;
    }
    let restoreScroll = ((storageKey2, restoreKey) => {
      if (!window.history.state || !window.history.state.key) {
        let key = Math.random().toString(32).slice(2);
        window.history.replaceState({ key }, "");
      }
      try {
        let positions = JSON.parse(sessionStorage.getItem(storageKey2) || "{}");
        let storedY = positions[restoreKey || window.history.state.key];
        if (typeof storedY === "number") {
          window.scrollTo(0, storedY);
        }
      } catch (error) {
        console.error(error);
        sessionStorage.removeItem(storageKey2);
      }
    }).toString();
    if (props.nonce == null && remixContext?.nonce) {
      props.nonce = remixContext.nonce;
    }
    return /* @__PURE__ */ React10.createElement(
      "script",
      {
        ...props,
        suppressHydrationWarning: true,
        dangerouslySetInnerHTML: {
          __html: `(${restoreScroll})(${escapeHtml(
            JSON.stringify(storageKey || SCROLL_RESTORATION_STORAGE_KEY)
          )}, ${escapeHtml(JSON.stringify(ssrKey))})`
        }
      }
    );
  }
  ScrollRestoration.displayName = "ScrollRestoration";
  function getDataRouterConsoleError2(hookName) {
    return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
  }
  function useDataRouterContext3(hookName) {
    let ctx = React10.useContext(DataRouterContext);
    invariant(ctx, getDataRouterConsoleError2(hookName));
    return ctx;
  }
  function useDataRouterState2(hookName) {
    let state = React10.useContext(DataRouterStateContext);
    invariant(state, getDataRouterConsoleError2(hookName));
    return state;
  }
  function useLinkClickHandler(to, {
    target,
    replace: replaceProp,
    mask,
    state,
    preventScrollReset,
    relative,
    viewTransition,
    defaultShouldRevalidate,
    useTransitions
  } = {}) {
    let navigate = useNavigate();
    let location = useLocation();
    let path = useResolvedPath(to, { relative });
    return React10.useCallback(
      (event) => {
        if (shouldProcessLinkClick(event, target)) {
          event.preventDefault();
          let replace2 = replaceProp !== void 0 ? replaceProp : createPath(location) === createPath(path);
          let doNavigate = () => navigate(to, {
            replace: replace2,
            mask,
            state,
            preventScrollReset,
            relative,
            viewTransition,
            defaultShouldRevalidate
          });
          if (useTransitions) {
            React10.startTransition(() => doNavigate());
          } else {
            doNavigate();
          }
        }
      },
      [
        location,
        navigate,
        path,
        replaceProp,
        mask,
        state,
        target,
        to,
        preventScrollReset,
        relative,
        viewTransition,
        defaultShouldRevalidate,
        useTransitions
      ]
    );
  }
  var fetcherId = 0;
  var getUniqueFetcherId = () => `__${String(++fetcherId)}__`;
  function useSubmit() {
    let { router } = useDataRouterContext3(
      "useSubmit"
      /* UseSubmit */
    );
    let { basename } = React10.useContext(NavigationContext);
    let currentRouteId = useRouteId();
    let routerFetch = router.fetch;
    let routerNavigate = router.navigate;
    return React10.useCallback(
      async (target, options = {}) => {
        let { action, method, encType, formData, body } = getFormSubmissionInfo(
          target,
          basename
        );
        if (options.navigate === false) {
          let key = options.fetcherKey || getUniqueFetcherId();
          await routerFetch(key, currentRouteId, options.action || action, {
            defaultShouldRevalidate: options.defaultShouldRevalidate,
            preventScrollReset: options.preventScrollReset,
            formData,
            body,
            formMethod: options.method || method,
            formEncType: options.encType || encType,
            flushSync: options.flushSync
          });
        } else {
          await routerNavigate(options.action || action, {
            defaultShouldRevalidate: options.defaultShouldRevalidate,
            preventScrollReset: options.preventScrollReset,
            formData,
            body,
            formMethod: options.method || method,
            formEncType: options.encType || encType,
            replace: options.replace,
            state: options.state,
            fromRouteId: currentRouteId,
            flushSync: options.flushSync,
            viewTransition: options.viewTransition
          });
        }
      },
      [routerFetch, routerNavigate, basename, currentRouteId]
    );
  }
  function useFormAction(action, { relative } = {}) {
    let { basename } = React10.useContext(NavigationContext);
    let routeContext = React10.useContext(RouteContext);
    invariant(routeContext, "useFormAction must be used inside a RouteContext");
    let [match] = routeContext.matches.slice(-1);
    let path = { ...useResolvedPath(action ? action : ".", { relative }) };
    let location = useLocation();
    if (action == null) {
      path.search = location.search;
      let params = new URLSearchParams(path.search);
      let indexValues = params.getAll("index");
      let hasNakedIndexParam = indexValues.some((v) => v === "");
      if (hasNakedIndexParam) {
        params.delete("index");
        indexValues.filter((v) => v).forEach((v) => params.append("index", v));
        let qs = params.toString();
        path.search = qs ? `?${qs}` : "";
      }
    }
    if ((!action || action === ".") && match.route.index) {
      path.search = path.search ? path.search.replace(/^\?/, "?index&") : "?index";
    }
    if (basename !== "/") {
      path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
    }
    return createPath(path);
  }
  var SCROLL_RESTORATION_STORAGE_KEY = "react-router-scroll-positions";
  var savedScrollPositions = {};
  function getScrollRestorationKey(location, matches, basename, getKey) {
    let key = null;
    if (getKey) {
      if (basename !== "/") {
        key = getKey(
          {
            ...location,
            pathname: stripBasename(location.pathname, basename) || location.pathname
          },
          matches
        );
      } else {
        key = getKey(location, matches);
      }
    }
    if (key == null) {
      key = location.key;
    }
    return key;
  }
  function useScrollRestoration({
    getKey,
    storageKey
  } = {}) {
    let { router } = useDataRouterContext3(
      "useScrollRestoration"
      /* UseScrollRestoration */
    );
    let { restoreScrollPosition, preventScrollReset } = useDataRouterState2(
      "useScrollRestoration"
      /* UseScrollRestoration */
    );
    let { basename } = React10.useContext(NavigationContext);
    let location = useLocation();
    let matches = useMatches();
    let navigation = useNavigation();
    React10.useEffect(() => {
      window.history.scrollRestoration = "manual";
      return () => {
        window.history.scrollRestoration = "auto";
      };
    }, []);
    usePageHide(
      React10.useCallback(() => {
        if (navigation.state === "idle") {
          let key = getScrollRestorationKey(location, matches, basename, getKey);
          savedScrollPositions[key] = window.scrollY;
        }
        try {
          sessionStorage.setItem(
            storageKey || SCROLL_RESTORATION_STORAGE_KEY,
            JSON.stringify(savedScrollPositions)
          );
        } catch (error) {
          warning(
            false,
            `Failed to save scroll positions in sessionStorage, <ScrollRestoration /> will not work properly (${error}).`
          );
        }
        window.history.scrollRestoration = "auto";
      }, [navigation.state, getKey, basename, location, matches, storageKey])
    );
    if (typeof document !== "undefined") {
      React10.useLayoutEffect(() => {
        try {
          let sessionPositions = sessionStorage.getItem(
            storageKey || SCROLL_RESTORATION_STORAGE_KEY
          );
          if (sessionPositions) {
            savedScrollPositions = JSON.parse(sessionPositions);
          }
        } catch (e) {
        }
      }, [storageKey]);
      React10.useLayoutEffect(() => {
        let disableScrollRestoration = router?.enableScrollRestoration(
          savedScrollPositions,
          () => window.scrollY,
          getKey ? (location2, matches2) => getScrollRestorationKey(location2, matches2, basename, getKey) : void 0
        );
        return () => disableScrollRestoration && disableScrollRestoration();
      }, [router, basename, getKey]);
      React10.useLayoutEffect(() => {
        if (restoreScrollPosition === false) {
          return;
        }
        if (typeof restoreScrollPosition === "number") {
          window.scrollTo(0, restoreScrollPosition);
          return;
        }
        try {
          if (location.hash) {
            let el = document.getElementById(
              decodeURIComponent(location.hash.slice(1))
            );
            if (el) {
              el.scrollIntoView();
              return;
            }
          }
        } catch {
          warning(
            false,
            `"${location.hash.slice(
              1
            )}" is not a decodable element ID. The view will not scroll to it.`
          );
        }
        if (preventScrollReset === true) {
          return;
        }
        window.scrollTo(0, 0);
      }, [location, restoreScrollPosition, preventScrollReset]);
    }
  }
  function usePageHide(callback, options) {
    let { capture } = options || {};
    React10.useEffect(() => {
      let opts = capture != null ? { capture } : void 0;
      window.addEventListener("pagehide", callback, opts);
      return () => {
        window.removeEventListener("pagehide", callback, opts);
      };
    }, [callback, capture]);
  }
  function useViewTransitionState(to, { relative } = {}) {
    let vtContext = React10.useContext(ViewTransitionContext);
    invariant(
      vtContext != null,
      "`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?"
    );
    let { basename } = useDataRouterContext3(
      "useViewTransitionState"
      /* useViewTransitionState */
    );
    let path = useResolvedPath(to, { relative });
    if (!vtContext.isTransitioning) {
      return false;
    }
    let currentPath = stripBasename(vtContext.currentLocation.pathname, basename) || vtContext.currentLocation.pathname;
    let nextPath = stripBasename(vtContext.nextLocation.pathname, basename) || vtContext.nextLocation.pathname;
    return matchPath(path.pathname, nextPath) != null || matchPath(path.pathname, currentPath) != null;
  }

  // node_modules/react-router/dist/development/index.mjs
  init_define_import_meta_env();

  // src/store/useAuthStore.ts
  init_define_import_meta_env();

  // node_modules/zustand/esm/vanilla.mjs
  init_define_import_meta_env();
  var createStoreImpl = (createState) => {
    let state;
    const listeners = /* @__PURE__ */ new Set();
    const setState = (partial, replace2) => {
      const nextState = typeof partial === "function" ? partial(state) : partial;
      if (!Object.is(nextState, state)) {
        const previousState = state;
        state = (replace2 != null ? replace2 : typeof nextState !== "object" || nextState === null) ? nextState : Object.assign({}, state, nextState);
        listeners.forEach((listener) => listener(state, previousState));
      }
    };
    const getState = () => state;
    const getInitialState = () => initialState;
    const subscribe = (listener) => {
      listeners.add(listener);
      return () => listeners.delete(listener);
    };
    const api2 = { setState, getState, getInitialState, subscribe };
    const initialState = state = createState(setState, getState, api2);
    return api2;
  };
  var createStore = ((createState) => createState ? createStoreImpl(createState) : createStoreImpl);

  // node_modules/zustand/esm/react.mjs
  init_define_import_meta_env();
  var import_react2 = __toESM(require_react_shim(), 1);
  var identity = (arg) => arg;
  function useStore(api2, selector = identity) {
    const slice = import_react2.default.useSyncExternalStore(
      api2.subscribe,
      import_react2.default.useCallback(() => selector(api2.getState()), [api2, selector]),
      import_react2.default.useCallback(() => selector(api2.getInitialState()), [api2, selector])
    );
    import_react2.default.useDebugValue(slice);
    return slice;
  }
  var createImpl = (createState) => {
    const api2 = createStore(createState);
    const useBoundStore = (selector) => useStore(api2, selector);
    Object.assign(useBoundStore, api2);
    return useBoundStore;
  };
  var create = ((createState) => createState ? createImpl(createState) : createImpl);

  // src/services/api.ts
  init_define_import_meta_env();

  // node_modules/axios/index.js
  init_define_import_meta_env();

  // node_modules/axios/lib/axios.js
  init_define_import_meta_env();

  // node_modules/axios/lib/utils.js
  init_define_import_meta_env();

  // node_modules/axios/lib/helpers/bind.js
  init_define_import_meta_env();
  function bind(fn, thisArg) {
    return function wrap() {
      return fn.apply(thisArg, arguments);
    };
  }

  // node_modules/axios/lib/utils.js
  var { toString } = Object.prototype;
  var { getPrototypeOf } = Object;
  var { iterator, toStringTag } = Symbol;
  var hasOwnProperty = (({ hasOwnProperty: hasOwnProperty2 }) => (obj, prop) => hasOwnProperty2.call(obj, prop))(Object.prototype);
  var hasOwnInPrototypeChain = (thing, prop) => {
    let obj = thing;
    const seen = [];
    while (obj != null && obj !== Object.prototype) {
      if (seen.indexOf(obj) !== -1) {
        return false;
      }
      seen.push(obj);
      if (hasOwnProperty(obj, prop)) {
        return true;
      }
      obj = getPrototypeOf(obj);
    }
    return false;
  };
  var getSafeProp = (obj, prop) => obj != null && hasOwnInPrototypeChain(obj, prop) ? obj[prop] : void 0;
  var kindOf = /* @__PURE__ */ ((cache) => (thing) => {
    const str = toString.call(thing);
    return cache[str] || (cache[str] = str.slice(8, -1).toLowerCase());
  })(/* @__PURE__ */ Object.create(null));
  var kindOfTest = (type) => {
    type = type.toLowerCase();
    return (thing) => kindOf(thing) === type;
  };
  var typeOfTest = (type) => (thing) => typeof thing === type;
  var { isArray } = Array;
  var isUndefined = typeOfTest("undefined");
  function isBuffer(val) {
    return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor) && isFunction(val.constructor.isBuffer) && val.constructor.isBuffer(val);
  }
  var isArrayBuffer = kindOfTest("ArrayBuffer");
  function isArrayBufferView(val) {
    let result;
    if (typeof ArrayBuffer !== "undefined" && ArrayBuffer.isView) {
      result = ArrayBuffer.isView(val);
    } else {
      result = val && val.buffer && isArrayBuffer(val.buffer);
    }
    return result;
  }
  var isString = typeOfTest("string");
  var isFunction = typeOfTest("function");
  var isNumber = typeOfTest("number");
  var isObject = (thing) => thing !== null && typeof thing === "object";
  var isBoolean = (thing) => thing === true || thing === false;
  var isPlainObject = (val) => {
    if (!isObject(val)) {
      return false;
    }
    const prototype2 = getPrototypeOf(val);
    return (prototype2 === null || prototype2 === Object.prototype || getPrototypeOf(prototype2) === null) && // Treat any genuine (non-Object.prototype-polluted) Symbol.toStringTag or
    // Symbol.iterator as evidence the value is a tagged/iterable type rather
    // than a plain object, while ignoring keys injected onto Object.prototype.
    !hasOwnInPrototypeChain(val, toStringTag) && !hasOwnInPrototypeChain(val, iterator);
  };
  var isEmptyObject = (val) => {
    if (!isObject(val) || isBuffer(val)) {
      return false;
    }
    try {
      return Object.keys(val).length === 0 && Object.getPrototypeOf(val) === Object.prototype;
    } catch (e) {
      return false;
    }
  };
  var isDate = kindOfTest("Date");
  var isFile = kindOfTest("File");
  var isReactNativeBlob = (value) => {
    return !!(value && typeof value.uri !== "undefined");
  };
  var isReactNative = (formData) => formData && typeof formData.getParts !== "undefined";
  var isBlob = kindOfTest("Blob");
  var isFileList = kindOfTest("FileList");
  var isStream = (val) => isObject(val) && isFunction(val.pipe);
  function getGlobal() {
    if (typeof globalThis !== "undefined") return globalThis;
    if (typeof self !== "undefined") return self;
    if (typeof window !== "undefined") return window;
    if (typeof global !== "undefined") return global;
    return {};
  }
  var G = getGlobal();
  var FormDataCtor = typeof G.FormData !== "undefined" ? G.FormData : void 0;
  var isFormData = (thing) => {
    if (!thing) return false;
    if (FormDataCtor && thing instanceof FormDataCtor) return true;
    const proto = getPrototypeOf(thing);
    if (!proto || proto === Object.prototype) return false;
    if (!isFunction(thing.append)) return false;
    const kind = kindOf(thing);
    return kind === "formdata" || // detect form-data instance
    kind === "object" && isFunction(thing.toString) && thing.toString() === "[object FormData]";
  };
  var isURLSearchParams = kindOfTest("URLSearchParams");
  var [isReadableStream, isRequest, isResponse, isHeaders] = [
    "ReadableStream",
    "Request",
    "Response",
    "Headers"
  ].map(kindOfTest);
  var trim = (str) => {
    return str.trim ? str.trim() : str.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
  };
  function forEach(obj, fn, { allOwnKeys = false } = {}) {
    if (obj === null || typeof obj === "undefined") {
      return;
    }
    let i;
    let l;
    if (typeof obj !== "object") {
      obj = [obj];
    }
    if (isArray(obj)) {
      for (i = 0, l = obj.length; i < l; i++) {
        fn.call(null, obj[i], i, obj);
      }
    } else {
      if (isBuffer(obj)) {
        return;
      }
      const keys = allOwnKeys ? Object.getOwnPropertyNames(obj) : Object.keys(obj);
      const len = keys.length;
      let key;
      for (i = 0; i < len; i++) {
        key = keys[i];
        fn.call(null, obj[key], key, obj);
      }
    }
  }
  function findKey(obj, key) {
    if (isBuffer(obj)) {
      return null;
    }
    key = key.toLowerCase();
    const keys = Object.keys(obj);
    let i = keys.length;
    let _key;
    while (i-- > 0) {
      _key = keys[i];
      if (key === _key.toLowerCase()) {
        return _key;
      }
    }
    return null;
  }
  var _global = (() => {
    if (typeof globalThis !== "undefined") return globalThis;
    return typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : global;
  })();
  var isContextDefined = (context) => !isUndefined(context) && context !== _global;
  function merge(...objs) {
    const { caseless, skipUndefined } = isContextDefined(this) && this || {};
    const result = {};
    const assignValue = (val, key) => {
      if (key === "__proto__" || key === "constructor" || key === "prototype") {
        return;
      }
      const targetKey = caseless && typeof key === "string" && findKey(result, key) || key;
      const existing = hasOwnProperty(result, targetKey) ? result[targetKey] : void 0;
      if (isPlainObject(existing) && isPlainObject(val)) {
        result[targetKey] = merge(existing, val);
      } else if (isPlainObject(val)) {
        result[targetKey] = merge({}, val);
      } else if (isArray(val)) {
        result[targetKey] = val.slice();
      } else if (!skipUndefined || !isUndefined(val)) {
        result[targetKey] = val;
      }
    };
    for (let i = 0, l = objs.length; i < l; i++) {
      const source = objs[i];
      if (!source || isBuffer(source)) {
        continue;
      }
      forEach(source, assignValue);
      if (typeof source !== "object" || isArray(source)) {
        continue;
      }
      const symbols = Object.getOwnPropertySymbols(source);
      for (let j = 0; j < symbols.length; j++) {
        const symbol = symbols[j];
        if (propertyIsEnumerable.call(source, symbol)) {
          assignValue(source[symbol], symbol);
        }
      }
    }
    return result;
  }
  var extend = (a, b, thisArg, { allOwnKeys } = {}) => {
    forEach(
      b,
      (val, key) => {
        if (thisArg && isFunction(val)) {
          Object.defineProperty(a, key, {
            // Null-proto descriptor so a polluted Object.prototype.get cannot
            // hijack defineProperty's accessor-vs-data resolution.
            __proto__: null,
            value: bind(val, thisArg),
            writable: true,
            enumerable: true,
            configurable: true
          });
        } else {
          Object.defineProperty(a, key, {
            __proto__: null,
            value: val,
            writable: true,
            enumerable: true,
            configurable: true
          });
        }
      },
      { allOwnKeys }
    );
    return a;
  };
  var stripBOM = (content) => {
    if (content.charCodeAt(0) === 65279) {
      content = content.slice(1);
    }
    return content;
  };
  var inherits = (constructor, superConstructor, props, descriptors) => {
    constructor.prototype = Object.create(superConstructor.prototype, descriptors);
    Object.defineProperty(constructor.prototype, "constructor", {
      __proto__: null,
      value: constructor,
      writable: true,
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(constructor, "super", {
      __proto__: null,
      value: superConstructor.prototype
    });
    props && Object.assign(constructor.prototype, props);
  };
  var toFlatObject = (sourceObj, destObj, filter2, propFilter) => {
    let props;
    let i;
    let prop;
    const merged = {};
    destObj = destObj || {};
    if (sourceObj == null) return destObj;
    do {
      props = Object.getOwnPropertyNames(sourceObj);
      i = props.length;
      while (i-- > 0) {
        prop = props[i];
        if ((!propFilter || propFilter(prop, sourceObj, destObj)) && !merged[prop]) {
          destObj[prop] = sourceObj[prop];
          merged[prop] = true;
        }
      }
      sourceObj = filter2 !== false && getPrototypeOf(sourceObj);
    } while (sourceObj && (!filter2 || filter2(sourceObj, destObj)) && sourceObj !== Object.prototype);
    return destObj;
  };
  var endsWith = (str, searchString, position) => {
    str = String(str);
    if (position === void 0 || position > str.length) {
      position = str.length;
    }
    position -= searchString.length;
    const lastIndex = str.indexOf(searchString, position);
    return lastIndex !== -1 && lastIndex === position;
  };
  var toArray = (thing) => {
    if (!thing) return null;
    if (isArray(thing)) return thing;
    let i = thing.length;
    if (!isNumber(i)) return null;
    const arr = new Array(i);
    while (i-- > 0) {
      arr[i] = thing[i];
    }
    return arr;
  };
  var isTypedArray = /* @__PURE__ */ ((TypedArray) => {
    return (thing) => {
      return TypedArray && thing instanceof TypedArray;
    };
  })(typeof Uint8Array !== "undefined" && getPrototypeOf(Uint8Array));
  var forEachEntry = (obj, fn) => {
    const generator = obj && obj[iterator];
    const _iterator = generator.call(obj);
    let result;
    while ((result = _iterator.next()) && !result.done) {
      const pair = result.value;
      fn.call(obj, pair[0], pair[1]);
    }
  };
  var matchAll = (regExp, str) => {
    let matches;
    const arr = [];
    while ((matches = regExp.exec(str)) !== null) {
      arr.push(matches);
    }
    return arr;
  };
  var isHTMLForm = kindOfTest("HTMLFormElement");
  var toCamelCase = (str) => {
    return str.toLowerCase().replace(/[-_\s]([a-z\d])(\w*)/g, function replacer(m, p1, p2) {
      return p1.toUpperCase() + p2;
    });
  };
  var { propertyIsEnumerable } = Object.prototype;
  var isRegExp = kindOfTest("RegExp");
  var reduceDescriptors = (obj, reducer) => {
    const descriptors = Object.getOwnPropertyDescriptors(obj);
    const reducedDescriptors = {};
    forEach(descriptors, (descriptor, name) => {
      let ret;
      if ((ret = reducer(descriptor, name, obj)) !== false) {
        reducedDescriptors[name] = ret || descriptor;
      }
    });
    Object.defineProperties(obj, reducedDescriptors);
  };
  var freezeMethods = (obj) => {
    reduceDescriptors(obj, (descriptor, name) => {
      if (isFunction(obj) && ["arguments", "caller", "callee"].includes(name)) {
        return false;
      }
      const value = obj[name];
      if (!isFunction(value)) return;
      descriptor.enumerable = false;
      if ("writable" in descriptor) {
        descriptor.writable = false;
        return;
      }
      if (!descriptor.set) {
        descriptor.set = () => {
          throw Error("Can not rewrite read-only method '" + name + "'");
        };
      }
    });
  };
  var toObjectSet = (arrayOrString, delimiter) => {
    const obj = {};
    const define = (arr) => {
      arr.forEach((value) => {
        obj[value] = true;
      });
    };
    isArray(arrayOrString) ? define(arrayOrString) : define(String(arrayOrString).split(delimiter));
    return obj;
  };
  var noop = () => {
  };
  var toFiniteNumber = (value, defaultValue) => {
    return value != null && Number.isFinite(value = +value) ? value : defaultValue;
  };
  function isSpecCompliantForm(thing) {
    return !!(thing && isFunction(thing.append) && thing[toStringTag] === "FormData" && thing[iterator]);
  }
  var toJSONObject = (obj) => {
    const visited = /* @__PURE__ */ new WeakSet();
    const visit = (source) => {
      if (isObject(source)) {
        if (visited.has(source)) {
          return;
        }
        if (isBuffer(source)) {
          return source;
        }
        if (!("toJSON" in source)) {
          visited.add(source);
          const target = isArray(source) ? [] : {};
          forEach(source, (value, key) => {
            const reducedValue = visit(value);
            !isUndefined(reducedValue) && (target[key] = reducedValue);
          });
          visited.delete(source);
          return target;
        }
      }
      return source;
    };
    return visit(obj);
  };
  var isAsyncFn = kindOfTest("AsyncFunction");
  var isThenable = (thing) => thing && (isObject(thing) || isFunction(thing)) && isFunction(thing.then) && isFunction(thing.catch);
  var _setImmediate = ((setImmediateSupported, postMessageSupported) => {
    if (setImmediateSupported) {
      return setImmediate;
    }
    return postMessageSupported ? ((token, callbacks) => {
      _global.addEventListener(
        "message",
        ({ source, data: data2 }) => {
          if (source === _global && data2 === token) {
            callbacks.length && callbacks.shift()();
          }
        },
        false
      );
      return (cb) => {
        callbacks.push(cb);
        _global.postMessage(token, "*");
      };
    })(`axios@${Math.random()}`, []) : (cb) => setTimeout(cb);
  })(typeof setImmediate === "function", isFunction(_global.postMessage));
  var asap = typeof queueMicrotask !== "undefined" ? queueMicrotask.bind(_global) : typeof process !== "undefined" && process.nextTick || _setImmediate;
  var isIterable = (thing) => thing != null && isFunction(thing[iterator]);
  var isSafeIterable = (thing) => thing != null && hasOwnInPrototypeChain(thing, iterator) && isIterable(thing);
  var utils_default = {
    isArray,
    isArrayBuffer,
    isBuffer,
    isFormData,
    isArrayBufferView,
    isString,
    isNumber,
    isBoolean,
    isObject,
    isPlainObject,
    isEmptyObject,
    isReadableStream,
    isRequest,
    isResponse,
    isHeaders,
    isUndefined,
    isDate,
    isFile,
    isReactNativeBlob,
    isReactNative,
    isBlob,
    isRegExp,
    isFunction,
    isStream,
    isURLSearchParams,
    isTypedArray,
    isFileList,
    forEach,
    merge,
    extend,
    trim,
    stripBOM,
    inherits,
    toFlatObject,
    kindOf,
    kindOfTest,
    endsWith,
    toArray,
    forEachEntry,
    matchAll,
    isHTMLForm,
    hasOwnProperty,
    hasOwnProp: hasOwnProperty,
    // an alias to avoid ESLint no-prototype-builtins detection
    hasOwnInPrototypeChain,
    getSafeProp,
    reduceDescriptors,
    freezeMethods,
    toObjectSet,
    toCamelCase,
    noop,
    toFiniteNumber,
    findKey,
    global: _global,
    isContextDefined,
    isSpecCompliantForm,
    toJSONObject,
    isAsyncFn,
    isThenable,
    setImmediate: _setImmediate,
    asap,
    isIterable,
    isSafeIterable
  };

  // node_modules/axios/lib/core/Axios.js
  init_define_import_meta_env();

  // node_modules/axios/lib/helpers/buildURL.js
  init_define_import_meta_env();

  // node_modules/axios/lib/helpers/AxiosURLSearchParams.js
  init_define_import_meta_env();

  // node_modules/axios/lib/helpers/toFormData.js
  init_define_import_meta_env();

  // node_modules/axios/lib/core/AxiosError.js
  init_define_import_meta_env();

  // node_modules/axios/lib/core/AxiosHeaders.js
  init_define_import_meta_env();

  // node_modules/axios/lib/helpers/parseHeaders.js
  init_define_import_meta_env();
  var ignoreDuplicateOf = utils_default.toObjectSet([
    "age",
    "authorization",
    "content-length",
    "content-type",
    "etag",
    "expires",
    "from",
    "host",
    "if-modified-since",
    "if-unmodified-since",
    "last-modified",
    "location",
    "max-forwards",
    "proxy-authorization",
    "referer",
    "retry-after",
    "user-agent"
  ]);
  var parseHeaders_default = (rawHeaders) => {
    const parsed = {};
    let key;
    let val;
    let i;
    rawHeaders && rawHeaders.split("\n").forEach(function parser(line) {
      i = line.indexOf(":");
      key = line.substring(0, i).trim().toLowerCase();
      val = line.substring(i + 1).trim();
      if (!key || parsed[key] && ignoreDuplicateOf[key]) {
        return;
      }
      if (key === "set-cookie") {
        if (parsed[key]) {
          parsed[key].push(val);
        } else {
          parsed[key] = [val];
        }
      } else {
        parsed[key] = parsed[key] ? parsed[key] + ", " + val : val;
      }
    });
    return parsed;
  };

  // node_modules/axios/lib/helpers/sanitizeHeaderValue.js
  init_define_import_meta_env();
  function trimSPorHTAB(str) {
    let start = 0;
    let end = str.length;
    while (start < end) {
      const code = str.charCodeAt(start);
      if (code !== 9 && code !== 32) {
        break;
      }
      start += 1;
    }
    while (end > start) {
      const code = str.charCodeAt(end - 1);
      if (code !== 9 && code !== 32) {
        break;
      }
      end -= 1;
    }
    return start === 0 && end === str.length ? str : str.slice(start, end);
  }
  var INVALID_UNICODE_HEADER_VALUE_CHARS = new RegExp("[\\u0000-\\u0008\\u000a-\\u001f\\u007f]+", "g");
  var INVALID_BYTE_STRING_HEADER_VALUE_CHARS = new RegExp("[^\\u0009\\u0020-\\u007e\\u0080-\\u00ff]+", "g");
  function sanitizeValue(value, invalidChars) {
    if (utils_default.isArray(value)) {
      return value.map((item) => sanitizeValue(item, invalidChars));
    }
    return trimSPorHTAB(String(value).replace(invalidChars, ""));
  }
  var sanitizeHeaderValue = (value) => sanitizeValue(value, INVALID_UNICODE_HEADER_VALUE_CHARS);
  var sanitizeByteStringHeaderValue = (value) => sanitizeValue(value, INVALID_BYTE_STRING_HEADER_VALUE_CHARS);
  function toByteStringHeaderObject(headers) {
    const byteStringHeaders = /* @__PURE__ */ Object.create(null);
    utils_default.forEach(headers.toJSON(), (value, header) => {
      byteStringHeaders[header] = sanitizeByteStringHeaderValue(value);
    });
    return byteStringHeaders;
  }

  // node_modules/axios/lib/core/AxiosHeaders.js
  var $internals = /* @__PURE__ */ Symbol("internals");
  function normalizeHeader(header) {
    return header && String(header).trim().toLowerCase();
  }
  function normalizeValue(value) {
    if (value === false || value == null) {
      return value;
    }
    return utils_default.isArray(value) ? value.map(normalizeValue) : sanitizeHeaderValue(String(value));
  }
  function parseTokens(str) {
    const tokens = /* @__PURE__ */ Object.create(null);
    const tokensRE = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
    let match;
    while (match = tokensRE.exec(str)) {
      tokens[match[1]] = match[2];
    }
    return tokens;
  }
  var isValidHeaderName = (str) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(str.trim());
  function matchHeaderValue(context, value, header, filter2, isHeaderNameFilter) {
    if (utils_default.isFunction(filter2)) {
      return filter2.call(this, value, header);
    }
    if (isHeaderNameFilter) {
      value = header;
    }
    if (!utils_default.isString(value)) return;
    if (utils_default.isString(filter2)) {
      return value.indexOf(filter2) !== -1;
    }
    if (utils_default.isRegExp(filter2)) {
      return filter2.test(value);
    }
  }
  function formatHeader(header) {
    return header.trim().toLowerCase().replace(/([a-z\d])(\w*)/g, (w, char, str) => {
      return char.toUpperCase() + str;
    });
  }
  function buildAccessors(obj, header) {
    const accessorName = utils_default.toCamelCase(" " + header);
    ["get", "set", "has"].forEach((methodName) => {
      Object.defineProperty(obj, methodName + accessorName, {
        // Null-proto descriptor so a polluted Object.prototype.get cannot turn
        // this data descriptor into an accessor descriptor on the way in.
        __proto__: null,
        value: function(arg1, arg2, arg3) {
          return this[methodName].call(this, header, arg1, arg2, arg3);
        },
        configurable: true
      });
    });
  }
  var AxiosHeaders = class {
    constructor(headers) {
      headers && this.set(headers);
    }
    set(header, valueOrRewrite, rewrite) {
      const self2 = this;
      function setHeader(_value, _header, _rewrite) {
        const lHeader = normalizeHeader(_header);
        if (!lHeader) {
          return;
        }
        const key = utils_default.findKey(self2, lHeader);
        if (!key || self2[key] === void 0 || _rewrite === true || _rewrite === void 0 && self2[key] !== false) {
          self2[key || _header] = normalizeValue(_value);
        }
      }
      const setHeaders = (headers, _rewrite) => utils_default.forEach(headers, (_value, _header) => setHeader(_value, _header, _rewrite));
      if (utils_default.isPlainObject(header) || header instanceof this.constructor) {
        setHeaders(header, valueOrRewrite);
      } else if (utils_default.isString(header) && (header = header.trim()) && !isValidHeaderName(header)) {
        setHeaders(parseHeaders_default(header), valueOrRewrite);
      } else if (utils_default.isObject(header) && utils_default.isSafeIterable(header)) {
        let obj = /* @__PURE__ */ Object.create(null), dest, key;
        for (const entry of header) {
          if (!utils_default.isArray(entry)) {
            throw new TypeError("Object iterator must return a key-value pair");
          }
          key = entry[0];
          if (utils_default.hasOwnProp(obj, key)) {
            dest = obj[key];
            obj[key] = utils_default.isArray(dest) ? [...dest, entry[1]] : [dest, entry[1]];
          } else {
            obj[key] = entry[1];
          }
        }
        setHeaders(obj, valueOrRewrite);
      } else {
        header != null && setHeader(valueOrRewrite, header, rewrite);
      }
      return this;
    }
    get(header, parser) {
      header = normalizeHeader(header);
      if (header) {
        const key = utils_default.findKey(this, header);
        if (key) {
          const value = this[key];
          if (!parser) {
            return value;
          }
          if (parser === true) {
            return parseTokens(value);
          }
          if (utils_default.isFunction(parser)) {
            return parser.call(this, value, key);
          }
          if (utils_default.isRegExp(parser)) {
            return parser.exec(value);
          }
          throw new TypeError("parser must be boolean|regexp|function");
        }
      }
    }
    has(header, matcher) {
      header = normalizeHeader(header);
      if (header) {
        const key = utils_default.findKey(this, header);
        return !!(key && this[key] !== void 0 && (!matcher || matchHeaderValue(this, this[key], key, matcher)));
      }
      return false;
    }
    delete(header, matcher) {
      const self2 = this;
      let deleted = false;
      function deleteHeader(_header) {
        _header = normalizeHeader(_header);
        if (_header) {
          const key = utils_default.findKey(self2, _header);
          if (key && (!matcher || matchHeaderValue(self2, self2[key], key, matcher))) {
            delete self2[key];
            deleted = true;
          }
        }
      }
      if (utils_default.isArray(header)) {
        header.forEach(deleteHeader);
      } else {
        deleteHeader(header);
      }
      return deleted;
    }
    clear(matcher) {
      const keys = Object.keys(this);
      let i = keys.length;
      let deleted = false;
      while (i--) {
        const key = keys[i];
        if (!matcher || matchHeaderValue(this, this[key], key, matcher, true)) {
          delete this[key];
          deleted = true;
        }
      }
      return deleted;
    }
    normalize(format) {
      const self2 = this;
      const headers = {};
      utils_default.forEach(this, (value, header) => {
        const key = utils_default.findKey(headers, header);
        if (key) {
          self2[key] = normalizeValue(value);
          delete self2[header];
          return;
        }
        const normalized = format ? formatHeader(header) : String(header).trim();
        if (normalized !== header) {
          delete self2[header];
        }
        self2[normalized] = normalizeValue(value);
        headers[normalized] = true;
      });
      return this;
    }
    concat(...targets) {
      return this.constructor.concat(this, ...targets);
    }
    toJSON(asStrings) {
      const obj = /* @__PURE__ */ Object.create(null);
      utils_default.forEach(this, (value, header) => {
        value != null && value !== false && (obj[header] = asStrings && utils_default.isArray(value) ? value.join(", ") : value);
      });
      return obj;
    }
    [Symbol.iterator]() {
      return Object.entries(this.toJSON())[Symbol.iterator]();
    }
    toString() {
      return Object.entries(this.toJSON()).map(([header, value]) => header + ": " + value).join("\n");
    }
    getSetCookie() {
      return this.get("set-cookie") || [];
    }
    get [Symbol.toStringTag]() {
      return "AxiosHeaders";
    }
    static from(thing) {
      return thing instanceof this ? thing : new this(thing);
    }
    static concat(first, ...targets) {
      const computed = new this(first);
      targets.forEach((target) => computed.set(target));
      return computed;
    }
    static accessor(header) {
      const internals = this[$internals] = this[$internals] = {
        accessors: {}
      };
      const accessors = internals.accessors;
      const prototype2 = this.prototype;
      function defineAccessor(_header) {
        const lHeader = normalizeHeader(_header);
        if (!accessors[lHeader]) {
          buildAccessors(prototype2, _header);
          accessors[lHeader] = true;
        }
      }
      utils_default.isArray(header) ? header.forEach(defineAccessor) : defineAccessor(header);
      return this;
    }
  };
  AxiosHeaders.accessor([
    "Content-Type",
    "Content-Length",
    "Accept",
    "Accept-Encoding",
    "User-Agent",
    "Authorization"
  ]);
  utils_default.reduceDescriptors(AxiosHeaders.prototype, ({ value }, key) => {
    let mapped = key[0].toUpperCase() + key.slice(1);
    return {
      get: () => value,
      set(headerValue) {
        this[mapped] = headerValue;
      }
    };
  });
  utils_default.freezeMethods(AxiosHeaders);
  var AxiosHeaders_default = AxiosHeaders;

  // node_modules/axios/lib/core/AxiosError.js
  var REDACTED = "[REDACTED ****]";
  function hasOwnOrPrototypeToJSON(source) {
    if (utils_default.hasOwnProp(source, "toJSON")) {
      return true;
    }
    let prototype2 = Object.getPrototypeOf(source);
    while (prototype2 && prototype2 !== Object.prototype) {
      if (utils_default.hasOwnProp(prototype2, "toJSON")) {
        return true;
      }
      prototype2 = Object.getPrototypeOf(prototype2);
    }
    return false;
  }
  function redactConfig(config, redactKeys) {
    const lowerKeys = new Set(redactKeys.map((k) => String(k).toLowerCase()));
    const seen = [];
    const visit = (source) => {
      if (source === null || typeof source !== "object") return source;
      if (utils_default.isBuffer(source)) return source;
      if (seen.indexOf(source) !== -1) return void 0;
      if (source instanceof AxiosHeaders_default) {
        source = source.toJSON();
      }
      seen.push(source);
      let result;
      if (utils_default.isArray(source)) {
        result = [];
        source.forEach((v, i) => {
          const reducedValue = visit(v);
          if (!utils_default.isUndefined(reducedValue)) {
            result[i] = reducedValue;
          }
        });
      } else {
        if (!utils_default.isPlainObject(source) && hasOwnOrPrototypeToJSON(source)) {
          seen.pop();
          return source;
        }
        result = /* @__PURE__ */ Object.create(null);
        for (const [key, value] of Object.entries(source)) {
          const reducedValue = lowerKeys.has(key.toLowerCase()) ? REDACTED : visit(value);
          if (!utils_default.isUndefined(reducedValue)) {
            result[key] = reducedValue;
          }
        }
      }
      seen.pop();
      return result;
    };
    return visit(config);
  }
  var AxiosError = class _AxiosError extends Error {
    static from(error, code, config, request, response, customProps) {
      const axiosError = new _AxiosError(error.message, code || error.code, config, request, response);
      Object.defineProperty(axiosError, "cause", {
        __proto__: null,
        value: error,
        writable: true,
        enumerable: false,
        configurable: true
      });
      axiosError.name = error.name;
      if (error.status != null && axiosError.status == null) {
        axiosError.status = error.status;
      }
      customProps && Object.assign(axiosError, customProps);
      return axiosError;
    }
    /**
     * Create an Error with the specified message, config, error code, request and response.
     *
     * @param {string} message The error message.
     * @param {string} [code] The error code (for example, 'ECONNABORTED').
     * @param {Object} [config] The config.
     * @param {Object} [request] The request.
     * @param {Object} [response] The response.
     *
     * @returns {Error} The created error.
     */
    constructor(message, code, config, request, response) {
      super(message);
      Object.defineProperty(this, "message", {
        // Null-proto descriptor so a polluted Object.prototype.get cannot turn
        // this data descriptor into an accessor descriptor on the way in.
        __proto__: null,
        value: message,
        enumerable: true,
        writable: true,
        configurable: true
      });
      this.name = "AxiosError";
      this.isAxiosError = true;
      code && (this.code = code);
      config && (this.config = config);
      request && (this.request = request);
      if (response) {
        this.response = response;
        this.status = response.status;
      }
    }
    toJSON() {
      const config = this.config;
      const redactKeys = config && utils_default.hasOwnProp(config, "redact") ? config.redact : void 0;
      const serializedConfig = utils_default.isArray(redactKeys) && redactKeys.length > 0 ? redactConfig(config, redactKeys) : utils_default.toJSONObject(config);
      return {
        // Standard
        message: this.message,
        name: this.name,
        // Microsoft
        description: this.description,
        number: this.number,
        // Mozilla
        fileName: this.fileName,
        lineNumber: this.lineNumber,
        columnNumber: this.columnNumber,
        stack: this.stack,
        // Axios
        config: serializedConfig,
        code: this.code,
        status: this.status
      };
    }
  };
  AxiosError.ERR_BAD_OPTION_VALUE = "ERR_BAD_OPTION_VALUE";
  AxiosError.ERR_BAD_OPTION = "ERR_BAD_OPTION";
  AxiosError.ECONNABORTED = "ECONNABORTED";
  AxiosError.ETIMEDOUT = "ETIMEDOUT";
  AxiosError.ECONNREFUSED = "ECONNREFUSED";
  AxiosError.ERR_NETWORK = "ERR_NETWORK";
  AxiosError.ERR_FR_TOO_MANY_REDIRECTS = "ERR_FR_TOO_MANY_REDIRECTS";
  AxiosError.ERR_DEPRECATED = "ERR_DEPRECATED";
  AxiosError.ERR_BAD_RESPONSE = "ERR_BAD_RESPONSE";
  AxiosError.ERR_BAD_REQUEST = "ERR_BAD_REQUEST";
  AxiosError.ERR_CANCELED = "ERR_CANCELED";
  AxiosError.ERR_NOT_SUPPORT = "ERR_NOT_SUPPORT";
  AxiosError.ERR_INVALID_URL = "ERR_INVALID_URL";
  AxiosError.ERR_FORM_DATA_DEPTH_EXCEEDED = "ERR_FORM_DATA_DEPTH_EXCEEDED";
  var AxiosError_default = AxiosError;

  // node_modules/axios/lib/helpers/null.js
  init_define_import_meta_env();
  var null_default = null;

  // node_modules/axios/lib/helpers/toFormData.js
  var DEFAULT_FORM_DATA_MAX_DEPTH = 100;
  function isVisitable(thing) {
    return utils_default.isPlainObject(thing) || utils_default.isArray(thing);
  }
  function removeBrackets(key) {
    return utils_default.endsWith(key, "[]") ? key.slice(0, -2) : key;
  }
  function renderKey(path, key, dots) {
    if (!path) return key;
    return path.concat(key).map(function each(token, i) {
      token = removeBrackets(token);
      return !dots && i ? "[" + token + "]" : token;
    }).join(dots ? "." : "");
  }
  function isFlatArray(arr) {
    return utils_default.isArray(arr) && !arr.some(isVisitable);
  }
  var predicates = utils_default.toFlatObject(utils_default, {}, null, function filter(prop) {
    return /^is[A-Z]/.test(prop);
  });
  function toFormData(obj, formData, options) {
    if (!utils_default.isObject(obj)) {
      throw new TypeError("target must be an object");
    }
    formData = formData || new (null_default || FormData)();
    options = utils_default.toFlatObject(
      options,
      {
        metaTokens: true,
        dots: false,
        indexes: false
      },
      false,
      function defined(option, source) {
        return !utils_default.isUndefined(source[option]);
      }
    );
    const metaTokens = options.metaTokens;
    const visitor = options.visitor || defaultVisitor;
    const dots = options.dots;
    const indexes = options.indexes;
    const _Blob = options.Blob || typeof Blob !== "undefined" && Blob;
    const maxDepth = options.maxDepth === void 0 ? DEFAULT_FORM_DATA_MAX_DEPTH : options.maxDepth;
    const useBlob = _Blob && utils_default.isSpecCompliantForm(formData);
    const stack = [];
    if (!utils_default.isFunction(visitor)) {
      throw new TypeError("visitor must be a function");
    }
    function convertValue(value) {
      if (value === null) return "";
      if (utils_default.isDate(value)) {
        return value.toISOString();
      }
      if (utils_default.isBoolean(value)) {
        return value.toString();
      }
      if (!useBlob && utils_default.isBlob(value)) {
        throw new AxiosError_default("Blob is not supported. Use a Buffer instead.");
      }
      if (utils_default.isArrayBuffer(value) || utils_default.isTypedArray(value)) {
        if (useBlob && typeof _Blob === "function") {
          return new _Blob([value]);
        }
        if (typeof Buffer !== "undefined") {
          return Buffer.from(value);
        }
        throw new AxiosError_default("Blob is not supported. Use a Buffer instead.", AxiosError_default.ERR_NOT_SUPPORT);
      }
      return value;
    }
    function throwIfMaxDepthExceeded(depth) {
      if (depth > maxDepth) {
        throw new AxiosError_default(
          "Object is too deeply nested (" + depth + " levels). Max depth: " + maxDepth,
          AxiosError_default.ERR_FORM_DATA_DEPTH_EXCEEDED
        );
      }
    }
    function stringifyWithDepthLimit(value, depth) {
      if (maxDepth === Infinity) {
        return JSON.stringify(value);
      }
      const ancestors = [];
      return JSON.stringify(value, function limitDepth(_key, currentValue) {
        if (!utils_default.isObject(currentValue)) {
          return currentValue;
        }
        while (ancestors.length && ancestors[ancestors.length - 1] !== this) {
          ancestors.pop();
        }
        ancestors.push(currentValue);
        throwIfMaxDepthExceeded(depth + ancestors.length - 1);
        return currentValue;
      });
    }
    function defaultVisitor(value, key, path) {
      let arr = value;
      if (utils_default.isReactNative(formData) && utils_default.isReactNativeBlob(value)) {
        formData.append(renderKey(path, key, dots), convertValue(value));
        return false;
      }
      if (value && !path && typeof value === "object") {
        if (utils_default.endsWith(key, "{}")) {
          key = metaTokens ? key : key.slice(0, -2);
          value = stringifyWithDepthLimit(value, 1);
        } else if (utils_default.isArray(value) && isFlatArray(value) || (utils_default.isFileList(value) || utils_default.endsWith(key, "[]")) && (arr = utils_default.toArray(value))) {
          key = removeBrackets(key);
          arr.forEach(function each(el, index) {
            !(utils_default.isUndefined(el) || el === null) && formData.append(
              // eslint-disable-next-line no-nested-ternary
              indexes === true ? renderKey([key], index, dots) : indexes === null ? key : key + "[]",
              convertValue(el)
            );
          });
          return false;
        }
      }
      if (isVisitable(value)) {
        return true;
      }
      formData.append(renderKey(path, key, dots), convertValue(value));
      return false;
    }
    const exposedHelpers = Object.assign(predicates, {
      defaultVisitor,
      convertValue,
      isVisitable
    });
    function build(value, path, depth = 0) {
      if (utils_default.isUndefined(value)) return;
      throwIfMaxDepthExceeded(depth);
      if (stack.indexOf(value) !== -1) {
        throw new Error("Circular reference detected in " + path.join("."));
      }
      stack.push(value);
      utils_default.forEach(value, function each(el, key) {
        const result = !(utils_default.isUndefined(el) || el === null) && visitor.call(formData, el, utils_default.isString(key) ? key.trim() : key, path, exposedHelpers);
        if (result === true) {
          build(el, path ? path.concat(key) : [key], depth + 1);
        }
      });
      stack.pop();
    }
    if (!utils_default.isObject(obj)) {
      throw new TypeError("data must be an object");
    }
    build(obj);
    return formData;
  }
  var toFormData_default = toFormData;

  // node_modules/axios/lib/helpers/AxiosURLSearchParams.js
  function encode(str) {
    const charMap = {
      "!": "%21",
      "'": "%27",
      "(": "%28",
      ")": "%29",
      "~": "%7E",
      "%20": "+"
    };
    return encodeURIComponent(str).replace(/[!'()~]|%20/g, function replacer(match) {
      return charMap[match];
    });
  }
  function AxiosURLSearchParams(params, options) {
    this._pairs = [];
    params && toFormData_default(params, this, options);
  }
  var prototype = AxiosURLSearchParams.prototype;
  prototype.append = function append(name, value) {
    this._pairs.push([name, value]);
  };
  prototype.toString = function toString2(encoder) {
    const _encode = encoder ? (value) => encoder.call(this, value, encode) : encode;
    return this._pairs.map(function each(pair) {
      return _encode(pair[0]) + "=" + _encode(pair[1]);
    }, "").join("&");
  };
  var AxiosURLSearchParams_default = AxiosURLSearchParams;

  // node_modules/axios/lib/helpers/buildURL.js
  function encode2(val) {
    return encodeURIComponent(val).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+");
  }
  function buildURL(url, params, options) {
    if (!params) {
      return url;
    }
    url = url || "";
    const _options = utils_default.isFunction(options) ? {
      serialize: options
    } : options;
    const _encode = utils_default.getSafeProp(_options, "encode") || encode2;
    const serializeFn = utils_default.getSafeProp(_options, "serialize");
    let serializedParams;
    if (serializeFn) {
      serializedParams = serializeFn(params, _options);
    } else {
      serializedParams = utils_default.isURLSearchParams(params) ? params.toString() : new AxiosURLSearchParams_default(params, _options).toString(_encode);
    }
    if (serializedParams) {
      const hashmarkIndex = url.indexOf("#");
      if (hashmarkIndex !== -1) {
        url = url.slice(0, hashmarkIndex);
      }
      url += (url.indexOf("?") === -1 ? "?" : "&") + serializedParams;
    }
    return url;
  }

  // node_modules/axios/lib/core/InterceptorManager.js
  init_define_import_meta_env();
  var InterceptorManager = class {
    constructor() {
      this.handlers = [];
    }
    /**
     * Add a new interceptor to the stack
     *
     * @param {Function} fulfilled The function to handle `then` for a `Promise`
     * @param {Function} rejected The function to handle `reject` for a `Promise`
     * @param {Object} options The options for the interceptor, synchronous and runWhen
     *
     * @return {Number} An ID used to remove interceptor later
     */
    use(fulfilled, rejected, options) {
      this.handlers.push({
        fulfilled,
        rejected,
        synchronous: options ? options.synchronous : false,
        runWhen: options ? options.runWhen : null
      });
      return this.handlers.length - 1;
    }
    /**
     * Remove an interceptor from the stack
     *
     * @param {Number} id The ID that was returned by `use`
     *
     * @returns {void}
     */
    eject(id) {
      if (this.handlers[id]) {
        this.handlers[id] = null;
      }
    }
    /**
     * Clear all interceptors from the stack
     *
     * @returns {void}
     */
    clear() {
      if (this.handlers) {
        this.handlers = [];
      }
    }
    /**
     * Iterate over all the registered interceptors
     *
     * This method is particularly useful for skipping over any
     * interceptors that may have become `null` calling `eject`.
     *
     * @param {Function} fn The function to call for each interceptor
     *
     * @returns {void}
     */
    forEach(fn) {
      utils_default.forEach(this.handlers, function forEachHandler(h) {
        if (h !== null) {
          fn(h);
        }
      });
    }
  };
  var InterceptorManager_default = InterceptorManager;

  // node_modules/axios/lib/core/dispatchRequest.js
  init_define_import_meta_env();

  // node_modules/axios/lib/core/transformData.js
  init_define_import_meta_env();

  // node_modules/axios/lib/defaults/index.js
  init_define_import_meta_env();

  // node_modules/axios/lib/defaults/transitional.js
  init_define_import_meta_env();
  var transitional_default = {
    silentJSONParsing: true,
    forcedJSONParsing: true,
    clarifyTimeoutError: false,
    legacyInterceptorReqResOrdering: true,
    advertiseZstdAcceptEncoding: false,
    validateStatusUndefinedResolves: true
  };

  // node_modules/axios/lib/helpers/toURLEncodedForm.js
  init_define_import_meta_env();

  // node_modules/axios/lib/platform/index.js
  init_define_import_meta_env();

  // node_modules/axios/lib/platform/browser/index.js
  init_define_import_meta_env();

  // node_modules/axios/lib/platform/browser/classes/URLSearchParams.js
  init_define_import_meta_env();
  var URLSearchParams_default = typeof URLSearchParams !== "undefined" ? URLSearchParams : AxiosURLSearchParams_default;

  // node_modules/axios/lib/platform/browser/classes/FormData.js
  init_define_import_meta_env();
  var FormData_default = typeof FormData !== "undefined" ? FormData : null;

  // node_modules/axios/lib/platform/browser/classes/Blob.js
  init_define_import_meta_env();
  var Blob_default = typeof Blob !== "undefined" ? Blob : null;

  // node_modules/axios/lib/platform/browser/index.js
  var browser_default = {
    isBrowser: true,
    classes: {
      URLSearchParams: URLSearchParams_default,
      FormData: FormData_default,
      Blob: Blob_default
    },
    protocols: ["http", "https", "file", "blob", "url", "data"]
  };

  // node_modules/axios/lib/platform/common/utils.js
  var utils_exports = {};
  __export(utils_exports, {
    hasBrowserEnv: () => hasBrowserEnv,
    hasStandardBrowserEnv: () => hasStandardBrowserEnv,
    hasStandardBrowserWebWorkerEnv: () => hasStandardBrowserWebWorkerEnv,
    navigator: () => _navigator,
    origin: () => origin
  });
  init_define_import_meta_env();
  var hasBrowserEnv = typeof window !== "undefined" && typeof document !== "undefined";
  var _navigator = typeof navigator === "object" && navigator || void 0;
  var hasStandardBrowserEnv = hasBrowserEnv && (!_navigator || ["ReactNative", "NativeScript", "NS"].indexOf(_navigator.product) < 0);
  var hasStandardBrowserWebWorkerEnv = (() => {
    return typeof WorkerGlobalScope !== "undefined" && // eslint-disable-next-line no-undef
    self instanceof WorkerGlobalScope && typeof self.importScripts === "function";
  })();
  var origin = hasBrowserEnv && window.location.href || "http://localhost";

  // node_modules/axios/lib/platform/index.js
  var platform_default = {
    ...utils_exports,
    ...browser_default
  };

  // node_modules/axios/lib/helpers/toURLEncodedForm.js
  function toURLEncodedForm(data2, options) {
    return toFormData_default(data2, new platform_default.classes.URLSearchParams(), {
      visitor: function(value, key, path, helpers) {
        if (platform_default.isNode && utils_default.isBuffer(value)) {
          this.append(key, value.toString("base64"));
          return false;
        }
        return helpers.defaultVisitor.apply(this, arguments);
      },
      ...options
    });
  }

  // node_modules/axios/lib/helpers/formDataToJSON.js
  init_define_import_meta_env();
  var MAX_DEPTH = DEFAULT_FORM_DATA_MAX_DEPTH;
  function throwIfDepthExceeded(index) {
    if (index > MAX_DEPTH) {
      throw new AxiosError_default(
        "FormData field is too deeply nested (" + index + " levels). Max depth: " + MAX_DEPTH,
        AxiosError_default.ERR_FORM_DATA_DEPTH_EXCEEDED
      );
    }
  }
  function parsePropPath(name) {
    const path = [];
    const pattern = /\w+|\[(\w*)]/g;
    let match;
    while ((match = pattern.exec(name)) !== null) {
      throwIfDepthExceeded(path.length);
      path.push(match[0] === "[]" ? "" : match[1] || match[0]);
    }
    return path;
  }
  function arrayToObject(arr) {
    const obj = {};
    const keys = Object.keys(arr);
    let i;
    const len = keys.length;
    let key;
    for (i = 0; i < len; i++) {
      key = keys[i];
      obj[key] = arr[key];
    }
    return obj;
  }
  function formDataToJSON(formData) {
    function buildPath(path, value, target, index) {
      throwIfDepthExceeded(index);
      let name = path[index++];
      if (name === "__proto__") return true;
      const isNumericKey = Number.isFinite(+name);
      const isLast = index >= path.length;
      name = !name && utils_default.isArray(target) ? target.length : name;
      if (isLast) {
        if (utils_default.hasOwnProp(target, name)) {
          target[name] = utils_default.isArray(target[name]) ? target[name].concat(value) : [target[name], value];
        } else {
          target[name] = value;
        }
        return !isNumericKey;
      }
      if (!utils_default.hasOwnProp(target, name) || !utils_default.isObject(target[name])) {
        target[name] = [];
      }
      const result = buildPath(path, value, target[name], index);
      if (result && utils_default.isArray(target[name])) {
        target[name] = arrayToObject(target[name]);
      }
      return !isNumericKey;
    }
    if (utils_default.isFormData(formData) && utils_default.isFunction(formData.entries)) {
      const obj = {};
      utils_default.forEachEntry(formData, (name, value) => {
        buildPath(parsePropPath(name), value, obj, 0);
      });
      return obj;
    }
    return null;
  }
  var formDataToJSON_default = formDataToJSON;

  // node_modules/axios/lib/defaults/index.js
  var own = (obj, key) => obj != null && utils_default.hasOwnProp(obj, key) ? obj[key] : void 0;
  function stringifySafely(rawValue, parser, encoder) {
    if (utils_default.isString(rawValue)) {
      try {
        (parser || JSON.parse)(rawValue);
        return utils_default.trim(rawValue);
      } catch (e) {
        if (e.name !== "SyntaxError") {
          throw e;
        }
      }
    }
    return (encoder || JSON.stringify)(rawValue);
  }
  var defaults = {
    transitional: transitional_default,
    adapter: ["xhr", "http", "fetch"],
    transformRequest: [
      function transformRequest(data2, headers) {
        const contentType = headers.getContentType() || "";
        const hasJSONContentType = contentType.indexOf("application/json") > -1;
        const isObjectPayload = utils_default.isObject(data2);
        if (isObjectPayload && utils_default.isHTMLForm(data2)) {
          data2 = new FormData(data2);
        }
        const isFormData2 = utils_default.isFormData(data2);
        if (isFormData2) {
          return hasJSONContentType ? JSON.stringify(formDataToJSON_default(data2)) : data2;
        }
        if (utils_default.isArrayBuffer(data2) || utils_default.isBuffer(data2) || utils_default.isStream(data2) || utils_default.isFile(data2) || utils_default.isBlob(data2) || utils_default.isReadableStream(data2)) {
          return data2;
        }
        if (utils_default.isArrayBufferView(data2)) {
          return data2.buffer;
        }
        if (utils_default.isURLSearchParams(data2)) {
          headers.setContentType("application/x-www-form-urlencoded;charset=utf-8", false);
          return data2.toString();
        }
        let isFileList2;
        if (isObjectPayload) {
          const formSerializer = own(this, "formSerializer");
          if (contentType.indexOf("application/x-www-form-urlencoded") > -1) {
            return toURLEncodedForm(data2, formSerializer).toString();
          }
          if ((isFileList2 = utils_default.isFileList(data2)) || contentType.indexOf("multipart/form-data") > -1) {
            const env = own(this, "env");
            const _FormData = env && env.FormData;
            return toFormData_default(
              isFileList2 ? { "files[]": data2 } : data2,
              _FormData && new _FormData(),
              formSerializer
            );
          }
        }
        if (isObjectPayload || hasJSONContentType) {
          headers.setContentType("application/json", false);
          return stringifySafely(data2);
        }
        return data2;
      }
    ],
    transformResponse: [
      function transformResponse(data2) {
        const transitional2 = own(this, "transitional") || defaults.transitional;
        const forcedJSONParsing = transitional2 && transitional2.forcedJSONParsing;
        const responseType = own(this, "responseType");
        const JSONRequested = responseType === "json";
        if (utils_default.isResponse(data2) || utils_default.isReadableStream(data2)) {
          return data2;
        }
        if (data2 && utils_default.isString(data2) && (forcedJSONParsing && !responseType || JSONRequested)) {
          const silentJSONParsing = transitional2 && transitional2.silentJSONParsing;
          const strictJSONParsing = !silentJSONParsing && JSONRequested;
          try {
            return JSON.parse(data2, own(this, "parseReviver"));
          } catch (e) {
            if (strictJSONParsing) {
              if (e.name === "SyntaxError") {
                throw AxiosError_default.from(e, AxiosError_default.ERR_BAD_RESPONSE, this, null, own(this, "response"));
              }
              throw e;
            }
          }
        }
        return data2;
      }
    ],
    /**
     * A timeout in milliseconds to abort a request. If set to 0 (default) a
     * timeout is not created.
     */
    timeout: 0,
    xsrfCookieName: "XSRF-TOKEN",
    xsrfHeaderName: "X-XSRF-TOKEN",
    maxContentLength: -1,
    maxBodyLength: -1,
    env: {
      FormData: platform_default.classes.FormData,
      Blob: platform_default.classes.Blob
    },
    validateStatus: function validateStatus(status) {
      return status >= 200 && status < 300;
    },
    headers: {
      common: {
        Accept: "application/json, text/plain, */*",
        "Content-Type": void 0
      }
    }
  };
  utils_default.forEach(["delete", "get", "head", "post", "put", "patch", "query"], (method) => {
    defaults.headers[method] = {};
  });
  var defaults_default = defaults;

  // node_modules/axios/lib/core/transformData.js
  function transformData(fns, response) {
    const config = this || defaults_default;
    const context = response || config;
    const headers = AxiosHeaders_default.from(context.headers);
    let data2 = context.data;
    utils_default.forEach(fns, function transform(fn) {
      data2 = fn.call(config, data2, headers.normalize(), response ? response.status : void 0);
    });
    headers.normalize();
    return data2;
  }

  // node_modules/axios/lib/cancel/isCancel.js
  init_define_import_meta_env();
  function isCancel(value) {
    return !!(value && value.__CANCEL__);
  }

  // node_modules/axios/lib/cancel/CanceledError.js
  init_define_import_meta_env();
  var CanceledError = class extends AxiosError_default {
    /**
     * A `CanceledError` is an object that is thrown when an operation is canceled.
     *
     * @param {string=} message The message.
     * @param {Object=} config The config.
     * @param {Object=} request The request.
     *
     * @returns {CanceledError} The created error.
     */
    constructor(message, config, request) {
      super(message == null ? "canceled" : message, AxiosError_default.ERR_CANCELED, config, request);
      this.name = "CanceledError";
      this.__CANCEL__ = true;
    }
  };
  var CanceledError_default = CanceledError;

  // node_modules/axios/lib/adapters/adapters.js
  init_define_import_meta_env();

  // node_modules/axios/lib/adapters/xhr.js
  init_define_import_meta_env();

  // node_modules/axios/lib/core/settle.js
  init_define_import_meta_env();
  function settle(resolve, reject, response) {
    const validateStatus2 = response.config.validateStatus;
    if (!response.status || !validateStatus2 || validateStatus2(response.status)) {
      resolve(response);
    } else {
      reject(new AxiosError_default(
        "Request failed with status code " + response.status,
        response.status >= 400 && response.status < 500 ? AxiosError_default.ERR_BAD_REQUEST : AxiosError_default.ERR_BAD_RESPONSE,
        response.config,
        response.request,
        response
      ));
    }
  }

  // node_modules/axios/lib/helpers/parseProtocol.js
  init_define_import_meta_env();
  function parseProtocol(url) {
    const match = /^([-+\w]{1,25}):(?:\/\/)?/.exec(url);
    return match && match[1] || "";
  }

  // node_modules/axios/lib/helpers/progressEventReducer.js
  init_define_import_meta_env();

  // node_modules/axios/lib/helpers/speedometer.js
  init_define_import_meta_env();
  function speedometer(samplesCount, min) {
    samplesCount = samplesCount || 10;
    const bytes = new Array(samplesCount);
    const timestamps = new Array(samplesCount);
    let head = 0;
    let tail = 0;
    let firstSampleTS;
    min = min !== void 0 ? min : 1e3;
    return function push(chunkLength) {
      const now = Date.now();
      const startedAt = timestamps[tail];
      if (!firstSampleTS) {
        firstSampleTS = now;
      }
      bytes[head] = chunkLength;
      timestamps[head] = now;
      let i = tail;
      let bytesCount = 0;
      while (i !== head) {
        bytesCount += bytes[i++];
        i = i % samplesCount;
      }
      head = (head + 1) % samplesCount;
      if (head === tail) {
        tail = (tail + 1) % samplesCount;
      }
      if (now - firstSampleTS < min) {
        return;
      }
      const passed = startedAt && now - startedAt;
      return passed ? Math.round(bytesCount * 1e3 / passed) : void 0;
    };
  }
  var speedometer_default = speedometer;

  // node_modules/axios/lib/helpers/throttle.js
  init_define_import_meta_env();
  function throttle(fn, freq) {
    let timestamp = 0;
    let threshold = 1e3 / freq;
    let lastArgs;
    let timer;
    const invoke = (args, now = Date.now()) => {
      timestamp = now;
      lastArgs = null;
      if (timer) {
        clearTimeout(timer);
        timer = null;
      }
      fn(...args);
    };
    const throttled = (...args) => {
      const now = Date.now();
      const passed = now - timestamp;
      if (passed >= threshold) {
        invoke(args, now);
      } else {
        lastArgs = args;
        if (!timer) {
          timer = setTimeout(() => {
            timer = null;
            invoke(lastArgs);
          }, threshold - passed);
        }
      }
    };
    const flush = () => lastArgs && invoke(lastArgs);
    return [throttled, flush];
  }
  var throttle_default = throttle;

  // node_modules/axios/lib/helpers/progressEventReducer.js
  var progressEventReducer = (listener, isDownloadStream, freq = 3) => {
    let bytesNotified = 0;
    const _speedometer = speedometer_default(50, 250);
    return throttle_default((e) => {
      if (!e || typeof e.loaded !== "number") {
        return;
      }
      const rawLoaded = e.loaded;
      const total = e.lengthComputable ? e.total : void 0;
      const loaded = total != null ? Math.min(rawLoaded, total) : rawLoaded;
      const progressBytes = Math.max(0, loaded - bytesNotified);
      const rate = _speedometer(progressBytes);
      bytesNotified = Math.max(bytesNotified, loaded);
      const data2 = {
        loaded,
        total,
        progress: total ? loaded / total : void 0,
        bytes: progressBytes,
        rate: rate ? rate : void 0,
        estimated: rate && total ? (total - loaded) / rate : void 0,
        event: e,
        lengthComputable: total != null,
        [isDownloadStream ? "download" : "upload"]: true
      };
      listener(data2);
    }, freq);
  };
  var progressEventDecorator = (total, throttled) => {
    const lengthComputable = total != null;
    return [
      (loaded) => throttled[0]({
        lengthComputable,
        total,
        loaded
      }),
      throttled[1]
    ];
  };
  var asyncDecorator = (fn) => (...args) => utils_default.asap(() => fn(...args));

  // node_modules/axios/lib/helpers/resolveConfig.js
  init_define_import_meta_env();

  // node_modules/axios/lib/helpers/isURLSameOrigin.js
  init_define_import_meta_env();
  var isURLSameOrigin_default = platform_default.hasStandardBrowserEnv ? /* @__PURE__ */ ((origin2, isMSIE) => (url) => {
    url = new URL(url, platform_default.origin);
    return origin2.protocol === url.protocol && origin2.host === url.host && (isMSIE || origin2.port === url.port);
  })(
    new URL(platform_default.origin),
    platform_default.navigator && /(msie|trident)/i.test(platform_default.navigator.userAgent)
  ) : () => true;

  // node_modules/axios/lib/helpers/cookies.js
  init_define_import_meta_env();
  var cookies_default = platform_default.hasStandardBrowserEnv ? (
    // Standard browser envs support document.cookie
    {
      write(name, value, expires, path, domain, secure, sameSite) {
        if (typeof document === "undefined") return;
        const cookie = [`${name}=${encodeURIComponent(value)}`];
        if (utils_default.isNumber(expires)) {
          cookie.push(`expires=${new Date(expires).toUTCString()}`);
        }
        if (utils_default.isString(path)) {
          cookie.push(`path=${path}`);
        }
        if (utils_default.isString(domain)) {
          cookie.push(`domain=${domain}`);
        }
        if (secure === true) {
          cookie.push("secure");
        }
        if (utils_default.isString(sameSite)) {
          cookie.push(`SameSite=${sameSite}`);
        }
        document.cookie = cookie.join("; ");
      },
      read(name) {
        if (typeof document === "undefined") return null;
        const cookies = document.cookie.split(";");
        for (let i = 0; i < cookies.length; i++) {
          const cookie = cookies[i].replace(/^\s+/, "");
          const eq = cookie.indexOf("=");
          if (eq !== -1 && cookie.slice(0, eq) === name) {
            try {
              return decodeURIComponent(cookie.slice(eq + 1));
            } catch (e) {
              return cookie.slice(eq + 1);
            }
          }
        }
        return null;
      },
      remove(name) {
        this.write(name, "", Date.now() - 864e5, "/");
      }
    }
  ) : (
    // Non-standard browser env (web workers, react-native) lack needed support.
    {
      write() {
      },
      read() {
        return null;
      },
      remove() {
      }
    }
  );

  // node_modules/axios/lib/core/buildFullPath.js
  init_define_import_meta_env();

  // node_modules/axios/lib/helpers/isAbsoluteURL.js
  init_define_import_meta_env();
  function isAbsoluteURL(url) {
    if (typeof url !== "string") {
      return false;
    }
    return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(url);
  }

  // node_modules/axios/lib/helpers/combineURLs.js
  init_define_import_meta_env();
  function combineURLs(baseURL, relativeURL) {
    return relativeURL ? baseURL.replace(/\/?\/$/, "") + "/" + relativeURL.replace(/^\/+/, "") : baseURL;
  }

  // node_modules/axios/lib/core/buildFullPath.js
  var malformedHttpProtocol = /^https?:(?!\/\/)/i;
  var httpProtocolControlCharacters = /[\t\n\r]/g;
  function stripLeadingC0ControlOrSpace(url) {
    let i = 0;
    while (i < url.length && url.charCodeAt(i) <= 32) {
      i++;
    }
    return url.slice(i);
  }
  function normalizeURLForProtocolCheck(url) {
    return stripLeadingC0ControlOrSpace(url).replace(httpProtocolControlCharacters, "");
  }
  function assertValidHttpProtocolURL(url, config) {
    if (typeof url === "string" && malformedHttpProtocol.test(normalizeURLForProtocolCheck(url))) {
      throw new AxiosError_default(
        'Invalid URL: missing "//" after protocol',
        AxiosError_default.ERR_INVALID_URL,
        config
      );
    }
  }
  function buildFullPath(baseURL, requestedURL, allowAbsoluteUrls, config) {
    assertValidHttpProtocolURL(requestedURL, config);
    let isRelativeUrl = !isAbsoluteURL(requestedURL);
    if (baseURL && (isRelativeUrl || allowAbsoluteUrls === false)) {
      assertValidHttpProtocolURL(baseURL, config);
      return combineURLs(baseURL, requestedURL);
    }
    return requestedURL;
  }

  // node_modules/axios/lib/core/mergeConfig.js
  init_define_import_meta_env();
  var headersToObject = (thing) => thing instanceof AxiosHeaders_default ? { ...thing } : thing;
  function mergeConfig(config1, config2) {
    config1 = config1 || {};
    config2 = config2 || {};
    const config = /* @__PURE__ */ Object.create(null);
    Object.defineProperty(config, "hasOwnProperty", {
      // Null-proto descriptor so a polluted Object.prototype.get cannot turn
      // this data descriptor into an accessor descriptor on the way in.
      __proto__: null,
      value: Object.prototype.hasOwnProperty,
      enumerable: false,
      writable: true,
      configurable: true
    });
    function getMergedValue(target, source, prop, caseless) {
      if (utils_default.isPlainObject(target) && utils_default.isPlainObject(source)) {
        return utils_default.merge.call({ caseless }, target, source);
      } else if (utils_default.isPlainObject(source)) {
        return utils_default.merge({}, source);
      } else if (utils_default.isArray(source)) {
        return source.slice();
      }
      return source;
    }
    function mergeDeepProperties(a, b, prop, caseless) {
      if (!utils_default.isUndefined(b)) {
        return getMergedValue(a, b, prop, caseless);
      } else if (!utils_default.isUndefined(a)) {
        return getMergedValue(void 0, a, prop, caseless);
      }
    }
    function valueFromConfig2(a, b) {
      if (!utils_default.isUndefined(b)) {
        return getMergedValue(void 0, b);
      }
    }
    function defaultToConfig2(a, b) {
      if (!utils_default.isUndefined(b)) {
        return getMergedValue(void 0, b);
      } else if (!utils_default.isUndefined(a)) {
        return getMergedValue(void 0, a);
      }
    }
    function getMergedTransitionalOption(prop) {
      const transitional2 = utils_default.hasOwnProp(config2, "transitional") ? config2.transitional : void 0;
      if (!utils_default.isUndefined(transitional2)) {
        if (utils_default.isPlainObject(transitional2)) {
          if (utils_default.hasOwnProp(transitional2, prop)) {
            return transitional2[prop];
          }
        } else {
          return void 0;
        }
      }
      const transitional1 = utils_default.hasOwnProp(config1, "transitional") ? config1.transitional : void 0;
      if (utils_default.isPlainObject(transitional1) && utils_default.hasOwnProp(transitional1, prop)) {
        return transitional1[prop];
      }
      return void 0;
    }
    function mergeDirectKeys(a, b, prop) {
      if (utils_default.hasOwnProp(config2, prop)) {
        return getMergedValue(a, b);
      } else if (utils_default.hasOwnProp(config1, prop)) {
        return getMergedValue(void 0, a);
      }
    }
    const mergeMap = {
      url: valueFromConfig2,
      method: valueFromConfig2,
      data: valueFromConfig2,
      baseURL: defaultToConfig2,
      transformRequest: defaultToConfig2,
      transformResponse: defaultToConfig2,
      paramsSerializer: defaultToConfig2,
      timeout: defaultToConfig2,
      timeoutMessage: defaultToConfig2,
      withCredentials: defaultToConfig2,
      withXSRFToken: defaultToConfig2,
      adapter: defaultToConfig2,
      responseType: defaultToConfig2,
      xsrfCookieName: defaultToConfig2,
      xsrfHeaderName: defaultToConfig2,
      onUploadProgress: defaultToConfig2,
      onDownloadProgress: defaultToConfig2,
      decompress: defaultToConfig2,
      maxContentLength: defaultToConfig2,
      maxBodyLength: defaultToConfig2,
      beforeRedirect: defaultToConfig2,
      transport: defaultToConfig2,
      httpAgent: defaultToConfig2,
      httpsAgent: defaultToConfig2,
      cancelToken: defaultToConfig2,
      socketPath: defaultToConfig2,
      allowedSocketPaths: defaultToConfig2,
      responseEncoding: defaultToConfig2,
      validateStatus: mergeDirectKeys,
      headers: (a, b, prop) => mergeDeepProperties(headersToObject(a), headersToObject(b), prop, true)
    };
    utils_default.forEach(Object.keys({ ...config1, ...config2 }), function computeConfigValue(prop) {
      if (prop === "__proto__" || prop === "constructor" || prop === "prototype") return;
      const merge2 = utils_default.hasOwnProp(mergeMap, prop) ? mergeMap[prop] : mergeDeepProperties;
      const a = utils_default.hasOwnProp(config1, prop) ? config1[prop] : void 0;
      const b = utils_default.hasOwnProp(config2, prop) ? config2[prop] : void 0;
      const configValue = merge2(a, b, prop);
      utils_default.isUndefined(configValue) && merge2 !== mergeDirectKeys || (config[prop] = configValue);
    });
    if (utils_default.hasOwnProp(config2, "validateStatus") && utils_default.isUndefined(config2.validateStatus) && getMergedTransitionalOption("validateStatusUndefinedResolves") === false) {
      if (utils_default.hasOwnProp(config1, "validateStatus")) {
        config.validateStatus = getMergedValue(void 0, config1.validateStatus);
      } else {
        delete config.validateStatus;
      }
    }
    return config;
  }

  // node_modules/axios/lib/helpers/resolveConfig.js
  var FORM_DATA_CONTENT_HEADERS = ["content-type", "content-length"];
  function setFormDataHeaders(headers, formHeaders, policy) {
    if (policy !== "content-only") {
      headers.set(formHeaders);
      return;
    }
    Object.entries(formHeaders || {}).forEach(([key, val]) => {
      if (FORM_DATA_CONTENT_HEADERS.includes(key.toLowerCase())) {
        headers.set(key, val);
      }
    });
  }
  var encodeUTF8 = (str) => encodeURIComponent(str).replace(
    /%([0-9A-F]{2})/gi,
    (_, hex) => String.fromCharCode(parseInt(hex, 16))
  );
  function resolveConfig(config) {
    const newConfig = mergeConfig({}, config);
    const own2 = (key) => utils_default.hasOwnProp(newConfig, key) ? newConfig[key] : void 0;
    const data2 = own2("data");
    let withXSRFToken = own2("withXSRFToken");
    const xsrfHeaderName = own2("xsrfHeaderName");
    const xsrfCookieName = own2("xsrfCookieName");
    let headers = own2("headers");
    const auth = own2("auth");
    const baseURL = own2("baseURL");
    const allowAbsoluteUrls = own2("allowAbsoluteUrls");
    const url = own2("url");
    newConfig.headers = headers = AxiosHeaders_default.from(headers);
    newConfig.url = buildURL(
      buildFullPath(baseURL, url, allowAbsoluteUrls, newConfig),
      own2("params"),
      own2("paramsSerializer")
    );
    if (auth) {
      const username = utils_default.getSafeProp(auth, "username") || "";
      const password = utils_default.getSafeProp(auth, "password") || "";
      try {
        headers.set(
          "Authorization",
          "Basic " + btoa(username + ":" + (password ? encodeUTF8(password) : ""))
        );
      } catch (e) {
        throw AxiosError_default.from(e, AxiosError_default.ERR_BAD_OPTION_VALUE, config);
      }
    }
    if (utils_default.isFormData(data2)) {
      if (platform_default.hasStandardBrowserEnv || platform_default.hasStandardBrowserWebWorkerEnv || utils_default.isReactNative(data2)) {
        headers.setContentType(void 0);
      } else if (utils_default.isFunction(data2.getHeaders)) {
        setFormDataHeaders(headers, data2.getHeaders(), own2("formDataHeaderPolicy"));
      }
    }
    if (platform_default.hasStandardBrowserEnv) {
      if (utils_default.isFunction(withXSRFToken)) {
        withXSRFToken = withXSRFToken(newConfig);
      }
      const shouldSendXSRF = withXSRFToken === true || withXSRFToken == null && isURLSameOrigin_default(newConfig.url);
      if (shouldSendXSRF) {
        const xsrfValue = xsrfHeaderName && xsrfCookieName && cookies_default.read(xsrfCookieName);
        if (xsrfValue) {
          headers.set(xsrfHeaderName, xsrfValue);
        }
      }
    }
    return newConfig;
  }
  var resolveConfig_default = resolveConfig;

  // node_modules/axios/lib/adapters/xhr.js
  var isXHRAdapterSupported = typeof XMLHttpRequest !== "undefined";
  var xhr_default = isXHRAdapterSupported && function(config) {
    return new Promise(function dispatchXhrRequest(resolve, reject) {
      const _config = resolveConfig_default(config);
      let requestData = _config.data;
      const requestHeaders = AxiosHeaders_default.from(_config.headers).normalize();
      let { responseType, onUploadProgress, onDownloadProgress } = _config;
      let onCanceled;
      let uploadThrottled, downloadThrottled;
      let flushUpload, flushDownload;
      function done() {
        flushUpload && flushUpload();
        flushDownload && flushDownload();
        _config.cancelToken && _config.cancelToken.unsubscribe(onCanceled);
        _config.signal && _config.signal.removeEventListener("abort", onCanceled);
      }
      let request = new XMLHttpRequest();
      request.open(_config.method.toUpperCase(), _config.url, true);
      request.timeout = _config.timeout;
      function onloadend() {
        if (!request) {
          return;
        }
        const responseHeaders = AxiosHeaders_default.from(
          "getAllResponseHeaders" in request && request.getAllResponseHeaders()
        );
        const responseData = !responseType || responseType === "text" || responseType === "json" ? request.responseText : request.response;
        const response = {
          data: responseData,
          status: request.status,
          statusText: request.statusText,
          headers: responseHeaders,
          config,
          request
        };
        settle(
          function _resolve(value) {
            resolve(value);
            done();
          },
          function _reject(err) {
            reject(err);
            done();
          },
          response
        );
        request = null;
      }
      if ("onloadend" in request) {
        request.onloadend = onloadend;
      } else {
        request.onreadystatechange = function handleLoad() {
          if (!request || request.readyState !== 4) {
            return;
          }
          if (request.status === 0 && !(request.responseURL && request.responseURL.startsWith("file:"))) {
            return;
          }
          setTimeout(onloadend);
        };
      }
      request.onabort = function handleAbort() {
        if (!request) {
          return;
        }
        reject(new AxiosError_default("Request aborted", AxiosError_default.ECONNABORTED, config, request));
        done();
        request = null;
      };
      request.onerror = function handleError(event) {
        const msg = event && event.message ? event.message : "Network Error";
        const err = new AxiosError_default(msg, AxiosError_default.ERR_NETWORK, config, request);
        err.event = event || null;
        reject(err);
        done();
        request = null;
      };
      request.ontimeout = function handleTimeout() {
        let timeoutErrorMessage = _config.timeout ? "timeout of " + _config.timeout + "ms exceeded" : "timeout exceeded";
        const transitional2 = _config.transitional || transitional_default;
        if (_config.timeoutErrorMessage) {
          timeoutErrorMessage = _config.timeoutErrorMessage;
        }
        reject(
          new AxiosError_default(
            timeoutErrorMessage,
            transitional2.clarifyTimeoutError ? AxiosError_default.ETIMEDOUT : AxiosError_default.ECONNABORTED,
            config,
            request
          )
        );
        done();
        request = null;
      };
      requestData === void 0 && requestHeaders.setContentType(null);
      if ("setRequestHeader" in request) {
        utils_default.forEach(toByteStringHeaderObject(requestHeaders), function setRequestHeader(val, key) {
          request.setRequestHeader(key, val);
        });
      }
      if (!utils_default.isUndefined(_config.withCredentials)) {
        request.withCredentials = !!_config.withCredentials;
      }
      if (responseType && responseType !== "json") {
        request.responseType = _config.responseType;
      }
      if (onDownloadProgress) {
        [downloadThrottled, flushDownload] = progressEventReducer(onDownloadProgress, true);
        request.addEventListener("progress", downloadThrottled);
      }
      if (onUploadProgress && request.upload) {
        [uploadThrottled, flushUpload] = progressEventReducer(onUploadProgress);
        request.upload.addEventListener("progress", uploadThrottled);
        request.upload.addEventListener("loadend", flushUpload);
      }
      if (_config.cancelToken || _config.signal) {
        onCanceled = (cancel) => {
          if (!request) {
            return;
          }
          reject(!cancel || cancel.type ? new CanceledError_default(null, config, request) : cancel);
          request.abort();
          done();
          request = null;
        };
        _config.cancelToken && _config.cancelToken.subscribe(onCanceled);
        if (_config.signal) {
          _config.signal.aborted ? onCanceled() : _config.signal.addEventListener("abort", onCanceled);
        }
      }
      const protocol = parseProtocol(_config.url);
      if (protocol && !platform_default.protocols.includes(protocol)) {
        reject(
          new AxiosError_default(
            "Unsupported protocol " + protocol + ":",
            AxiosError_default.ERR_BAD_REQUEST,
            config
          )
        );
        done();
        return;
      }
      request.send(requestData || null);
    });
  };

  // node_modules/axios/lib/adapters/fetch.js
  init_define_import_meta_env();

  // node_modules/axios/lib/helpers/composeSignals.js
  init_define_import_meta_env();
  var composeSignals = (signals, timeout) => {
    signals = signals ? signals.filter(Boolean) : [];
    if (!timeout && !signals.length) {
      return;
    }
    const controller = new AbortController();
    let aborted = false;
    const onabort = function(reason) {
      if (!aborted) {
        aborted = true;
        unsubscribe();
        const err = reason instanceof Error ? reason : this.reason;
        controller.abort(
          err instanceof AxiosError_default ? err : new CanceledError_default(err instanceof Error ? err.message : err)
        );
      }
    };
    let timer = timeout && setTimeout(() => {
      timer = null;
      onabort(new AxiosError_default(`timeout of ${timeout}ms exceeded`, AxiosError_default.ETIMEDOUT));
    }, timeout);
    const unsubscribe = () => {
      if (!signals) {
        return;
      }
      timer && clearTimeout(timer);
      timer = null;
      signals.forEach((signal2) => {
        signal2.unsubscribe ? signal2.unsubscribe(onabort) : signal2.removeEventListener("abort", onabort);
      });
      signals = null;
    };
    signals.forEach((signal2) => signal2.addEventListener("abort", onabort, { once: true }));
    const { signal } = controller;
    signal.unsubscribe = () => utils_default.asap(unsubscribe);
    return signal;
  };
  var composeSignals_default = composeSignals;

  // node_modules/axios/lib/helpers/trackStream.js
  init_define_import_meta_env();
  var streamChunk = function* (chunk, chunkSize) {
    let len = chunk.byteLength;
    if (!chunkSize || len < chunkSize) {
      yield chunk;
      return;
    }
    let pos = 0;
    let end;
    while (pos < len) {
      end = pos + chunkSize;
      yield chunk.slice(pos, end);
      pos = end;
    }
  };
  var readBytes = async function* (iterable, chunkSize) {
    for await (const chunk of readStream(iterable)) {
      yield* streamChunk(chunk, chunkSize);
    }
  };
  var readStream = async function* (stream) {
    if (stream[Symbol.asyncIterator]) {
      yield* stream;
      return;
    }
    const reader = stream.getReader();
    try {
      for (; ; ) {
        const { done, value } = await reader.read();
        if (done) {
          break;
        }
        yield value;
      }
    } finally {
      await reader.cancel();
    }
  };
  var trackStream = (stream, chunkSize, onProgress, onFinish) => {
    const iterator2 = readBytes(stream, chunkSize);
    let bytes = 0;
    let done;
    let _onFinish = (e) => {
      if (!done) {
        done = true;
        onFinish && onFinish(e);
      }
    };
    return new ReadableStream(
      {
        async pull(controller) {
          try {
            const { done: done2, value } = await iterator2.next();
            if (done2) {
              _onFinish();
              controller.close();
              return;
            }
            let len = value.byteLength;
            if (onProgress) {
              let loadedBytes = bytes += len;
              onProgress(loadedBytes);
            }
            controller.enqueue(new Uint8Array(value));
          } catch (err) {
            _onFinish(err);
            throw err;
          }
        },
        cancel(reason) {
          _onFinish(reason);
          return iterator2.return();
        }
      },
      {
        highWaterMark: 2
      }
    );
  };

  // node_modules/axios/lib/helpers/estimateDataURLDecodedBytes.js
  init_define_import_meta_env();
  var isHexDigit = (charCode) => charCode >= 48 && charCode <= 57 || charCode >= 65 && charCode <= 70 || charCode >= 97 && charCode <= 102;
  var isPercentEncodedByte = (str, i, len) => i + 2 < len && isHexDigit(str.charCodeAt(i + 1)) && isHexDigit(str.charCodeAt(i + 2));
  function estimateDataURLDecodedBytes(url) {
    if (!url || typeof url !== "string") return 0;
    if (!url.startsWith("data:")) return 0;
    const comma = url.indexOf(",");
    if (comma < 0) return 0;
    const meta = url.slice(5, comma);
    const body = url.slice(comma + 1);
    const isBase64 = /;base64/i.test(meta);
    if (isBase64) {
      let effectiveLen = body.length;
      const len = body.length;
      for (let i = 0; i < len; i++) {
        if (body.charCodeAt(i) === 37 && i + 2 < len) {
          const a = body.charCodeAt(i + 1);
          const b = body.charCodeAt(i + 2);
          const isHex = isHexDigit(a) && isHexDigit(b);
          if (isHex) {
            effectiveLen -= 2;
            i += 2;
          }
        }
      }
      let pad = 0;
      let idx = len - 1;
      const tailIsPct3D = (j) => j >= 2 && body.charCodeAt(j - 2) === 37 && // '%'
      body.charCodeAt(j - 1) === 51 && // '3'
      (body.charCodeAt(j) === 68 || body.charCodeAt(j) === 100);
      if (idx >= 0) {
        if (body.charCodeAt(idx) === 61) {
          pad++;
          idx--;
        } else if (tailIsPct3D(idx)) {
          pad++;
          idx -= 3;
        }
      }
      if (pad === 1 && idx >= 0) {
        if (body.charCodeAt(idx) === 61) {
          pad++;
        } else if (tailIsPct3D(idx)) {
          pad++;
        }
      }
      const groups = Math.floor(effectiveLen / 4);
      const bytes2 = groups * 3 - (pad || 0);
      return bytes2 > 0 ? bytes2 : 0;
    }
    let bytes = 0;
    for (let i = 0, len = body.length; i < len; i++) {
      const c = body.charCodeAt(i);
      if (c === 37 && isPercentEncodedByte(body, i, len)) {
        bytes += 1;
        i += 2;
      } else if (c < 128) {
        bytes += 1;
      } else if (c < 2048) {
        bytes += 2;
      } else if (c >= 55296 && c <= 56319 && i + 1 < len) {
        const next = body.charCodeAt(i + 1);
        if (next >= 56320 && next <= 57343) {
          bytes += 4;
          i++;
        } else {
          bytes += 3;
        }
      } else {
        bytes += 3;
      }
    }
    return bytes;
  }

  // node_modules/axios/lib/env/data.js
  init_define_import_meta_env();
  var VERSION = "1.18.1";

  // node_modules/axios/lib/adapters/fetch.js
  var DEFAULT_CHUNK_SIZE = 64 * 1024;
  var { isFunction: isFunction2 } = utils_default;
  var encodeUTF82 = (str) => encodeURIComponent(str).replace(
    /%([0-9A-F]{2})/gi,
    (_, hex) => String.fromCharCode(parseInt(hex, 16))
  );
  var decodeURIComponentSafe = (value) => {
    if (!utils_default.isString(value)) {
      return value;
    }
    try {
      return decodeURIComponent(value);
    } catch (error) {
      return value;
    }
  };
  var test = (fn, ...args) => {
    try {
      return !!fn(...args);
    } catch (e) {
      return false;
    }
  };
  var maybeWithAuthCredentials = (url) => {
    const protocolIndex = url.indexOf("://");
    let urlToCheck = url;
    if (protocolIndex !== -1) {
      urlToCheck = urlToCheck.slice(protocolIndex + 3);
    }
    return urlToCheck.includes("@") || urlToCheck.includes(":");
  };
  var factory = (env) => {
    const globalObject = utils_default.global !== void 0 && utils_default.global !== null ? utils_default.global : globalThis;
    const { ReadableStream: ReadableStream2, TextEncoder: TextEncoder2 } = globalObject;
    env = utils_default.merge.call(
      {
        skipUndefined: true
      },
      {
        Request: globalObject.Request,
        Response: globalObject.Response
      },
      env
    );
    const { fetch: envFetch, Request: Request2, Response: Response2 } = env;
    const isFetchSupported = envFetch ? isFunction2(envFetch) : typeof fetch === "function";
    const isRequestSupported = isFunction2(Request2);
    const isResponseSupported = isFunction2(Response2);
    if (!isFetchSupported) {
      return false;
    }
    const isReadableStreamSupported = isFetchSupported && isFunction2(ReadableStream2);
    const encodeText = isFetchSupported && (typeof TextEncoder2 === "function" ? /* @__PURE__ */ ((encoder) => (str) => encoder.encode(str))(new TextEncoder2()) : async (str) => new Uint8Array(await new Request2(str).arrayBuffer()));
    const supportsRequestStream = isRequestSupported && isReadableStreamSupported && test(() => {
      let duplexAccessed = false;
      const request = new Request2(platform_default.origin, {
        body: new ReadableStream2(),
        method: "POST",
        get duplex() {
          duplexAccessed = true;
          return "half";
        }
      });
      const hasContentType = request.headers.has("Content-Type");
      if (request.body != null) {
        request.body.cancel();
      }
      return duplexAccessed && !hasContentType;
    });
    const supportsResponseStream = isResponseSupported && isReadableStreamSupported && test(() => utils_default.isReadableStream(new Response2("").body));
    const resolvers = {
      stream: supportsResponseStream && ((res) => res.body)
    };
    isFetchSupported && (() => {
      ["text", "arrayBuffer", "blob", "formData", "stream"].forEach((type) => {
        !resolvers[type] && (resolvers[type] = (res, config) => {
          let method = res && res[type];
          if (method) {
            return method.call(res);
          }
          throw new AxiosError_default(
            `Response type '${type}' is not supported`,
            AxiosError_default.ERR_NOT_SUPPORT,
            config
          );
        });
      });
    })();
    const getBodyLength = async (body) => {
      if (body == null) {
        return 0;
      }
      if (utils_default.isBlob(body)) {
        return body.size;
      }
      if (utils_default.isSpecCompliantForm(body)) {
        const _request = new Request2(platform_default.origin, {
          method: "POST",
          body
        });
        return (await _request.arrayBuffer()).byteLength;
      }
      if (utils_default.isArrayBufferView(body) || utils_default.isArrayBuffer(body)) {
        return body.byteLength;
      }
      if (utils_default.isURLSearchParams(body)) {
        body = body + "";
      }
      if (utils_default.isString(body)) {
        return (await encodeText(body)).byteLength;
      }
    };
    const resolveBodyLength = async (headers, body) => {
      const length = utils_default.toFiniteNumber(headers.getContentLength());
      return length == null ? getBodyLength(body) : length;
    };
    return async (config) => {
      let {
        url,
        method,
        data: data2,
        signal,
        cancelToken,
        timeout,
        onDownloadProgress,
        onUploadProgress,
        responseType,
        headers,
        withCredentials = "same-origin",
        fetchOptions,
        maxContentLength,
        maxBodyLength
      } = resolveConfig_default(config);
      const hasMaxContentLength = utils_default.isNumber(maxContentLength) && maxContentLength > -1;
      const hasMaxBodyLength = utils_default.isNumber(maxBodyLength) && maxBodyLength > -1;
      const own2 = (key) => utils_default.hasOwnProp(config, key) ? config[key] : void 0;
      let _fetch = envFetch || fetch;
      responseType = responseType ? (responseType + "").toLowerCase() : "text";
      let composedSignal = composeSignals_default(
        [signal, cancelToken && cancelToken.toAbortSignal()],
        timeout
      );
      let request = null;
      const unsubscribe = composedSignal && composedSignal.unsubscribe && (() => {
        composedSignal.unsubscribe();
      });
      let requestContentLength;
      let pendingBodyError = null;
      const maxBodyLengthError = () => new AxiosError_default(
        "Request body larger than maxBodyLength limit",
        AxiosError_default.ERR_BAD_REQUEST,
        config,
        request
      );
      try {
        let auth = void 0;
        const configAuth = own2("auth");
        if (configAuth) {
          const username = utils_default.getSafeProp(configAuth, "username") || "";
          const password = utils_default.getSafeProp(configAuth, "password") || "";
          auth = {
            username,
            password
          };
        }
        if (maybeWithAuthCredentials(url)) {
          const parsedURL = new URL(url, platform_default.origin);
          if (!auth && (parsedURL.username || parsedURL.password)) {
            const urlUsername = decodeURIComponentSafe(parsedURL.username);
            const urlPassword = decodeURIComponentSafe(parsedURL.password);
            auth = {
              username: urlUsername,
              password: urlPassword
            };
          }
          if (parsedURL.username || parsedURL.password) {
            parsedURL.username = "";
            parsedURL.password = "";
            url = parsedURL.href;
          }
        }
        if (auth) {
          headers.delete("authorization");
          headers.set(
            "Authorization",
            "Basic " + btoa(encodeUTF82((auth.username || "") + ":" + (auth.password || "")))
          );
        }
        if (hasMaxContentLength && typeof url === "string" && url.startsWith("data:")) {
          const estimated = estimateDataURLDecodedBytes(url);
          if (estimated > maxContentLength) {
            throw new AxiosError_default(
              "maxContentLength size of " + maxContentLength + " exceeded",
              AxiosError_default.ERR_BAD_RESPONSE,
              config,
              request
            );
          }
        }
        if (hasMaxBodyLength && method !== "get" && method !== "head") {
          const outboundLength = await getBodyLength(data2);
          if (typeof outboundLength === "number" && isFinite(outboundLength)) {
            requestContentLength = outboundLength;
            if (outboundLength > maxBodyLength) {
              throw maxBodyLengthError();
            }
          }
        }
        const mustEnforceStreamBody = hasMaxBodyLength && (utils_default.isReadableStream(data2) || utils_default.isStream(data2));
        const trackRequestStream = (stream, onProgress, flush) => trackStream(
          stream,
          DEFAULT_CHUNK_SIZE,
          (loadedBytes) => {
            if (hasMaxBodyLength && loadedBytes > maxBodyLength) {
              throw pendingBodyError = maxBodyLengthError();
            }
            onProgress && onProgress(loadedBytes);
          },
          flush
        );
        if (supportsRequestStream && method !== "get" && method !== "head" && (onUploadProgress || mustEnforceStreamBody)) {
          requestContentLength = requestContentLength == null ? await resolveBodyLength(headers, data2) : requestContentLength;
          if (requestContentLength !== 0 || mustEnforceStreamBody) {
            let _request = new Request2(url, {
              method: "POST",
              body: data2,
              duplex: "half"
            });
            let contentTypeHeader;
            if (utils_default.isFormData(data2) && (contentTypeHeader = _request.headers.get("content-type"))) {
              headers.setContentType(contentTypeHeader);
            }
            if (_request.body) {
              const [onProgress, flush] = onUploadProgress && progressEventDecorator(
                requestContentLength,
                progressEventReducer(asyncDecorator(onUploadProgress))
              ) || [];
              data2 = trackRequestStream(_request.body, onProgress, flush);
            }
          }
        } else if (mustEnforceStreamBody && !isRequestSupported && isReadableStreamSupported && method !== "get" && method !== "head") {
          data2 = trackRequestStream(data2);
        } else if (mustEnforceStreamBody && isRequestSupported && !supportsRequestStream && method !== "get" && method !== "head") {
          throw new AxiosError_default(
            "Stream request bodies are not supported by the current fetch implementation",
            AxiosError_default.ERR_NOT_SUPPORT,
            config,
            request
          );
        }
        if (!utils_default.isString(withCredentials)) {
          withCredentials = withCredentials ? "include" : "omit";
        }
        const isCredentialsSupported = isRequestSupported && "credentials" in Request2.prototype;
        if (utils_default.isFormData(data2)) {
          const contentType = headers.getContentType();
          if (contentType && /^multipart\/form-data/i.test(contentType) && !/boundary=/i.test(contentType)) {
            headers.delete("content-type");
          }
        }
        headers.set("User-Agent", "axios/" + VERSION, false);
        const resolvedOptions = {
          ...fetchOptions,
          signal: composedSignal,
          method: method.toUpperCase(),
          headers: toByteStringHeaderObject(headers.normalize()),
          body: data2,
          duplex: "half",
          credentials: isCredentialsSupported ? withCredentials : void 0
        };
        request = isRequestSupported && new Request2(url, resolvedOptions);
        let response = await (isRequestSupported ? _fetch(request, fetchOptions) : _fetch(url, resolvedOptions));
        const responseHeaders = AxiosHeaders_default.from(response.headers);
        if (hasMaxContentLength) {
          const declaredLength = utils_default.toFiniteNumber(responseHeaders.getContentLength());
          if (declaredLength != null && declaredLength > maxContentLength) {
            throw new AxiosError_default(
              "maxContentLength size of " + maxContentLength + " exceeded",
              AxiosError_default.ERR_BAD_RESPONSE,
              config,
              request
            );
          }
        }
        const isStreamResponse = supportsResponseStream && (responseType === "stream" || responseType === "response");
        if (supportsResponseStream && response.body && (onDownloadProgress || hasMaxContentLength || isStreamResponse && unsubscribe)) {
          const options = {};
          ["status", "statusText", "headers"].forEach((prop) => {
            options[prop] = response[prop];
          });
          const responseContentLength = utils_default.toFiniteNumber(responseHeaders.getContentLength());
          const [onProgress, flush] = onDownloadProgress && progressEventDecorator(
            responseContentLength,
            progressEventReducer(asyncDecorator(onDownloadProgress), true)
          ) || [];
          let bytesRead = 0;
          const onChunkProgress = (loadedBytes) => {
            if (hasMaxContentLength) {
              bytesRead = loadedBytes;
              if (bytesRead > maxContentLength) {
                throw new AxiosError_default(
                  "maxContentLength size of " + maxContentLength + " exceeded",
                  AxiosError_default.ERR_BAD_RESPONSE,
                  config,
                  request
                );
              }
            }
            onProgress && onProgress(loadedBytes);
          };
          response = new Response2(
            trackStream(response.body, DEFAULT_CHUNK_SIZE, onChunkProgress, () => {
              flush && flush();
              unsubscribe && unsubscribe();
            }),
            options
          );
        }
        responseType = responseType || "text";
        let responseData = await resolvers[utils_default.findKey(resolvers, responseType) || "text"](
          response,
          config
        );
        if (hasMaxContentLength && !supportsResponseStream && !isStreamResponse) {
          let materializedSize;
          if (responseData != null) {
            if (typeof responseData.byteLength === "number") {
              materializedSize = responseData.byteLength;
            } else if (typeof responseData.size === "number") {
              materializedSize = responseData.size;
            } else if (typeof responseData === "string") {
              materializedSize = typeof TextEncoder2 === "function" ? new TextEncoder2().encode(responseData).byteLength : responseData.length;
            }
          }
          if (typeof materializedSize === "number" && materializedSize > maxContentLength) {
            throw new AxiosError_default(
              "maxContentLength size of " + maxContentLength + " exceeded",
              AxiosError_default.ERR_BAD_RESPONSE,
              config,
              request
            );
          }
        }
        !isStreamResponse && unsubscribe && unsubscribe();
        return await new Promise((resolve, reject) => {
          settle(resolve, reject, {
            data: responseData,
            headers: AxiosHeaders_default.from(response.headers),
            status: response.status,
            statusText: response.statusText,
            config,
            request
          });
        });
      } catch (err) {
        unsubscribe && unsubscribe();
        if (composedSignal && composedSignal.aborted && composedSignal.reason instanceof AxiosError_default) {
          const canceledError = composedSignal.reason;
          canceledError.config = config;
          request && (canceledError.request = request);
          if (err !== canceledError) {
            Object.defineProperty(canceledError, "cause", {
              __proto__: null,
              value: err,
              writable: true,
              enumerable: false,
              configurable: true
            });
          }
          throw canceledError;
        }
        if (pendingBodyError) {
          request && !pendingBodyError.request && (pendingBodyError.request = request);
          throw pendingBodyError;
        }
        if (err instanceof AxiosError_default) {
          request && !err.request && (err.request = request);
          throw err;
        }
        if (err && err.name === "TypeError" && /Load failed|fetch/i.test(err.message)) {
          const networkError = new AxiosError_default(
            "Network Error",
            AxiosError_default.ERR_NETWORK,
            config,
            request,
            err && err.response
          );
          Object.defineProperty(networkError, "cause", {
            __proto__: null,
            value: err.cause || err,
            writable: true,
            enumerable: false,
            configurable: true
          });
          throw networkError;
        }
        throw AxiosError_default.from(err, err && err.code, config, request, err && err.response);
      }
    };
  };
  var seedCache = /* @__PURE__ */ new Map();
  var getFetch = (config) => {
    let env = config && config.env || {};
    const { fetch: fetch2, Request: Request2, Response: Response2 } = env;
    const seeds = [Request2, Response2, fetch2];
    let len = seeds.length, i = len, seed, target, map = seedCache;
    while (i--) {
      seed = seeds[i];
      target = map.get(seed);
      target === void 0 && map.set(seed, target = i ? /* @__PURE__ */ new Map() : factory(env));
      map = target;
    }
    return target;
  };
  var adapter = getFetch();

  // node_modules/axios/lib/adapters/adapters.js
  var knownAdapters = {
    http: null_default,
    xhr: xhr_default,
    fetch: {
      get: getFetch
    }
  };
  utils_default.forEach(knownAdapters, (fn, value) => {
    if (fn) {
      try {
        Object.defineProperty(fn, "name", { __proto__: null, value });
      } catch (e) {
      }
      Object.defineProperty(fn, "adapterName", { __proto__: null, value });
    }
  });
  var renderReason = (reason) => `- ${reason}`;
  var isResolvedHandle = (adapter2) => utils_default.isFunction(adapter2) || adapter2 === null || adapter2 === false;
  function getAdapter(adapters, config) {
    adapters = utils_default.isArray(adapters) ? adapters : [adapters];
    const { length } = adapters;
    let nameOrAdapter;
    let adapter2;
    const rejectedReasons = {};
    for (let i = 0; i < length; i++) {
      nameOrAdapter = adapters[i];
      let id;
      adapter2 = nameOrAdapter;
      if (!isResolvedHandle(nameOrAdapter)) {
        adapter2 = knownAdapters[(id = String(nameOrAdapter)).toLowerCase()];
        if (adapter2 === void 0) {
          throw new AxiosError_default(`Unknown adapter '${id}'`);
        }
      }
      if (adapter2 && (utils_default.isFunction(adapter2) || (adapter2 = adapter2.get(config)))) {
        break;
      }
      rejectedReasons[id || "#" + i] = adapter2;
    }
    if (!adapter2) {
      const reasons = Object.entries(rejectedReasons).map(
        ([id, state]) => `adapter ${id} ` + (state === false ? "is not supported by the environment" : "is not available in the build")
      );
      let s = length ? reasons.length > 1 ? "since :\n" + reasons.map(renderReason).join("\n") : " " + renderReason(reasons[0]) : "as no adapter specified";
      throw new AxiosError_default(
        `There is no suitable adapter to dispatch the request ` + s,
        AxiosError_default.ERR_NOT_SUPPORT
      );
    }
    return adapter2;
  }
  var adapters_default = {
    /**
     * Resolve an adapter from a list of adapter names or functions.
     * @type {Function}
     */
    getAdapter,
    /**
     * Exposes all known adapters
     * @type {Object<string, Function|Object>}
     */
    adapters: knownAdapters
  };

  // node_modules/axios/lib/core/dispatchRequest.js
  function throwIfCancellationRequested(config) {
    if (config.cancelToken) {
      config.cancelToken.throwIfRequested();
    }
    if (config.signal && config.signal.aborted) {
      throw new CanceledError_default(null, config);
    }
  }
  function dispatchRequest(config) {
    throwIfCancellationRequested(config);
    config.headers = AxiosHeaders_default.from(config.headers);
    config.data = transformData.call(config, config.transformRequest);
    if (["post", "put", "patch"].indexOf(config.method) !== -1) {
      config.headers.setContentType("application/x-www-form-urlencoded", false);
    }
    const adapter2 = adapters_default.getAdapter(config.adapter || defaults_default.adapter, config);
    return adapter2(config).then(
      function onAdapterResolution(response) {
        throwIfCancellationRequested(config);
        config.response = response;
        try {
          response.data = transformData.call(config, config.transformResponse, response);
        } finally {
          delete config.response;
        }
        response.headers = AxiosHeaders_default.from(response.headers);
        return response;
      },
      function onAdapterRejection(reason) {
        if (!isCancel(reason)) {
          throwIfCancellationRequested(config);
          if (reason && reason.response) {
            config.response = reason.response;
            try {
              reason.response.data = transformData.call(
                config,
                config.transformResponse,
                reason.response
              );
            } finally {
              delete config.response;
            }
            reason.response.headers = AxiosHeaders_default.from(reason.response.headers);
          }
        }
        return Promise.reject(reason);
      }
    );
  }

  // node_modules/axios/lib/helpers/validator.js
  init_define_import_meta_env();
  var validators = {};
  ["object", "boolean", "number", "function", "string", "symbol"].forEach((type, i) => {
    validators[type] = function validator(thing) {
      return typeof thing === type || "a" + (i < 1 ? "n " : " ") + type;
    };
  });
  var deprecatedWarnings = {};
  validators.transitional = function transitional(validator, version, message) {
    function formatMessage(opt, desc) {
      return "[Axios v" + VERSION + "] Transitional option '" + opt + "'" + desc + (message ? ". " + message : "");
    }
    return (value, opt, opts) => {
      if (validator === false) {
        throw new AxiosError_default(
          formatMessage(opt, " has been removed" + (version ? " in " + version : "")),
          AxiosError_default.ERR_DEPRECATED
        );
      }
      if (version && !deprecatedWarnings[opt]) {
        deprecatedWarnings[opt] = true;
        console.warn(
          formatMessage(
            opt,
            " has been deprecated since v" + version + " and will be removed in the near future"
          )
        );
      }
      return validator ? validator(value, opt, opts) : true;
    };
  };
  validators.spelling = function spelling(correctSpelling) {
    return (value, opt) => {
      console.warn(`${opt} is likely a misspelling of ${correctSpelling}`);
      return true;
    };
  };
  function assertOptions(options, schema, allowUnknown) {
    if (typeof options !== "object" || options === null) {
      throw new AxiosError_default("options must be an object", AxiosError_default.ERR_BAD_OPTION_VALUE);
    }
    const keys = Object.keys(options);
    let i = keys.length;
    while (i-- > 0) {
      const opt = keys[i];
      const validator = Object.prototype.hasOwnProperty.call(schema, opt) ? schema[opt] : void 0;
      if (validator) {
        const value = options[opt];
        const result = value === void 0 || validator(value, opt, options);
        if (result !== true) {
          throw new AxiosError_default(
            "option " + opt + " must be " + result,
            AxiosError_default.ERR_BAD_OPTION_VALUE
          );
        }
        continue;
      }
      if (allowUnknown !== true) {
        throw new AxiosError_default("Unknown option " + opt, AxiosError_default.ERR_BAD_OPTION);
      }
    }
  }
  var validator_default = {
    assertOptions,
    validators
  };

  // node_modules/axios/lib/core/Axios.js
  var validators2 = validator_default.validators;
  var Axios = class {
    constructor(instanceConfig) {
      this.defaults = instanceConfig || {};
      this.interceptors = {
        request: new InterceptorManager_default(),
        response: new InterceptorManager_default()
      };
    }
    /**
     * Dispatch a request
     *
     * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
     * @param {?Object} config
     *
     * @returns {Promise} The Promise to be fulfilled
     */
    async request(configOrUrl, config) {
      try {
        return await this._request(configOrUrl, config);
      } catch (err) {
        if (err instanceof Error) {
          let dummy = {};
          Error.captureStackTrace ? Error.captureStackTrace(dummy) : dummy = new Error();
          const stack = (() => {
            if (!dummy.stack) {
              return "";
            }
            const firstNewlineIndex = dummy.stack.indexOf("\n");
            return firstNewlineIndex === -1 ? "" : dummy.stack.slice(firstNewlineIndex + 1);
          })();
          try {
            if (!err.stack) {
              err.stack = stack;
            } else if (stack) {
              const firstNewlineIndex = stack.indexOf("\n");
              const secondNewlineIndex = firstNewlineIndex === -1 ? -1 : stack.indexOf("\n", firstNewlineIndex + 1);
              const stackWithoutTwoTopLines = secondNewlineIndex === -1 ? "" : stack.slice(secondNewlineIndex + 1);
              if (!String(err.stack).endsWith(stackWithoutTwoTopLines)) {
                err.stack += "\n" + stack;
              }
            }
          } catch (e) {
          }
        }
        throw err;
      }
    }
    _request(configOrUrl, config) {
      if (typeof configOrUrl === "string") {
        config = config || {};
        config.url = configOrUrl;
      } else {
        config = configOrUrl || {};
      }
      config = mergeConfig(this.defaults, config);
      const { transitional: transitional2, paramsSerializer, headers } = config;
      if (transitional2 !== void 0) {
        validator_default.assertOptions(
          transitional2,
          {
            silentJSONParsing: validators2.transitional(validators2.boolean),
            forcedJSONParsing: validators2.transitional(validators2.boolean),
            clarifyTimeoutError: validators2.transitional(validators2.boolean),
            legacyInterceptorReqResOrdering: validators2.transitional(validators2.boolean),
            advertiseZstdAcceptEncoding: validators2.transitional(validators2.boolean),
            validateStatusUndefinedResolves: validators2.transitional(validators2.boolean)
          },
          false
        );
      }
      if (paramsSerializer != null) {
        if (utils_default.isFunction(paramsSerializer)) {
          config.paramsSerializer = {
            serialize: paramsSerializer
          };
        } else {
          validator_default.assertOptions(
            paramsSerializer,
            {
              encode: validators2.function,
              serialize: validators2.function
            },
            true
          );
        }
      }
      if (config.allowAbsoluteUrls !== void 0) {
      } else if (this.defaults.allowAbsoluteUrls !== void 0) {
        config.allowAbsoluteUrls = this.defaults.allowAbsoluteUrls;
      } else {
        config.allowAbsoluteUrls = true;
      }
      validator_default.assertOptions(
        config,
        {
          baseUrl: validators2.spelling("baseURL"),
          withXsrfToken: validators2.spelling("withXSRFToken")
        },
        true
      );
      config.method = (config.method || this.defaults.method || "get").toLowerCase();
      let contextHeaders = headers && utils_default.merge(headers.common, headers[config.method]);
      headers && utils_default.forEach(["delete", "get", "head", "post", "put", "patch", "query", "common"], (method) => {
        delete headers[method];
      });
      config.headers = AxiosHeaders_default.concat(contextHeaders, headers);
      const requestInterceptorChain = [];
      let synchronousRequestInterceptors = true;
      this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
        if (typeof interceptor.runWhen === "function" && interceptor.runWhen(config) === false) {
          return;
        }
        synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;
        const transitional3 = config.transitional || transitional_default;
        const legacyInterceptorReqResOrdering = transitional3 && transitional3.legacyInterceptorReqResOrdering;
        if (legacyInterceptorReqResOrdering) {
          requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
        } else {
          requestInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
        }
      });
      const responseInterceptorChain = [];
      this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
        responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
      });
      let promise;
      let i = 0;
      let len;
      if (!synchronousRequestInterceptors) {
        const chain = [dispatchRequest.bind(this), void 0];
        chain.unshift(...requestInterceptorChain);
        chain.push(...responseInterceptorChain);
        len = chain.length;
        promise = Promise.resolve(config);
        while (i < len) {
          promise = promise.then(chain[i++], chain[i++]);
        }
        return promise;
      }
      len = requestInterceptorChain.length;
      let newConfig = config;
      while (i < len) {
        const onFulfilled = requestInterceptorChain[i++];
        const onRejected = requestInterceptorChain[i++];
        try {
          newConfig = onFulfilled(newConfig);
        } catch (error) {
          onRejected.call(this, error);
          break;
        }
      }
      try {
        promise = dispatchRequest.call(this, newConfig);
      } catch (error) {
        return Promise.reject(error);
      }
      i = 0;
      len = responseInterceptorChain.length;
      while (i < len) {
        promise = promise.then(responseInterceptorChain[i++], responseInterceptorChain[i++]);
      }
      return promise;
    }
    getUri(config) {
      config = mergeConfig(this.defaults, config);
      const fullPath = buildFullPath(config.baseURL, config.url, config.allowAbsoluteUrls, config);
      return buildURL(fullPath, config.params, config.paramsSerializer);
    }
  };
  utils_default.forEach(["delete", "get", "head", "options"], function forEachMethodNoData(method) {
    Axios.prototype[method] = function(url, config) {
      return this.request(
        mergeConfig(config || {}, {
          method,
          url,
          data: config && utils_default.hasOwnProp(config, "data") ? config.data : void 0
        })
      );
    };
  });
  utils_default.forEach(["post", "put", "patch", "query"], function forEachMethodWithData(method) {
    function generateHTTPMethod(isForm) {
      return function httpMethod(url, data2, config) {
        return this.request(
          mergeConfig(config || {}, {
            method,
            headers: isForm ? {
              "Content-Type": "multipart/form-data"
            } : {},
            url,
            data: data2
          })
        );
      };
    }
    Axios.prototype[method] = generateHTTPMethod();
    if (method !== "query") {
      Axios.prototype[method + "Form"] = generateHTTPMethod(true);
    }
  });
  var Axios_default = Axios;

  // node_modules/axios/lib/cancel/CancelToken.js
  init_define_import_meta_env();
  var CancelToken = class _CancelToken {
    constructor(executor) {
      if (typeof executor !== "function") {
        throw new TypeError("executor must be a function.");
      }
      let resolvePromise;
      this.promise = new Promise(function promiseExecutor(resolve) {
        resolvePromise = resolve;
      });
      const token = this;
      this.promise.then((cancel) => {
        if (!token._listeners) return;
        let i = token._listeners.length;
        while (i-- > 0) {
          token._listeners[i](cancel);
        }
        token._listeners = null;
      });
      this.promise.then = (onfulfilled) => {
        let _resolve;
        const promise = new Promise((resolve) => {
          token.subscribe(resolve);
          _resolve = resolve;
        }).then(onfulfilled);
        promise.cancel = function reject() {
          token.unsubscribe(_resolve);
        };
        return promise;
      };
      executor(function cancel(message, config, request) {
        if (token.reason) {
          return;
        }
        token.reason = new CanceledError_default(message, config, request);
        resolvePromise(token.reason);
      });
    }
    /**
     * Throws a `CanceledError` if cancellation has been requested.
     */
    throwIfRequested() {
      if (this.reason) {
        throw this.reason;
      }
    }
    /**
     * Subscribe to the cancel signal
     */
    subscribe(listener) {
      if (this.reason) {
        listener(this.reason);
        return;
      }
      if (this._listeners) {
        this._listeners.push(listener);
      } else {
        this._listeners = [listener];
      }
    }
    /**
     * Unsubscribe from the cancel signal
     */
    unsubscribe(listener) {
      if (!this._listeners) {
        return;
      }
      const index = this._listeners.indexOf(listener);
      if (index !== -1) {
        this._listeners.splice(index, 1);
      }
    }
    toAbortSignal() {
      const controller = new AbortController();
      const abort = (err) => {
        controller.abort(err);
      };
      this.subscribe(abort);
      controller.signal.unsubscribe = () => this.unsubscribe(abort);
      return controller.signal;
    }
    /**
     * Returns an object that contains a new `CancelToken` and a function that, when called,
     * cancels the `CancelToken`.
     */
    static source() {
      let cancel;
      const token = new _CancelToken(function executor(c) {
        cancel = c;
      });
      return {
        token,
        cancel
      };
    }
  };
  var CancelToken_default = CancelToken;

  // node_modules/axios/lib/helpers/spread.js
  init_define_import_meta_env();
  function spread(callback) {
    return function wrap(arr) {
      return callback.apply(null, arr);
    };
  }

  // node_modules/axios/lib/helpers/isAxiosError.js
  init_define_import_meta_env();
  function isAxiosError(payload) {
    return utils_default.isObject(payload) && payload.isAxiosError === true;
  }

  // node_modules/axios/lib/helpers/HttpStatusCode.js
  init_define_import_meta_env();
  var HttpStatusCode = {
    Continue: 100,
    SwitchingProtocols: 101,
    Processing: 102,
    EarlyHints: 103,
    Ok: 200,
    Created: 201,
    Accepted: 202,
    NonAuthoritativeInformation: 203,
    NoContent: 204,
    ResetContent: 205,
    PartialContent: 206,
    MultiStatus: 207,
    AlreadyReported: 208,
    ImUsed: 226,
    MultipleChoices: 300,
    MovedPermanently: 301,
    Found: 302,
    SeeOther: 303,
    NotModified: 304,
    UseProxy: 305,
    Unused: 306,
    TemporaryRedirect: 307,
    PermanentRedirect: 308,
    BadRequest: 400,
    Unauthorized: 401,
    PaymentRequired: 402,
    Forbidden: 403,
    NotFound: 404,
    MethodNotAllowed: 405,
    NotAcceptable: 406,
    ProxyAuthenticationRequired: 407,
    RequestTimeout: 408,
    Conflict: 409,
    Gone: 410,
    LengthRequired: 411,
    PreconditionFailed: 412,
    PayloadTooLarge: 413,
    UriTooLong: 414,
    UnsupportedMediaType: 415,
    RangeNotSatisfiable: 416,
    ExpectationFailed: 417,
    ImATeapot: 418,
    MisdirectedRequest: 421,
    UnprocessableEntity: 422,
    Locked: 423,
    FailedDependency: 424,
    TooEarly: 425,
    UpgradeRequired: 426,
    PreconditionRequired: 428,
    TooManyRequests: 429,
    RequestHeaderFieldsTooLarge: 431,
    UnavailableForLegalReasons: 451,
    InternalServerError: 500,
    NotImplemented: 501,
    BadGateway: 502,
    ServiceUnavailable: 503,
    GatewayTimeout: 504,
    HttpVersionNotSupported: 505,
    VariantAlsoNegotiates: 506,
    InsufficientStorage: 507,
    LoopDetected: 508,
    NotExtended: 510,
    NetworkAuthenticationRequired: 511,
    WebServerIsDown: 521,
    ConnectionTimedOut: 522,
    OriginIsUnreachable: 523,
    TimeoutOccurred: 524,
    SslHandshakeFailed: 525,
    InvalidSslCertificate: 526
  };
  Object.entries(HttpStatusCode).forEach(([key, value]) => {
    HttpStatusCode[value] = key;
  });
  var HttpStatusCode_default = HttpStatusCode;

  // node_modules/axios/lib/axios.js
  function createInstance(defaultConfig) {
    const context = new Axios_default(defaultConfig);
    const instance = bind(Axios_default.prototype.request, context);
    utils_default.extend(instance, Axios_default.prototype, context, { allOwnKeys: true });
    utils_default.extend(instance, context, null, { allOwnKeys: true });
    instance.create = function create3(instanceConfig) {
      return createInstance(mergeConfig(defaultConfig, instanceConfig));
    };
    return instance;
  }
  var axios = createInstance(defaults_default);
  axios.Axios = Axios_default;
  axios.CanceledError = CanceledError_default;
  axios.CancelToken = CancelToken_default;
  axios.isCancel = isCancel;
  axios.VERSION = VERSION;
  axios.toFormData = toFormData_default;
  axios.AxiosError = AxiosError_default;
  axios.Cancel = axios.CanceledError;
  axios.all = function all(promises) {
    return Promise.all(promises);
  };
  axios.spread = spread;
  axios.isAxiosError = isAxiosError;
  axios.mergeConfig = mergeConfig;
  axios.AxiosHeaders = AxiosHeaders_default;
  axios.formToJSON = (thing) => formDataToJSON_default(utils_default.isHTMLForm(thing) ? new FormData(thing) : thing);
  axios.getAdapter = adapters_default.getAdapter;
  axios.HttpStatusCode = HttpStatusCode_default;
  axios.default = axios;
  var axios_default = axios;

  // node_modules/axios/index.js
  var {
    Axios: Axios2,
    AxiosError: AxiosError2,
    CanceledError: CanceledError2,
    isCancel: isCancel2,
    CancelToken: CancelToken2,
    VERSION: VERSION2,
    all: all2,
    Cancel,
    isAxiosError: isAxiosError2,
    spread: spread2,
    toFormData: toFormData2,
    AxiosHeaders: AxiosHeaders2,
    HttpStatusCode: HttpStatusCode2,
    formToJSON,
    getAdapter: getAdapter2,
    mergeConfig: mergeConfig2,
    create: create2
  } = axios_default;

  // src/services/api.ts
  var api = axios_default.create({
    baseURL: "/api",
    headers: { "Content-Type": "application/json" }
  });
  api.interceptors.request.use((config) => {
    const token = localStorage.getItem("dwes_token");
    if (token) config.headers.Authorization = `Bearer ${token}`;
    return config;
  });
  api.interceptors.response.use(
    (res) => res,
    (err) => {
      const url = err.config?.url ?? "";
      const isLoginRequest = url.includes("/auth/login");
      if (err.response?.status === 401 && !isLoginRequest) {
        localStorage.removeItem("dwes_token");
        localStorage.removeItem("dwes_user");
        window.location.href = "/";
      }
      return Promise.reject(err);
    }
  );
  var api_default = api;
  var authApi = {
    login: (username, password, project_code) => api.post("/auth/login", { username, password, project_code }).then((r) => r.data),
    logout: () => api.post("/auth/logout").then((r) => r.data),
    me: () => api.get("/me").then((r) => r.data),
    health: () => api.get("/health").then((r) => r.data),
    env: () => api.get("/env").then((r) => r.data),
    hints: () => api.get("/login-hints").then((r) => r.data)
  };
  var usersApi = {
    list: () => api.get("/users").then((r) => r.data),
    get: (id) => api.get(`/users/${id}`).then((r) => r.data),
    technicians: () => api.get("/users/technicians").then((r) => r.data),
    create: (dto) => api.post("/users", dto).then((r) => r.data),
    update: (id, dto) => api.put(`/users/${id}`, dto).then((r) => r.data),
    toggleStatus: (id) => api.post(`/users/${id}/toggle-status`).then((r) => r.data),
    resetPassword: (id, password) => api.post(`/users/${id}/reset-password`, { password }).then((r) => r.data),
    remove: (id) => api.delete(`/users/${id}`).then((r) => r.data)
  };
  var projectsApi = {
    list: () => api.get("/projects").then((r) => r.data),
    create: (dto) => api.post("/projects", dto).then((r) => r.data),
    update: (code, dto) => api.put(`/projects/${code}`, dto).then((r) => r.data),
    remove: (code) => api.delete(`/projects/${code}`).then((r) => r.data),
    setState: (code, state) => api.post(`/projects/${code}/state`, { state }).then((r) => r.data),
    assign: (code, techIds) => api.post(`/projects/${code}/assign`, { technicians: techIds }).then((r) => r.data),
    frameReportPdf: (code, frameId) => api.get(`/projects/${code}/frames/${frameId}/report-pdf`, { responseType: "blob" }).then((r) => r.data),
    reportPdf: (code, frameId) => frameId ? api.get(`/projects/${code}/frames/${frameId}/report-pdf`, { responseType: "blob" }).then((r) => r.data) : api.get(`/projects/${code}/report-pdf`, { responseType: "blob" }).then((r) => r.data),
    reportXlsx: (code) => api.get(`/projects/${code}/report-xlsx`, { responseType: "blob" }).then((r) => r.data),
    frames: (code) => api.get(`/projects/${code}/frames`).then((r) => r.data),
    frame: (code, frameId) => api.get(`/projects/${code}/frames/${frameId}`).then((r) => r.data),
    cables: (code) => api.get(`/projects/${code}/cables`).then((r) => r.data),
    compareVerify: (code, frameId) => api.post(`/projects/${code}/frames/${frameId}/compare-verify`).then((r) => r.data),
    compareSubmit: (code, frameId) => api.post(`/projects/${code}/frames/${frameId}/compare-submit`).then((r) => r.data),
    compareStatus: (code, frameId) => api.get(`/projects/${code}/frames/${frameId}/compare-status`).then((r) => r.data),
    verifyData: (code, frameId) => api.get(`/projects/${code}/frames/${frameId}/verify-data`).then((r) => r.data),
    patchCable: (code, frameId, idx, field, value) => api.post(`/projects/${code}/frames/${frameId}/patch-cable`, { cable_index: idx, field, value }).then((r) => r.data),
    remapColumn: (code, frameId, systemField, excelHeader) => api.post(`/projects/${code}/frames/${frameId}/remap-column`, { system_field: systemField, excel_header: excelHeader }).then((r) => r.data),
    verifyConfirm: (code, frameId) => api.post(`/projects/${code}/frames/${frameId}/verify-confirm`).then((r) => r.data),
    compareSourceFile: (code, frameId, formData) => api.post(`/projects/${code}/frames/${frameId}/compare-source-file`, formData, {
      headers: { "Content-Type": "multipart/form-data" }
    }).then((r) => r.data),
    drawings: (code) => api.get(`/projects/${code}/drawings`).then((r) => r.data),
    // Fetch a drawing file as a Blob (auth header is attached by the axios interceptor;
    // a raw new-tab GET would not carry the JWT). Caller decides inline-open vs download.
    drawingFile: (code, id) => api.get(`/projects/${code}/drawings/${id}/file`, { responseType: "blob" }).then((r) => r.data),
    deleteFramePrecheck: (code, frameId) => api.get(`/projects/${code}/frames/${frameId}/delete-precheck`).then((r) => r.data),
    deleteFrameGuarded: (code, frameId, confirmedPhrase) => api.post(`/projects/${code}/frames/${frameId}/delete-guarded`, { confirmed_phrase: confirmedPhrase }).then((r) => r.data),
    deleteDrawingPrecheck: (code, drawingId) => api.get(`/projects/${code}/drawings/${drawingId}/delete-precheck`).then((r) => r.data),
    deleteDrawingGuarded: (code, drawingId, confirmedPhrase) => api.post(`/projects/${code}/drawings/${drawingId}/delete-guarded`, { confirmed_phrase: confirmedPhrase }).then((r) => r.data),
    deleteDrawing: (code, drawingId) => api.delete(`/projects/${code}/drawings/${drawingId}`).then((r) => r.data),
    directorReports: (code) => api.get(`/projects/${code}/director-reports`).then((r) => r.data),
    directorReportFile: (code, id) => api.get(`/projects/${code}/director-reports/${id}/file`, { responseType: "blob" }).then((r) => r.data),
    deleteDirectorReport: (code, reportId) => api.delete(`/projects/${code}/director-reports/${reportId}`).then((r) => r.data)
  };
  var supervisorApi = {
    allPanels: () => api.get("/supervisor/all-panels").then((r) => r.data),
    reviewPanels: (code) => api.get(`/supervisor/review-panels/${code}`).then((r) => r.data),
    panelDetail: (id) => api.get(`/supervisor/panel-detail/${id}`).then((r) => r.data),
    review: (id, status, notes) => api.post(`/supervisor/review/${id}`, { review_status: status, review_notes: notes }).then((r) => r.data),
    pendingApprovals: () => api.get("/supervisor/pending-approvals").then((r) => r.data),
    approve: (id) => api.post(`/supervisor/approve-assignment/${id}`).then((r) => r.data),
    rework: (id, reason) => api.post(`/supervisor/rework-assignment/${id}`, { reason }).then((r) => r.data),
    pendingChangeovers: () => api.get("/supervisor/pending-changeovers").then((r) => r.data),
    changeoverCandidate: (projectCode, frameId) => api.get(`/supervisor/changeover-candidate/${encodeURIComponent(projectCode)}/${encodeURIComponent(frameId)}`).then((r) => r.data),
    midChangeover: (payload) => api.post("/supervisor/mid-changeover", payload).then((r) => r.data),
    changeover: (oldId, newTechId, changeoverReason, reasonNotes) => api.post("/tech/changeover", {
      old_assignment_id: oldId,
      new_technician_id: newTechId,
      changeover_reason: changeoverReason,
      reason_notes: reasonNotes
    }).then((r) => r.data),
    frameProgress: (projectCode, frameId) => api.get(`/supervisor/frame-progress/${projectCode}/${frameId}`).then((r) => r.data),
    completionReport: (id) => api.get(`/supervisor/completion-report/${id}`).then((r) => r.data),
    assignFrame: (dto) => api.post("/tech/assign-frame", dto).then((r) => r.data),
    panelReport: (code, frame) => api.get(`/supervisor/panel-report/${code}/${frame}`).then((r) => r.data),
    panelReportXlsx: (code, frame) => api.get(`/supervisor/panel-report/${code}/${frame}/xlsx`, { responseType: "blob" }).then((r) => r.data),
    wiringScheduleXlsx: (code, frameId) => api.get(`/supervisor/wiring-schedule/${code}/${frameId}/xlsx`, { responseType: "blob" }).then((r) => r.data)
  };
  var adminApi = {
    diagnostics: () => api.get("/admin/diagnostics").then((r) => r.data),
    clearCache: () => api.post("/admin/diagnostics/clear-cache").then((r) => r.data),
    resetCounters: () => api.post("/admin/diagnostics/reset-counters").then((r) => r.data),
    dbPing: () => api.post("/admin/diagnostics/db-ping").then((r) => r.data),
    syncStorage: () => api.get("/admin/sync/storage").then((r) => r.data),
    syncInspect: () => api.post("/admin/sync/inspect").then((r) => r.data),
    syncStatus: () => api.get("/admin/sync/status").then((r) => r.data),
    syncExecute: (strategy) => api.post("/admin/sync/execute", { strategy }).then((r) => r.data),
    allUsers: () => api.get("/admin/users").then((r) => r.data),
    changeRole: (id, role) => api.post(`/admin/users/${id}/change-role`, { role }).then((r) => r.data),
    bulkSetRole: (pattern, role) => api.post("/admin/users/bulk-role", { pattern, role }).then((r) => r.data),
    // Database configuration (System Admin only)
    getDbConfig: () => api.get("/admin/db-config").then((r) => r.data),
    setDbConfig: (mode, cloudUrl, notes) => api.post("/admin/db-config", { mode, cloudUrl, notes }).then((r) => r.data),
    getDeploymentConfig: () => api.get("/admin/deployment-config").then((r) => r.data),
    setDeploymentConfig: (mode, cloudAppUrl, cloudTier, cloudRegion, notes) => api.post("/admin/deployment-config", { mode, cloudAppUrl, cloudTier, cloudRegion, notes }).then((r) => r.data),
    testDbConnection: (url) => api.post("/admin/db-config/test", { url }).then((r) => r.data),
    triggerRestart: () => api.post("/admin/restart").then((r) => r.data),
    fileStorageInfo: () => api.get("/admin/file-storage").then((r) => r.data),
    hardResetPrecheck: (code) => api.get(`/admin/projects/${code}/hard-reset`).then((r) => r.data),
    hardReset: (code, confirmedCode) => api.post(`/admin/projects/${code}/hard-reset`, { confirmed_code: confirmedCode }).then((r) => r.data),
    hardDeletePrecheck: (code) => api.get(`/admin/projects/${code}/hard-delete`).then((r) => r.data),
    hardDelete: (code, confirmedCode) => api.post(`/admin/projects/${code}/hard-delete`, { confirmed_code: confirmedCode }).then((r) => r.data),
    resetAllPrecheck: () => api.get("/admin/reset-all-projects").then((r) => r.data),
    resetAllProjects: (confirmedPhrase) => api.post("/admin/reset-all-projects", { confirmed_phrase: confirmedPhrase }).then((r) => r.data)
  };

  // src/store/useProjectSelectionStore.ts
  init_define_import_meta_env();

  // node_modules/zustand/esm/middleware.mjs
  init_define_import_meta_env();
  function createJSONStorage(getStorage, options) {
    let storage;
    try {
      storage = getStorage();
    } catch (e) {
      return;
    }
    const persistStorage = {
      getItem: (name) => {
        var _a;
        const parse = (str2) => {
          if (str2 === null) {
            return null;
          }
          return JSON.parse(str2, options == null ? void 0 : options.reviver);
        };
        const str = (_a = storage.getItem(name)) != null ? _a : null;
        if (str instanceof Promise) {
          return str.then(parse);
        }
        return parse(str);
      },
      setItem: (name, newValue) => storage.setItem(name, JSON.stringify(newValue, options == null ? void 0 : options.replacer)),
      removeItem: (name) => storage.removeItem(name)
    };
    return persistStorage;
  }
  var toThenable = (fn) => (input) => {
    try {
      const result = fn(input);
      if (result instanceof Promise) {
        return result;
      }
      return {
        then(onFulfilled) {
          return toThenable(onFulfilled)(result);
        },
        catch(_onRejected) {
          return this;
        }
      };
    } catch (e) {
      return {
        then(_onFulfilled) {
          return this;
        },
        catch(onRejected) {
          return toThenable(onRejected)(e);
        }
      };
    }
  };
  var persistImpl = (config, baseOptions) => (set, get, api2) => {
    let options = {
      storage: createJSONStorage(() => window.localStorage),
      partialize: (state) => state,
      version: 0,
      merge: (persistedState, currentState) => ({
        ...currentState,
        ...persistedState
      }),
      ...baseOptions
    };
    let hasHydrated = false;
    let hydrationVersion = 0;
    const hydrationListeners = /* @__PURE__ */ new Set();
    const finishHydrationListeners = /* @__PURE__ */ new Set();
    let storage = options.storage;
    if (!storage) {
      return config(
        (...args) => {
          console.warn(
            `[zustand persist middleware] Unable to update item '${options.name}', the given storage is currently unavailable.`
          );
          set(...args);
        },
        get,
        api2
      );
    }
    const setItem = () => {
      const state = options.partialize({ ...get() });
      return storage.setItem(options.name, {
        state,
        version: options.version
      });
    };
    const savedSetState = api2.setState;
    api2.setState = (state, replace2) => {
      savedSetState(state, replace2);
      return setItem();
    };
    const configResult = config(
      (...args) => {
        set(...args);
        return setItem();
      },
      get,
      api2
    );
    api2.getInitialState = () => configResult;
    let stateFromStorage;
    const hydrate = () => {
      var _a, _b;
      if (!storage) return;
      const currentVersion = ++hydrationVersion;
      hasHydrated = false;
      hydrationListeners.forEach((cb) => {
        var _a2;
        return cb((_a2 = get()) != null ? _a2 : configResult);
      });
      const postRehydrationCallback = ((_b = options.onRehydrateStorage) == null ? void 0 : _b.call(options, (_a = get()) != null ? _a : configResult)) || void 0;
      return toThenable(storage.getItem.bind(storage))(options.name).then((deserializedStorageValue) => {
        if (deserializedStorageValue) {
          if (typeof deserializedStorageValue.version === "number" && deserializedStorageValue.version !== options.version) {
            if (options.migrate) {
              const migration = options.migrate(
                deserializedStorageValue.state,
                deserializedStorageValue.version
              );
              if (migration instanceof Promise) {
                return migration.then((result) => [true, result]);
              }
              return [true, migration];
            }
            console.error(
              `State loaded from storage couldn't be migrated since no migrate function was provided`
            );
          } else {
            return [false, deserializedStorageValue.state];
          }
        }
        return [false, void 0];
      }).then((migrationResult) => {
        var _a2;
        if (currentVersion !== hydrationVersion) {
          return;
        }
        const [migrated, migratedState] = migrationResult;
        stateFromStorage = options.merge(
          migratedState,
          (_a2 = get()) != null ? _a2 : configResult
        );
        set(stateFromStorage, true);
        if (migrated) {
          return setItem();
        }
      }).then(() => {
        if (currentVersion !== hydrationVersion) {
          return;
        }
        postRehydrationCallback == null ? void 0 : postRehydrationCallback(get(), void 0);
        stateFromStorage = get();
        hasHydrated = true;
        finishHydrationListeners.forEach((cb) => cb(stateFromStorage));
      }).catch((e) => {
        if (currentVersion !== hydrationVersion) {
          return;
        }
        postRehydrationCallback == null ? void 0 : postRehydrationCallback(void 0, e);
      });
    };
    api2.persist = {
      setOptions: (newOptions) => {
        options = {
          ...options,
          ...newOptions
        };
        if (newOptions.storage) {
          storage = newOptions.storage;
        }
      },
      clearStorage: () => {
        storage == null ? void 0 : storage.removeItem(options.name);
      },
      getOptions: () => options,
      rehydrate: () => hydrate(),
      hasHydrated: () => hasHydrated,
      onHydrate: (cb) => {
        hydrationListeners.add(cb);
        return () => {
          hydrationListeners.delete(cb);
        };
      },
      onFinishHydration: (cb) => {
        finishHydrationListeners.add(cb);
        return () => {
          finishHydrationListeners.delete(cb);
        };
      }
    };
    if (!options.skipHydration) {
      hydrate();
    }
    return stateFromStorage || configResult;
  };
  var persist = persistImpl;

  // src/store/useProjectSelectionStore.ts
  var PROJECT_SELECTION_STORAGE_KEY = "dwes-project-selection-session";
  var useProjectSelectionStore = create()(
    persist(
      (set, get) => ({
        selectedProject: null,
        ownerUserId: null,
        setProjectForUser: (project, userId) => set({ selectedProject: project, ownerUserId: userId }),
        initializeForUser: (userId) => {
          const state = get();
          if (state.ownerUserId !== userId) {
            set({ selectedProject: null, ownerUserId: userId });
          }
        },
        clearSelection: () => set({ selectedProject: null, ownerUserId: null })
      }),
      {
        name: PROJECT_SELECTION_STORAGE_KEY,
        storage: createJSONStorage(() => sessionStorage)
      }
    )
  );

  // src/store/useAuthStore.ts
  var useAuthStore = create((set) => ({
    user: null,
    token: null,
    isLoading: false,
    error: null,
    hydrateFromStorage: () => {
      try {
        const token = localStorage.getItem("dwes_token");
        const userRaw = localStorage.getItem("dwes_user");
        if (token && userRaw) {
          set({ token, user: JSON.parse(userRaw) });
        }
      } catch {
        localStorage.removeItem("dwes_token");
        localStorage.removeItem("dwes_user");
      }
    },
    login: async (username, password, projectCode) => {
      set({ isLoading: true, error: null });
      try {
        const data2 = await authApi.login(username, password, projectCode);
        localStorage.setItem("dwes_token", data2.access_token);
        localStorage.setItem("dwes_user", JSON.stringify(data2.user));
        set({ user: data2.user, token: data2.access_token, isLoading: false, error: null });
      } catch (err) {
        const ax = err;
        const status = ax.response?.status;
        console.error("[auth] login failed:", {
          status: status ?? "(no response)",
          code: ax.code,
          message: ax.message,
          data: ax.response?.data
        });
        let msg;
        if (status === 401 || status === 403) {
          msg = "Incorrect username or password.";
        } else if (status === void 0 || status >= 500) {
          msg = "Can't reach the server. Please try again or contact support.";
        } else {
          msg = ax.response?.data?.message || "Login failed. Please try again.";
        }
        set({ isLoading: false, error: msg });
        throw err;
      }
    },
    logout: async () => {
      try {
        await authApi.logout();
      } catch {
      }
      localStorage.removeItem("dwes_token");
      localStorage.removeItem("dwes_user");
      sessionStorage.removeItem(PROJECT_SELECTION_STORAGE_KEY);
      set({ user: null, token: null, error: null });
    },
    clearError: () => set({ error: null })
  }));

  // src/components/Badge.tsx
  init_define_import_meta_env();
  var STATUS_CLASS = {
    active: "badge-green",
    completed: "badge-green",
    validated: "badge-green",
    verified: "badge-green",
    approved: "badge-green",
    in_progress: "badge-blue",
    in_review: "badge-blue",
    ready_for_qc: "badge-blue",
    paused: "badge-amber",
    rework: "badge-orange",
    stopped: "badge-orange",
    pending: "badge-gray",
    not_started: "badge-gray",
    none: "badge-gray",
    assigned: "badge-blue",
    pending_approval: "badge-amber"
  };
  function Badge({ label, className = "" }) {
    const key = label.toLowerCase().replace(/\s+/g, "_");
    const cls = STATUS_CLASS[key] ?? "badge-gray";
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        className: `badge ${cls} ${className}`
      },
      label.replace(/_/g, " ")
    );
  }

  // src/components/Modal.tsx
  init_define_import_meta_env();
  var import_react5 = __toESM(require_react_shim(), 1);
  var import_react_dom = __toESM(require_react_dom_shim(), 1);

  // src/components/ui/icons/index.tsx
  init_define_import_meta_env();

  // src/components/ui/Icon.tsx
  init_define_import_meta_env();
  var import_react3 = __toESM(require_react_shim(), 1);
  var SIZE_PX = {
    xs: 14,
    sm: 16,
    md: 20,
    lg: 24,
    xl: 32
  };
  function resolveSize(size) {
    if (size === void 0) return SIZE_PX.md;
    if (typeof size === "number") return size;
    return SIZE_PX[size];
  }
  function resolveWeight(strokeWidth, weight) {
    if (weight !== void 0) return weight;
    if (strokeWidth === void 0) return 400;
    if (strokeWidth <= 1.5) return 300;
    if (strokeWidth <= 2) return 400;
    return 500;
  }
  var Icon = (0, import_react3.forwardRef)(function Icon2({
    name,
    size,
    weight,
    filled = false,
    strokeWidth,
    depth = false,
    className = "",
    style,
    ...rest
  }, ref) {
    const px = resolveSize(size);
    const w = resolveWeight(strokeWidth, weight);
    const opsz = Math.min(48, Math.max(20, px));
    const iconStyle = {
      fontSize: px,
      fontVariationSettings: `'FILL' ${filled ? 1 : 0}, 'wght' ${w}, 'GRAD' 0, 'opsz' ${opsz}`,
      ...style
    };
    const classes = [
      "ui-icon",
      "material-symbols-rounded",
      depth ? "ui-icon--depth" : "",
      className
    ].filter(Boolean).join(" ");
    const ariaHidden = rest["aria-label"] ? void 0 : rest["aria-hidden"];
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        ref,
        className: classes,
        style: iconStyle,
        "aria-hidden": ariaHidden ?? true,
        ...rest
      },
      name
    );
  });

  // src/components/ui/icons/named-icons.tsx
  init_define_import_meta_env();
  var import_react4 = __toESM(require_react_shim(), 1);
  function sym(materialName, defaults2 = {}) {
    return (0, import_react4.forwardRef)(function NamedIcon({ filled, className = "", ...props }, ref) {
      const mergedClass = [defaults2.className, className].filter(Boolean).join(" ");
      return /* @__PURE__ */ React.createElement(
        Icon,
        {
          ref,
          name: materialName,
          filled: filled ?? defaults2.filled,
          className: mergedClass || void 0,
          ...props
        }
      );
    });
  }
  var Menu = sym("menu");
  var X = sym("close");
  var ChevronDown = sym("expand_more");
  var ChevronUp = sym("expand_less");
  var ChevronLeft = sym("chevron_left");
  var ChevronRight = sym("chevron_right");
  var ArrowRight = sym("arrow_forward");
  var ArrowLeft = sym("arrow_back");
  var ArrowLeftRight = sym("swap_horiz");
  var ArrowDownUp = sym("swap_vert");
  var ArrowUpRight = sym("north_east");
  var PanelLeftClose = sym("left_panel_close");
  var PanelLeftOpen = sym("left_panel_open");
  var LayoutGrid = sym("grid_view");
  var LayoutPanelTop = sym("dashboard");
  var FolderKanban = sym("view_kanban");
  var Maximize = sym("fullscreen");
  var Plus = sym("add");
  var Pencil = sym("edit");
  var Trash2 = sym("delete");
  var Download = sym("download");
  var InstallDesktop = sym("install_desktop");
  var Upload = sym("upload");
  var RefreshCw = sym("refresh");
  var RotateCcw = sym("restart_alt");
  var Save = sym("save");
  var Search = sym("search");
  var Check = sym("check");
  var CheckCheck = sym("done_all");
  var SendHorizonal = sym("send");
  var LogOut = sym("logout");
  var ExternalLink = sym("open_in_new");
  var ZoomIn = sym("zoom_in");
  var ZoomOut = sym("zoom_out");
  var Play = sym("play_arrow");
  var PlayCircle = sym("play_circle");
  var PauseCircle = sym("pause_circle");
  var Eye = sym("visibility");
  var EyeOff = sym("visibility_off");
  var Star = sym("star");
  var Flag = sym("flag");
  var Info = sym("info");
  var UserPlus = sym("person_add");
  var UserMinus = sym("person_remove");
  var CheckCircle = sym("check_circle");
  var CheckCircle2 = sym("task_alt");
  var CheckSquare = sym("check_box");
  var AlertCircle = sym("error");
  var AlertTriangle = sym("warning");
  var TriangleAlert = sym("warning");
  var CircleX = sym("cancel");
  var XCircle = sym("cancel");
  var ShieldAlert = sym("gpp_bad");
  var ShieldCheck = sym("verified_user");
  var ShieldOff = sym("remove_moderator");
  var Shield = sym("shield");
  var User = sym("person");
  var Users = sym("group");
  var UserCog = sym("manage_accounts");
  var UserX = sym("person_off");
  var Fingerprint = sym("fingerprint");
  var Lock = sym("lock");
  var Unlock = sym("lock_open");
  var Key = sym("key");
  var KeyRound = sym("key");
  var File = sym("draft");
  var FileText = sym("description");
  var FileImage = sym("image");
  var FileSpreadsheet = sym("table_chart");
  var FileDown = sym("file_download");
  var FolderOpen = sym("folder_open");
  var Paperclip = sym("attach_file");
  var ClipboardCheck = sym("assignment_turned_in");
  var ClipboardList = sym("assignment");
  var ListChecks = sym("checklist");
  var Wrench = sym("build");
  var PenTool = sym("draw");
  var QrCode = sym("qr_code_scanner");
  var Cable = sym("cable");
  var Zap = sym("bolt");
  var Database = sym("database");
  var HardDrive = sym("hard_drive");
  var Cloud = sym("cloud");
  var Wifi = sym("wifi");
  var Globe = sym("language");
  var Building2 = sym("apartment");
  var MapPin = sym("location_on");
  var Map2 = sym("map");
  var Calendar = sym("calendar_today");
  var Tag = sym("sell");
  var Hash = sym("tag");
  var Phone = sym("phone");
  var DollarSign = sym("attach_money");
  var Activity = sym("monitoring");
  var BarChart3 = sym("bar_chart");
  var Clock3 = sym("schedule");
  var Clock = sym("schedule");
  var Bell = sym("notifications");
  var Settings = sym("settings");
  var Moon = sym("dark_mode");
  var Sun = sym("light_mode");
  var Pin = sym("push_pin");
  var Columns3 = sym("view_column");
  var PanelTop = sym("web_asset");
  var MessageCircle = sym("chat");
  var Loader = sym("progress_activity", { className: "ui-icon--spin" });

  // src/components/Modal.tsx
  var MODAL_ROOT_ID = "modal-root";
  var modalStackDepth = 0;
  function getModalRoot() {
    if (typeof document === "undefined") return null;
    let root = document.getElementById(MODAL_ROOT_ID);
    if (!root) {
      root = document.createElement("div");
      root.id = MODAL_ROOT_ID;
      document.body.appendChild(root);
    }
    return root;
  }
  function Modal({
    title,
    onClose,
    children,
    footer,
    size = "default",
    closeOnBackdrop = true,
    closeOnEscape = true
  }) {
    const openedAt = (0, import_react5.useRef)(Date.now());
    const onCloseRef = (0, import_react5.useRef)(onClose);
    const closeOnBackdropRef = (0, import_react5.useRef)(closeOnBackdrop);
    const closeOnEscapeRef = (0, import_react5.useRef)(closeOnEscape);
    const [stackLevel, setStackLevel] = (0, import_react5.useState)(0);
    onCloseRef.current = onClose;
    closeOnBackdropRef.current = closeOnBackdrop;
    closeOnEscapeRef.current = closeOnEscape;
    (0, import_react5.useEffect)(() => {
      modalStackDepth += 1;
      const level = modalStackDepth;
      setStackLevel(level);
      const prevOverflow = document.body.style.overflow;
      document.body.style.overflow = "hidden";
      const onKey = (e) => {
        if (e.key === "Escape" && closeOnEscapeRef.current) onCloseRef.current();
      };
      window.addEventListener("keydown", onKey);
      return () => {
        window.removeEventListener("keydown", onKey);
        modalStackDepth = Math.max(0, modalStackDepth - 1);
        if (modalStackDepth === 0) {
          document.body.style.overflow = prevOverflow;
        }
      };
    }, []);
    const boxClass = size === "xl" ? "modal-box-xl" : size === "wide" ? "modal-box-wide" : size === "form" ? "modal-box-form" : size === "lg" ? "modal-box-lg" : size === "sm" ? "modal-box-sm" : "modal-box";
    const handleOverlayClick = () => {
      if (!closeOnBackdropRef.current) return;
      if (Date.now() - openedAt.current < 350) return;
      onCloseRef.current();
    };
    const overlay = /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "modal-overlay",
        style: { zIndex: 200 + stackLevel * 10 },
        role: "dialog",
        "aria-modal": "true",
        "aria-labelledby": "modal-title",
        onMouseDown: (e) => {
          if (e.target === e.currentTarget) e.preventDefault();
        },
        onClick: (e) => {
          if (e.target === e.currentTarget) handleOverlayClick();
        }
      },
      /* @__PURE__ */ React.createElement("div", { className: boxClass, onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("div", { className: "modal-header" }, /* @__PURE__ */ React.createElement("h2", { id: "modal-title", className: "modal-title" }, title), /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => onCloseRef.current(),
          className: "btn-icon",
          type: "button",
          "aria-label": "Close"
        },
        /* @__PURE__ */ React.createElement(X, { size: 18, strokeWidth: 1.5 })
      )), /* @__PURE__ */ React.createElement("div", { className: "modal-body" }, children), footer && /* @__PURE__ */ React.createElement("div", { className: "modal-footer" }, footer))
    );
    const root = getModalRoot();
    if (!root) return null;
    return (0, import_react_dom.createPortal)(overlay, root);
  }

  // src/components/ui/KpiCard.tsx
  init_define_import_meta_env();
  var import_react6 = __toESM(require_react_shim(), 1);
  var VALUE_CLASS = {
    default: "kpi-value",
    green: "kpi-value-green",
    blue: "kpi-value-blue",
    amber: "kpi-value-amber",
    red: "kpi-value-red"
  };
  function enhanceKpiIcon(icon) {
    if (!(0, import_react6.isValidElement)(icon)) return icon;
    return (0, import_react6.cloneElement)(icon, {
      size: 22,
      filled: true,
      className: "kpi-card-icon"
    });
  }
  function KpiCard({ label, value, variant = "default", icon }) {
    return /* @__PURE__ */ React.createElement(
      "article",
      {
        className: "kpi-card",
        "data-variant": variant
      },
      icon && /* @__PURE__ */ React.createElement("div", { className: "kpi-icon-wrap", "aria-hidden": "true" }, enhanceKpiIcon(icon)),
      /* @__PURE__ */ React.createElement("div", { className: "kpi-content" }, /* @__PURE__ */ React.createElement("div", { className: "kpi-label" }, label), /* @__PURE__ */ React.createElement("div", { className: `kpi-value-base ${VALUE_CLASS[variant]}` }, value))
    );
  }

  // src/components/ui/SectionHeader.tsx
  init_define_import_meta_env();
  function SectionHeader({
    title,
    description,
    actions,
    icon
  }) {
    return /* @__PURE__ */ React.createElement("header", { className: "app-section-header" }, /* @__PURE__ */ React.createElement("div", { className: "app-section-header-accent", "aria-hidden": true }), /* @__PURE__ */ React.createElement("div", { className: "app-section-header-body" }, /* @__PURE__ */ React.createElement("div", { className: "app-section-header-text min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "app-section-title-row" }, icon ? /* @__PURE__ */ React.createElement("span", { className: "app-section-icon" }, icon) : null, /* @__PURE__ */ React.createElement("h2", { className: "app-section-title" }, title)), /* @__PURE__ */ React.createElement("p", { className: "app-section-desc" }, description)), actions ? /* @__PURE__ */ React.createElement("div", { className: "app-section-header-actions shrink-0" }, actions) : null));
  }

  // src/components/ui/Toast.tsx
  init_define_import_meta_env();
  var import_react7 = __toESM(require_react_shim(), 1);
  var import_react_dom2 = __toESM(require_react_dom_shim(), 1);
  function Toast({
    message,
    tone = "warn",
    duration = 2600,
    onDismiss
  }) {
    (0, import_react7.useEffect)(() => {
      const id = setTimeout(onDismiss, duration);
      return () => clearTimeout(id);
    }, [onDismiss, duration]);
    const node = /* @__PURE__ */ React.createElement("div", { className: "wire-toast-wrap", "aria-live": "polite" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `wire-toast ${tone === "warn" ? "warn" : ""}`,
        role: "status"
      },
      /* @__PURE__ */ React.createElement("span", { className: "wire-toast-icon" }, tone === "warn" ? /* @__PURE__ */ React.createElement(TriangleAlert, { size: 14, strokeWidth: 2 }) : /* @__PURE__ */ React.createElement(CheckCircle, { size: 14, strokeWidth: 2 })),
      /* @__PURE__ */ React.createElement("div", { className: "wire-toast-title self-center" }, message)
    ));
    return typeof document !== "undefined" ? (0, import_react_dom2.createPortal)(node, document.body) : node;
  }

  // src/components/ui/ThemeToggle.tsx
  init_define_import_meta_env();
  function ThemeToggle() {
    const { mode, toggleMode } = useTheme();
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: toggleMode,
        className: "theme-toggle",
        "aria-label": mode === "light" ? "Switch to dark mode" : "Switch to light mode",
        title: mode === "light" ? "Switch to dark mode" : "Switch to light mode"
      },
      mode === "light" ? /* @__PURE__ */ React.createElement(Moon, { size: 20 }) : /* @__PURE__ */ React.createElement(Sun, { size: 20 })
    );
  }

  // src/components/ui/ButtonHintPopover.tsx
  init_define_import_meta_env();
  var import_react8 = __toESM(require_react_shim(), 1);
  var import_react_dom3 = __toESM(require_react_dom_shim(), 1);
  function ButtonHintPopover({
    anchorEl,
    message,
    tone = "warn",
    duration = 3200,
    onDismiss
  }) {
    const [pos, setPos] = (0, import_react8.useState)(null);
    const updatePos = (0, import_react8.useCallback)(() => {
      if (!anchorEl) {
        setPos(null);
        return;
      }
      const rect = anchorEl.getBoundingClientRect();
      const gap = 8;
      const estWidth = 220;
      const estHeight = 36;
      const fitsRight = rect.right + gap + estWidth <= window.innerWidth - 8;
      if (fitsRight) {
        setPos({
          top: rect.top + rect.height / 2,
          left: rect.right + gap,
          placement: "right"
        });
        return;
      }
      setPos({
        top: rect.bottom + gap,
        left: rect.left + rect.width / 2,
        placement: "bottom"
      });
      if (rect.bottom + gap + estHeight > window.innerHeight - 8) {
        setPos({
          top: Math.max(8, rect.top - gap - estHeight),
          left: rect.left + rect.width / 2,
          placement: "bottom"
        });
      }
    }, [anchorEl]);
    (0, import_react8.useLayoutEffect)(() => {
      updatePos();
      window.addEventListener("scroll", updatePos, true);
      window.addEventListener("resize", updatePos);
      return () => {
        window.removeEventListener("scroll", updatePos, true);
        window.removeEventListener("resize", updatePos);
      };
    }, [updatePos]);
    (0, import_react8.useEffect)(() => {
      if (!anchorEl || !message) return;
      const id = setTimeout(onDismiss, duration);
      return () => clearTimeout(id);
    }, [anchorEl, message, duration, onDismiss]);
    if (!anchorEl || !message || !pos) return null;
    const transform = pos.placement === "right" ? "translateY(-50%)" : "translateX(-50%)";
    return (0, import_react_dom3.createPortal)(
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "button-hint-popover-wrap",
          style: { top: pos.top, left: pos.left, transform },
          role: "alert",
          "aria-live": "polite"
        },
        /* @__PURE__ */ React.createElement("div", { className: `button-hint-popover ${tone}` }, /* @__PURE__ */ React.createElement("span", { className: "button-hint-popover-icon", "aria-hidden": true }, /* @__PURE__ */ React.createElement(TriangleAlert, { size: 14, strokeWidth: 2 })), /* @__PURE__ */ React.createElement("span", { className: "button-hint-popover-text" }, message))
      ),
      document.body
    );
  }

  // src/components/ui/OverflowActionMenu.tsx
  init_define_import_meta_env();
  var import_react9 = __toESM(require_react_shim(), 1);
  function OverflowActionMenu({
    actions,
    ariaLabel = "More actions"
  }) {
    const [open, setOpen] = (0, import_react9.useState)(false);
    const rootRef = (0, import_react9.useRef)(null);
    (0, import_react9.useEffect)(() => {
      if (!open) return;
      const onClickAway = (event) => {
        const root = rootRef.current;
        if (!root) return;
        if (event.target instanceof Node && !root.contains(event.target)) {
          setOpen(false);
        }
      };
      const onEsc = (event) => {
        if (event.key === "Escape") setOpen(false);
      };
      document.addEventListener("mousedown", onClickAway);
      document.addEventListener("keydown", onEsc);
      return () => {
        document.removeEventListener("mousedown", onClickAway);
        document.removeEventListener("keydown", onEsc);
      };
    }, [open]);
    return /* @__PURE__ */ React.createElement("div", { className: "ops-overflow", ref: rootRef }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "ops-icon-btn",
        "aria-label": ariaLabel,
        onClick: () => setOpen((v) => !v)
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "more_horiz", size: 18 })
    ), open && /* @__PURE__ */ React.createElement("div", { className: "ops-overflow-menu", role: "menu", "aria-label": ariaLabel }, actions.map((action) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: action.id,
        type: "button",
        role: "menuitem",
        className: `ops-overflow-item${action.destructive ? " danger" : ""}`,
        disabled: action.disabled,
        onClick: () => {
          setOpen(false);
          action.onClick();
        }
      },
      action.label
    ))));
  }

  // src/components/ui/CompanyLogo.tsx
  init_define_import_meta_env();
  var CANONICAL_LOGO_SRC = "/logo-full.png";
  var HEIGHT_CLS = { sm: "h-8", md: "h-10", lg: "h-12" };
  var PAD_CLS = {
    sm: "px-2 py-1.5",
    md: "px-3 py-2",
    lg: "px-4 py-3"
  };
  var RADIUS_CLS = {
    sm: "rounded-lg",
    md: "rounded-xl",
    lg: "rounded-2xl"
  };
  function CompanyLogo({ variant = "full", size = "md", className = "" }) {
    const hCls = HEIGHT_CLS[size];
    const pad = PAD_CLS[size];
    const rad = RADIUS_CLS[size];
    const img = /* @__PURE__ */ React.createElement(
      "img",
      {
        src: CANONICAL_LOGO_SRC,
        alt: "Ingenious Network FZC",
        className: `${hCls} w-auto object-contain block`
      }
    );
    if (variant === "white") {
      return /* @__PURE__ */ React.createElement("div", { className: `inline-flex w-fit items-center ${pad} bg-white ${rad} overflow-visible shadow-md ${className}` }, img);
    }
    if (variant === "icon") {
      return /* @__PURE__ */ React.createElement("div", { className: `inline-flex w-fit items-center ${pad} bg-white ${rad} overflow-visible ${className}` }, img);
    }
    return /* @__PURE__ */ React.createElement("div", { className: `inline-flex w-fit items-center overflow-visible ${className}` }, img);
  }

  // src/components/ui/DashboardShell.tsx
  init_define_import_meta_env();

  // src/components/layout/AppShell.tsx
  init_define_import_meta_env();
  var import_react14 = __toESM(require_react_shim(), 1);

  // src/components/layout/Topbar.tsx
  init_define_import_meta_env();
  var import_react13 = __toESM(require_react_shim(), 1);

  // src/components/layout/Avatar.tsx
  init_define_import_meta_env();
  var ROLE_BG = {
    system_admin: "bg-red-600 text-white",
    ops_director: "bg-purple-600 text-white",
    prod_supervisor: "bg-blue-600 text-white",
    qaqc_engineer: "bg-amber-500 text-white",
    wiring_technician: "bg-green-600 text-white"
  };
  function Avatar({ name, role, size = 36 }) {
    const initials = name.split(" ").map((p) => p[0]).slice(0, 2).join("").toUpperCase() || "?";
    const colorClass = role ? ROLE_BG[role] ?? "bg-slate-500 text-white" : "bg-slate-500 text-white";
    const fontSize = size <= 28 ? "text-[10px]" : size <= 36 ? "text-xs" : "text-sm";
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `avatar ${colorClass} ${fontSize}`,
        style: { width: size, height: size }
      },
      initials
    );
  }

  // src/components/biometric/BiometricSettings.tsx
  init_define_import_meta_env();
  var import_react11 = __toESM(require_react_shim(), 1);

  // src/services/webauthnApi.ts
  init_define_import_meta_env();
  var webAuthnApi = {
    registerOptions: () => api_default.post("/auth/webauthn/register/options").then((r) => r.data),
    registerVerify: (credential, deviceLabel2) => api_default.post("/auth/webauthn/register/verify", {
      credential,
      device_label: deviceLabel2
    }).then((r) => r.data),
    loginOptions: (username) => api_default.post(
      "/auth/webauthn/login/options",
      { username }
    ).then((r) => r.data),
    loginVerify: (sessionId, credential) => api_default.post("/auth/webauthn/login/verify", { sessionId, credential }).then((r) => r.data),
    status: () => api_default.get("/auth/webauthn/status").then((r) => r.data),
    config: () => api_default.get("/auth/webauthn/config").then((r) => r.data),
    removeCredential: (id) => api_default.delete(`/auth/webauthn/credentials/${encodeURIComponent(id)}`).then((r) => r.data)
  };

  // src/hooks/useBiometric.ts
  init_define_import_meta_env();
  var import_react10 = __toESM(require_react_shim(), 1);

  // node_modules/@simplewebauthn/browser/esm/index.js
  init_define_import_meta_env();

  // node_modules/@simplewebauthn/browser/esm/methods/startRegistration.js
  init_define_import_meta_env();

  // node_modules/@simplewebauthn/browser/esm/helpers/bufferToBase64URLString.js
  init_define_import_meta_env();
  function bufferToBase64URLString(buffer) {
    const bytes = new Uint8Array(buffer);
    let str = "";
    for (const charCode of bytes) {
      str += String.fromCharCode(charCode);
    }
    const base64String = btoa(str);
    return base64String.replace(/\+/g, "-").replace(/\//g, "_").replace(/=/g, "");
  }

  // node_modules/@simplewebauthn/browser/esm/helpers/base64URLStringToBuffer.js
  init_define_import_meta_env();
  function base64URLStringToBuffer(base64URLString) {
    const base64 = base64URLString.replace(/-/g, "+").replace(/_/g, "/");
    const padLength = (4 - base64.length % 4) % 4;
    const padded = base64.padEnd(base64.length + padLength, "=");
    const binary = atob(padded);
    const buffer = new ArrayBuffer(binary.length);
    const bytes = new Uint8Array(buffer);
    for (let i = 0; i < binary.length; i++) {
      bytes[i] = binary.charCodeAt(i);
    }
    return buffer;
  }

  // node_modules/@simplewebauthn/browser/esm/helpers/browserSupportsWebAuthn.js
  init_define_import_meta_env();
  function browserSupportsWebAuthn() {
    return _browserSupportsWebAuthnInternals.stubThis(globalThis?.PublicKeyCredential !== void 0 && typeof globalThis.PublicKeyCredential === "function");
  }
  var _browserSupportsWebAuthnInternals = {
    stubThis: (value) => value
  };

  // node_modules/@simplewebauthn/browser/esm/helpers/toPublicKeyCredentialDescriptor.js
  init_define_import_meta_env();
  function toPublicKeyCredentialDescriptor(descriptor) {
    const { id } = descriptor;
    return {
      ...descriptor,
      id: base64URLStringToBuffer(id),
      /**
       * `descriptor.transports` is an array of our `AuthenticatorTransportFuture` that includes newer
       * transports that TypeScript's DOM lib is ignorant of. Convince TS that our list of transports
       * are fine to pass to WebAuthn since browsers will recognize the new value.
       */
      transports: descriptor.transports
    };
  }

  // node_modules/@simplewebauthn/browser/esm/helpers/identifyRegistrationError.js
  init_define_import_meta_env();

  // node_modules/@simplewebauthn/browser/esm/helpers/isValidDomain.js
  init_define_import_meta_env();
  function isValidDomain(hostname) {
    return (
      // Consider localhost valid as well since it's okay wrt Secure Contexts
      hostname === "localhost" || // Support punycode (ACE) or ascii labels and domains
      /^((xn--[a-z0-9-]+|[a-z0-9]+(-[a-z0-9]+)*)\.)+([a-z]{2,}|xn--[a-z0-9-]+)$/i.test(hostname)
    );
  }

  // node_modules/@simplewebauthn/browser/esm/helpers/webAuthnError.js
  init_define_import_meta_env();
  var WebAuthnError = class extends Error {
    constructor({ message, code, cause, name }) {
      super(message, { cause });
      Object.defineProperty(this, "code", {
        enumerable: true,
        configurable: true,
        writable: true,
        value: void 0
      });
      this.name = name ?? cause.name;
      this.code = code;
    }
  };

  // node_modules/@simplewebauthn/browser/esm/helpers/identifyRegistrationError.js
  function identifyRegistrationError({ error, options }) {
    const { publicKey } = options;
    if (!publicKey) {
      throw Error("options was missing required publicKey property");
    }
    if (error.name === "AbortError") {
      if (options.signal instanceof AbortSignal) {
        return new WebAuthnError({
          message: "Registration ceremony was sent an abort signal",
          code: "ERROR_CEREMONY_ABORTED",
          cause: error
        });
      }
    } else if (error.name === "ConstraintError") {
      if (publicKey.authenticatorSelection?.requireResidentKey === true) {
        return new WebAuthnError({
          message: "Discoverable credentials were required but no available authenticator supported it",
          code: "ERROR_AUTHENTICATOR_MISSING_DISCOVERABLE_CREDENTIAL_SUPPORT",
          cause: error
        });
      } else if (
        // @ts-ignore: `mediation` doesn't yet exist on CredentialCreationOptions but it's possible as of Sept 2024
        options.mediation === "conditional" && publicKey.authenticatorSelection?.userVerification === "required"
      ) {
        return new WebAuthnError({
          message: "User verification was required during automatic registration but it could not be performed",
          code: "ERROR_AUTO_REGISTER_USER_VERIFICATION_FAILURE",
          cause: error
        });
      } else if (publicKey.authenticatorSelection?.userVerification === "required") {
        return new WebAuthnError({
          message: "User verification was required but no available authenticator supported it",
          code: "ERROR_AUTHENTICATOR_MISSING_USER_VERIFICATION_SUPPORT",
          cause: error
        });
      }
    } else if (error.name === "InvalidStateError") {
      return new WebAuthnError({
        message: "The authenticator was previously registered",
        code: "ERROR_AUTHENTICATOR_PREVIOUSLY_REGISTERED",
        cause: error
      });
    } else if (error.name === "NotAllowedError") {
      return new WebAuthnError({
        message: error.message,
        code: "ERROR_PASSTHROUGH_SEE_CAUSE_PROPERTY",
        cause: error
      });
    } else if (error.name === "NotSupportedError") {
      const validPubKeyCredParams = publicKey.pubKeyCredParams.filter((param) => param.type === "public-key");
      if (validPubKeyCredParams.length === 0) {
        return new WebAuthnError({
          message: 'No entry in pubKeyCredParams was of type "public-key"',
          code: "ERROR_MALFORMED_PUBKEYCREDPARAMS",
          cause: error
        });
      }
      return new WebAuthnError({
        message: "No available authenticator supported any of the specified pubKeyCredParams algorithms",
        code: "ERROR_AUTHENTICATOR_NO_SUPPORTED_PUBKEYCREDPARAMS_ALG",
        cause: error
      });
    } else if (error.name === "SecurityError") {
      const effectiveDomain = globalThis.location.hostname;
      if (!isValidDomain(effectiveDomain)) {
        return new WebAuthnError({
          message: `${globalThis.location.hostname} is an invalid domain`,
          code: "ERROR_INVALID_DOMAIN",
          cause: error
        });
      } else if (publicKey.rp.id !== effectiveDomain) {
        return new WebAuthnError({
          message: `The RP ID "${publicKey.rp.id}" is invalid for this domain`,
          code: "ERROR_INVALID_RP_ID",
          cause: error
        });
      }
    } else if (error.name === "TypeError") {
      if (publicKey.user.id.byteLength < 1 || publicKey.user.id.byteLength > 64) {
        return new WebAuthnError({
          message: "User ID was not between 1 and 64 characters",
          code: "ERROR_INVALID_USER_ID_LENGTH",
          cause: error
        });
      }
    } else if (error.name === "UnknownError") {
      return new WebAuthnError({
        message: "The authenticator was unable to process the specified options, or could not create a new credential",
        code: "ERROR_AUTHENTICATOR_GENERAL_ERROR",
        cause: error
      });
    }
    return error;
  }

  // node_modules/@simplewebauthn/browser/esm/helpers/webAuthnAbortService.js
  init_define_import_meta_env();
  var BaseWebAuthnAbortService = class {
    constructor() {
      Object.defineProperty(this, "controller", {
        enumerable: true,
        configurable: true,
        writable: true,
        value: void 0
      });
    }
    createNewAbortSignal() {
      if (this.controller) {
        const abortError = new Error("Cancelling existing WebAuthn API call for new one");
        abortError.name = "AbortError";
        this.controller.abort(abortError);
      }
      const newController = new AbortController();
      this.controller = newController;
      return newController.signal;
    }
    cancelCeremony() {
      if (this.controller) {
        const abortError = new Error("Manually cancelling existing WebAuthn API call");
        abortError.name = "AbortError";
        this.controller.abort(abortError);
        this.controller = void 0;
      }
    }
  };
  var WebAuthnAbortService = new BaseWebAuthnAbortService();

  // node_modules/@simplewebauthn/browser/esm/helpers/toAuthenticatorAttachment.js
  init_define_import_meta_env();
  var attachments = ["cross-platform", "platform"];
  function toAuthenticatorAttachment(attachment) {
    if (!attachment) {
      return;
    }
    if (attachments.indexOf(attachment) < 0) {
      return;
    }
    return attachment;
  }

  // node_modules/@simplewebauthn/browser/esm/methods/startRegistration.js
  async function startRegistration(options) {
    if (!options.optionsJSON && options.challenge) {
      console.warn("startRegistration() was not called correctly. It will try to continue with the provided options, but this call should be refactored to use the expected call structure instead. See https://simplewebauthn.dev/docs/packages/browser#typeerror-cannot-read-properties-of-undefined-reading-challenge for more information.");
      options = { optionsJSON: options };
    }
    const { optionsJSON, useAutoRegister = false } = options;
    if (!browserSupportsWebAuthn()) {
      throw new Error("WebAuthn is not supported in this browser");
    }
    const publicKey = {
      ...optionsJSON,
      challenge: base64URLStringToBuffer(optionsJSON.challenge),
      user: {
        ...optionsJSON.user,
        id: base64URLStringToBuffer(optionsJSON.user.id)
      },
      excludeCredentials: optionsJSON.excludeCredentials?.map(toPublicKeyCredentialDescriptor)
    };
    const createOptions = {};
    if (useAutoRegister) {
      createOptions.mediation = "conditional";
    }
    createOptions.publicKey = publicKey;
    createOptions.signal = WebAuthnAbortService.createNewAbortSignal();
    let credential;
    try {
      credential = await navigator.credentials.create(createOptions);
    } catch (err) {
      throw identifyRegistrationError({ error: err, options: createOptions });
    }
    if (!credential) {
      throw new Error("Registration was not completed");
    }
    const { id, rawId, response, type } = credential;
    let transports = void 0;
    if (typeof response.getTransports === "function") {
      transports = response.getTransports();
    }
    let responsePublicKeyAlgorithm = void 0;
    if (typeof response.getPublicKeyAlgorithm === "function") {
      try {
        responsePublicKeyAlgorithm = response.getPublicKeyAlgorithm();
      } catch (error) {
        warnOnBrokenImplementation("getPublicKeyAlgorithm()", error);
      }
    }
    let responsePublicKey = void 0;
    if (typeof response.getPublicKey === "function") {
      try {
        const _publicKey = response.getPublicKey();
        if (_publicKey !== null) {
          responsePublicKey = bufferToBase64URLString(_publicKey);
        }
      } catch (error) {
        warnOnBrokenImplementation("getPublicKey()", error);
      }
    }
    let responseAuthenticatorData;
    if (typeof response.getAuthenticatorData === "function") {
      try {
        responseAuthenticatorData = bufferToBase64URLString(response.getAuthenticatorData());
      } catch (error) {
        warnOnBrokenImplementation("getAuthenticatorData()", error);
      }
    }
    return {
      id,
      rawId: bufferToBase64URLString(rawId),
      response: {
        attestationObject: bufferToBase64URLString(response.attestationObject),
        clientDataJSON: bufferToBase64URLString(response.clientDataJSON),
        transports,
        publicKeyAlgorithm: responsePublicKeyAlgorithm,
        publicKey: responsePublicKey,
        authenticatorData: responseAuthenticatorData
      },
      type,
      clientExtensionResults: credential.getClientExtensionResults(),
      authenticatorAttachment: toAuthenticatorAttachment(credential.authenticatorAttachment)
    };
  }
  function warnOnBrokenImplementation(methodName, cause) {
    console.warn(`The browser extension that intercepted this WebAuthn API call incorrectly implemented ${methodName}. You should report this error to them.
`, cause);
  }

  // node_modules/@simplewebauthn/browser/esm/methods/startAuthentication.js
  init_define_import_meta_env();

  // node_modules/@simplewebauthn/browser/esm/helpers/browserSupportsWebAuthnAutofill.js
  init_define_import_meta_env();

  // node_modules/@simplewebauthn/browser/esm/helpers/identifyAuthenticationError.js
  init_define_import_meta_env();

  // node_modules/@simplewebauthn/browser/esm/helpers/platformAuthenticatorIsAvailable.js
  init_define_import_meta_env();
  function platformAuthenticatorIsAvailable() {
    if (!browserSupportsWebAuthn()) {
      return new Promise((resolve) => resolve(false));
    }
    return PublicKeyCredential.isUserVerifyingPlatformAuthenticatorAvailable();
  }

  // node_modules/@simplewebauthn/browser/esm/types/index.js
  init_define_import_meta_env();

  // src/types/index.ts
  init_define_import_meta_env();

  // src/utils/webauthnSupport.ts
  init_define_import_meta_env();
  var DWES_HOSTNAME = define_import_meta_env_default.VITE_DWES_HOSTNAME?.trim() || "dwes.local";
  var CANONICAL_HTTPS_PORT = define_import_meta_env_default.VITE_DWES_HTTPS_PORT?.trim() || "5173";
  function fingerprintEntryUrl() {
    if (typeof window !== "undefined") {
      const { hostname, protocol, port } = window.location;
      const isLocal = hostname === "localhost" || hostname === "127.0.0.1";
      const validHostname = !isIpHostname(hostname);
      const portOk = !port || port === CANONICAL_HTTPS_PORT;
      if (protocol === "https:" && portOk && (validHostname || isLocal)) {
        return window.location.origin;
      }
    }
    return `https://${DWES_HOSTNAME}:${CANONICAL_HTTPS_PORT}`;
  }
  function isIpHostname(hostname) {
    return /^(?:\d{1,3}\.){3}\d{1,3}$/.test(hostname);
  }
  function assessWebAuthnContext() {
    const { hostname, protocol, port } = window.location;
    const entryUrl = fingerprintEntryUrl();
    const portOk = !port || port === CANONICAL_HTTPS_PORT;
    if (isIpHostname(hostname)) {
      return {
        supported: false,
        message: `Fingerprint requires HTTPS + a hostname \u2014 open at ${entryUrl}`,
        suggestedUrl: entryUrl
      };
    }
    if (hostname !== DWES_HOSTNAME) {
      const onLocalhost = hostname === "localhost" || hostname === "127.0.0.1";
      return {
        supported: false,
        message: onLocalhost ? `You are on plain HTTP dev \u2014 stop dev:all, run npm run dev:fingerprint, add hosts (127.0.0.1 ${DWES_HOSTNAME}), then open ${entryUrl}` : `Fingerprint requires HTTPS + a hostname \u2014 open at ${entryUrl}`,
        suggestedUrl: entryUrl
      };
    }
    if (protocol !== "https:") {
      return {
        supported: false,
        message: `Fingerprint requires HTTPS \u2014 open at ${entryUrl} (not ${window.location.origin})`,
        suggestedUrl: entryUrl
      };
    }
    if (!portOk) {
      return {
        supported: false,
        message: `Use the HTTPS gateway port \u2014 open at ${entryUrl}`,
        suggestedUrl: entryUrl
      };
    }
    return { supported: true };
  }
  function friendlyWebAuthnError(err) {
    const axiosMsg = err?.response?.data?.message;
    const status = err?.response?.status;
    if (status === 401) {
      return "You must be signed in to enable fingerprint sign-in.";
    }
    const raw = axiosMsg ?? (err instanceof Error ? err.message : "") ?? "";
    if (/invalid for this domain/i.test(raw) || /ERROR_INVALID_RP_ID/i.test(raw) || /SecurityError/i.test(String(err))) {
      const ctx = assessWebAuthnContext();
      return ctx.message ?? `Fingerprint requires HTTPS + a hostname \u2014 open at ${ctx.suggestedUrl ?? suggestedUrl()}`;
    }
    if (err instanceof Error && err.name === "NotAllowedError") {
      return "Fingerprint was cancelled or not recognised \u2014 try again.";
    }
    if (err instanceof Error && err.name === "AbortError") {
      return "Fingerprint setup was cancelled.";
    }
    return raw || "Fingerprint setup failed \u2014 try again.";
  }
  function suggestedUrl() {
    return fingerprintEntryUrl();
  }

  // src/hooks/useBiometric.ts
  var BIOMETRIC_ENROLLED_KEY = "dwes-biometric-enrolled";
  function useBiometricAvailable() {
    const [available, setAvailable] = (0, import_react10.useState)(false);
    const [checking, setChecking] = (0, import_react10.useState)(true);
    const context = (0, import_react10.useMemo)(() => assessWebAuthnContext(), []);
    (0, import_react10.useEffect)(() => {
      if (!context.supported) {
        setAvailable(false);
        setChecking(false);
        return;
      }
      platformAuthenticatorIsAvailable().then((ok) => setAvailable(ok)).catch(() => setAvailable(false)).finally(() => setChecking(false));
    }, [context.supported]);
    const isEnrolled = available && localStorage.getItem(BIOMETRIC_ENROLLED_KEY) === "true";
    return {
      available,
      checking,
      isEnrolled,
      contextSupported: context.supported,
      contextMessage: context.message,
      suggestedUrl: context.suggestedUrl
    };
  }
  function useEnrollBiometric() {
    const [enrolling, setEnrolling] = (0, import_react10.useState)(false);
    const [error, setError] = (0, import_react10.useState)(null);
    const token = useAuthStore((s) => s.token);
    const enroll = (0, import_react10.useCallback)(async (deviceLabel2) => {
      const ctx = assessWebAuthnContext();
      if (!ctx.supported) {
        setError(ctx.message ?? "Fingerprint is not available on this page.");
        return false;
      }
      if (!token && !localStorage.getItem("dwes_token")) {
        setError("You must be signed in to enable fingerprint sign-in.");
        return false;
      }
      setEnrolling(true);
      setError(null);
      try {
        const opts = await webAuthnApi.registerOptions();
        const credential = await startRegistration({ optionsJSON: opts });
        await webAuthnApi.registerVerify(credential, deviceLabel2);
        localStorage.setItem(BIOMETRIC_ENROLLED_KEY, "true");
        return true;
      } catch (e) {
        setError(friendlyWebAuthnError(e));
        return false;
      } finally {
        setEnrolling(false);
      }
    }, [token]);
    return { enroll, enrolling, error, clearError: () => setError(null) };
  }

  // src/components/biometric/BiometricSettings.tsx
  function deviceLabel() {
    const ua = navigator.userAgent;
    let os = "Device";
    if (/Windows/i.test(ua)) os = "Windows";
    else if (/Android/i.test(ua)) os = "Android";
    else if (/iPhone|iPad|iPod/i.test(ua)) os = "iOS";
    else if (/Mac OS X|Macintosh/i.test(ua)) os = "macOS";
    else if (/Linux/i.test(ua)) os = "Linux";
    let browser = "Browser";
    if (/Edg\//i.test(ua)) browser = "Edge";
    else if (/OPR\//i.test(ua)) browser = "Opera";
    else if (/Chrome\//i.test(ua)) browser = "Chrome";
    else if (/Firefox\//i.test(ua)) browser = "Firefox";
    else if (/Safari\//i.test(ua)) browser = "Safari";
    return `${os} / ${browser}`;
  }
  function BiometricSettings() {
    const [creds, setCreds] = (0, import_react11.useState)([]);
    const [loading, setLoading] = (0, import_react11.useState)(true);
    const [removing, setRemoving] = (0, import_react11.useState)(null);
    const [justEnrolled, setJustEnrolled] = (0, import_react11.useState)(false);
    const token = useAuthStore((s) => s.token);
    const isSignedIn = Boolean(token || localStorage.getItem("dwes_token"));
    const {
      available,
      checking,
      contextSupported,
      contextMessage,
      suggestedUrl: suggestedUrl2
    } = useBiometricAvailable();
    const { enroll, enrolling, error: enrollError, clearError } = useEnrollBiometric();
    const load = async () => {
      if (!isSignedIn) {
        setCreds([]);
        setLoading(false);
        return;
      }
      setLoading(true);
      try {
        const { credentials } = await webAuthnApi.status();
        setCreds(credentials);
      } catch {
        setCreds([]);
      } finally {
        setLoading(false);
      }
    };
    (0, import_react11.useEffect)(() => {
      load();
    }, [isSignedIn]);
    const handleEnroll = async () => {
      if (!isSignedIn) return;
      clearError();
      setJustEnrolled(false);
      const ok = await enroll(deviceLabel());
      if (ok) {
        setJustEnrolled(true);
        await load();
      }
    };
    const handleRemove = async (id) => {
      setRemoving(id);
      try {
        await webAuthnApi.removeCredential(id);
        const next = creds.filter((c) => c.id !== id);
        setCreds(next);
        if (next.length === 0) localStorage.removeItem(BIOMETRIC_ENROLLED_KEY);
      } catch {
      } finally {
        setRemoving(null);
      }
    };
    if (loading) {
      return /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 text-sm text-slate-500 py-2" }, /* @__PURE__ */ React.createElement("div", { className: "w-4 h-4 rounded-full border-2 border-slate-300 border-t-slate-500 animate-spin" }), "Loading devices\u2026");
    }
    if (!contextSupported) {
      return /* @__PURE__ */ React.createElement("div", { className: "space-y-2" }, /* @__PURE__ */ React.createElement("p", { className: "flex items-start gap-1.5 text-xs text-amber-700 bg-amber-50 rounded-xl px-3 py-2.5 leading-relaxed" }, /* @__PURE__ */ React.createElement(AlertCircle, { size: 14, className: "shrink-0 mt-0.5" }), /* @__PURE__ */ React.createElement("span", null, contextMessage ?? "Fingerprint requires HTTPS and a hostname.", suggestedUrl2 && /* @__PURE__ */ React.createElement(React.Fragment, null, " ", /* @__PURE__ */ React.createElement("a", { href: suggestedUrl2, className: "underline font-medium" }, suggestedUrl2)))), /* @__PURE__ */ React.createElement("p", { className: "text-[11px] text-slate-400 leading-relaxed" }, "Currently: ", /* @__PURE__ */ React.createElement("code", { className: "text-[10px]" }, window.location.origin), " \xB7 ", "Required: ", /* @__PURE__ */ React.createElement("code", { className: "text-[10px]" }, suggestedUrl2 ?? fingerprintEntryUrl())), /* @__PURE__ */ React.createElement("p", { className: "text-[11px] text-slate-400 leading-relaxed" }, "1. ", /* @__PURE__ */ React.createElement("code", { className: "text-[10px]" }, "npm run hosts:install"), " (Admin) \xB7 2.", " ", /* @__PURE__ */ React.createElement("code", { className: "text-[10px]" }, "npm run certs:trust"), " \xB7 3. Stop", " ", /* @__PURE__ */ React.createElement("code", { className: "text-[10px]" }, "dev:all"), " \u2192", " ", /* @__PURE__ */ React.createElement("code", { className: "text-[10px]" }, "npm run dev:fingerprint")));
    }
    if (!isSignedIn) {
      return /* @__PURE__ */ React.createElement("p", { className: "flex items-center gap-1.5 text-xs text-slate-500" }, /* @__PURE__ */ React.createElement(AlertCircle, { size: 14, className: "shrink-0" }), "You must be signed in to enable fingerprint sign-in.");
    }
    return /* @__PURE__ */ React.createElement("div", { className: "space-y-3" }, creds.length > 0 ? /* @__PURE__ */ React.createElement("div", { className: "space-y-2" }, creds.map((c) => /* @__PURE__ */ React.createElement(
      "div",
      {
        key: c.id,
        className: "flex items-center justify-between p-3 bg-slate-50 rounded-xl gap-3"
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3 min-w-0" }, /* @__PURE__ */ React.createElement(Fingerprint, { size: 18, className: "text-blue-600 shrink-0" }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "text-sm font-medium text-slate-900 truncate" }, c.deviceLabel ?? "Unknown device"), /* @__PURE__ */ React.createElement("div", { className: "text-xs text-slate-500" }, "Added ", new Date(c.createdAt).toLocaleDateString("en-GB", {
        day: "2-digit",
        month: "short",
        year: "numeric"
      }), c.lastUsedAt && /* @__PURE__ */ React.createElement(React.Fragment, null, " \xB7 Last used ", new Date(c.lastUsedAt).toLocaleDateString("en-GB", {
        day: "2-digit",
        month: "short"
      }))))),
      /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: () => handleRemove(c.id),
          disabled: removing === c.id,
          title: "Remove this fingerprint",
          className: "text-red-400 hover:text-red-600 hover:bg-red-50 p-1.5 rounded-lg transition-colors disabled:opacity-40 shrink-0"
        },
        removing === c.id ? /* @__PURE__ */ React.createElement("div", { className: "w-4 h-4 rounded-full border-2 border-red-300 border-t-red-500 animate-spin" }) : /* @__PURE__ */ React.createElement(Trash2, { size: 16 })
      )
    ))) : /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 text-sm text-slate-400" }, /* @__PURE__ */ React.createElement(Fingerprint, { size: 16, className: "shrink-0" }), "No fingerprint devices enrolled yet."), available ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: handleEnroll,
        disabled: enrolling,
        className: "w-full flex items-center justify-center gap-2.5 py-2.5 rounded-xl border-2 border-blue-200 text-blue-700 font-semibold text-sm hover:bg-blue-50 hover:border-blue-300 active:scale-[0.98] transition-all disabled:opacity-60"
      },
      enrolling ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "w-4 h-4 rounded-full border-2 border-blue-300 border-t-blue-600 animate-spin" }), "Waiting for fingerprint\u2026") : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(Fingerprint, { size: 18, strokeWidth: 1.5 }), "Enable fingerprint on this device")
    ), justEnrolled && !enrollError && /* @__PURE__ */ React.createElement("p", { className: "flex items-center gap-1.5 text-xs text-green-600" }, /* @__PURE__ */ React.createElement(CheckCircle2, { size: 14, className: "shrink-0" }), "Fingerprint enabled on this device."), enrollError && /* @__PURE__ */ React.createElement("p", { className: "flex items-center gap-1.5 text-xs text-red-600" }, /* @__PURE__ */ React.createElement(AlertCircle, { size: 14, className: "shrink-0" }), enrollError), /* @__PURE__ */ React.createElement("p", { className: "text-[11px] text-slate-400 leading-relaxed" }, "Your device will prompt for Windows Hello / its fingerprint sensor.")) : !checking ? /* @__PURE__ */ React.createElement("p", { className: "text-xs text-slate-400 leading-relaxed" }, "This device doesn\u2019t support fingerprint / Windows Hello sign-in.") : null);
  }

  // src/components/profile/MyProfileModal.tsx
  init_define_import_meta_env();
  var import_react12 = __toESM(require_react_shim(), 1);

  // src/components/ui/TabletFields.tsx
  init_define_import_meta_env();
  function InputField({ label, value, onChange, placeholder, type = "text", icon, error }) {
    return /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", { className: "form-label" }, label), /* @__PURE__ */ React.createElement("div", { className: "relative flex items-center" }, icon && /* @__PURE__ */ React.createElement("div", { className: "absolute left-3 flex items-center justify-center text-slate-400 pointer-events-none" }, icon), /* @__PURE__ */ React.createElement(
      "input",
      {
        type,
        value,
        onChange: (event) => onChange(event.target.value),
        placeholder,
        autoComplete: "off",
        spellCheck: type === "text",
        className: `form-input ${icon ? "pl-10" : ""} ${error ? "border-red-400 focus:border-red-500 focus:ring-red-100" : ""}`
      }
    )), error && /* @__PURE__ */ React.createElement("p", { className: "mt-1 text-[11px] font-medium text-red-600" }, error));
  }
  function ComboField({ label, value, onChange, options, placeholder = "Type or pick\u2026", icon, error }) {
    const listId = `combo-${label.replace(/[^a-zA-Z0-9]+/g, "-").toLowerCase()}`;
    return /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", { className: "form-label" }, label), /* @__PURE__ */ React.createElement("div", { className: "relative flex items-center" }, icon && /* @__PURE__ */ React.createElement("div", { className: "absolute left-3 flex items-center justify-center text-slate-400 pointer-events-none" }, icon), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value,
        onChange: (event) => onChange(event.target.value),
        placeholder,
        list: listId,
        autoComplete: "off",
        className: `form-input ${icon ? "pl-10" : ""} ${error ? "border-red-400 focus:border-red-500 focus:ring-red-100" : ""}`
      }
    ), /* @__PURE__ */ React.createElement("datalist", { id: listId }, options.map((option) => /* @__PURE__ */ React.createElement("option", { key: option, value: option })))), error && /* @__PURE__ */ React.createElement("p", { className: "mt-1 text-[11px] font-medium text-red-600" }, error));
  }
  function SelectField({ label, value, onChange, options, placeholder = "-- select --", icon, error }) {
    return /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", { className: "form-label" }, label), /* @__PURE__ */ React.createElement("div", { className: "relative flex items-center" }, icon && /* @__PURE__ */ React.createElement("div", { className: "absolute left-3 flex items-center justify-center text-slate-400 pointer-events-none" }, icon), /* @__PURE__ */ React.createElement(
      "select",
      {
        value,
        onChange: (event) => onChange(event.target.value),
        title: label,
        className: `form-select ${icon ? "pl-10" : ""} ${error ? "border-red-400 focus:border-red-500 focus:ring-red-100" : ""}`
      },
      /* @__PURE__ */ React.createElement("option", { value: "" }, placeholder),
      options.map((option) => /* @__PURE__ */ React.createElement("option", { key: option, value: option }, option))
    )), error && /* @__PURE__ */ React.createElement("p", { className: "mt-1 text-[11px] font-medium text-red-600" }, error));
  }

  // src/components/profile/MyProfileModal.tsx
  function MyProfileModal({ onClose }) {
    const { user, token } = useAuthStore();
    const [form, setForm] = (0, import_react12.useState)({
      full_name: user?.full_name || "",
      whatsapp_number: user?.whatsapp_number || ""
    });
    const [password, setPassword] = (0, import_react12.useState)("");
    const [confirm, setConfirm] = (0, import_react12.useState)("");
    const [saving, setSaving] = (0, import_react12.useState)(false);
    const [error, setError] = (0, import_react12.useState)("");
    const [done, setDone] = (0, import_react12.useState)(false);
    if (!user) return null;
    const set = (k) => (v) => setForm((f) => ({ ...f, [k]: v }));
    const handleSave = async () => {
      if (!form.full_name.trim()) {
        setError("Full name is required.");
        return;
      }
      if (password && (password.length < 6 || password !== confirm)) {
        setError(password.length < 6 ? "Password must be at least 6 characters." : "Passwords do not match.");
        return;
      }
      setSaving(true);
      setError("");
      try {
        const payload = {
          full_name: form.full_name,
          whatsapp_number: form.whatsapp_number
        };
        if (password) payload.password = password;
        await usersApi.update(user.id, payload);
        const fresh = await authApi.me();
        localStorage.setItem("dwes_user", JSON.stringify(fresh));
        useAuthStore.setState({ user: fresh, token });
        setDone(true);
      } catch (e) {
        setError(e?.response?.data?.message || "Save failed");
      } finally {
        setSaving(false);
      }
    };
    return /* @__PURE__ */ React.createElement(
      Modal,
      {
        title: "My Profile",
        onClose,
        size: "lg",
        footer: !done ? /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-end gap-3 w-full" }, /* @__PURE__ */ React.createElement("button", { type: "button", onClick: onClose, className: "btn-secondary" }, "Cancel"), /* @__PURE__ */ React.createElement("button", { type: "button", onClick: handleSave, disabled: saving, className: "btn-primary" }, saving ? "Saving\u2026" : "Save Changes")) : /* @__PURE__ */ React.createElement("div", { className: "flex justify-end w-full" }, /* @__PURE__ */ React.createElement("button", { type: "button", onClick: onClose, className: "btn-primary" }, "Done"))
      },
      !done ? /* @__PURE__ */ React.createElement("div", { className: "flex flex-col gap-4" }, /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-2 gap-4" }, /* @__PURE__ */ React.createElement(InputField, { label: "Full Name *", icon: /* @__PURE__ */ React.createElement(User, { size: 16 }), value: form.full_name, onChange: set("full_name"), placeholder: "Your full name" }), /* @__PURE__ */ React.createElement("div", { className: "flex flex-col gap-1.5" }, /* @__PURE__ */ React.createElement("label", { className: "text-[12px] font-medium text-slate-600 uppercase tracking-[0.06em]" }, "Username"), /* @__PURE__ */ React.createElement("div", { className: "h-[44px] px-3 flex items-center text-[14px] font-mono border border-[#E2E8F0] rounded-[10px] bg-slate-50 text-slate-500 select-none" }, user.username)), /* @__PURE__ */ React.createElement("div", { className: "flex flex-col gap-1.5" }, /* @__PURE__ */ React.createElement("label", { className: "text-[12px] font-medium text-slate-600 uppercase tracking-[0.06em]" }, "Employee ID"), /* @__PURE__ */ React.createElement("div", { className: "h-[44px] px-3 flex items-center text-[14px] border border-[#E2E8F0] rounded-[10px] bg-slate-50 text-slate-500 select-none" }, user.employee_id || "\u2014")), /* @__PURE__ */ React.createElement("div", { className: "flex flex-col gap-1.5" }, /* @__PURE__ */ React.createElement("label", { className: "text-[12px] font-medium text-slate-600 uppercase tracking-[0.06em]" }, "Role"), /* @__PURE__ */ React.createElement("div", { className: "h-[44px] px-3 flex items-center text-[14px] border border-[#E2E8F0] rounded-[10px] bg-slate-50 text-slate-500 select-none" }, "Wiring Technician"))), /* @__PURE__ */ React.createElement(
        InputField,
        {
          label: "WhatsApp Number",
          icon: /* @__PURE__ */ React.createElement(Phone, { size: 16 }),
          value: form.whatsapp_number,
          onChange: set("whatsapp_number"),
          placeholder: "+971 50 000 0000"
        }
      ), /* @__PURE__ */ React.createElement("div", { className: "border-t border-[#E2E8F0] pt-4" }, /* @__PURE__ */ React.createElement("p", { className: "text-[12px] font-semibold uppercase tracking-wide text-slate-500 mb-3" }, "Change Password (optional)"), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-2 gap-4" }, /* @__PURE__ */ React.createElement(InputField, { label: "New Password", icon: /* @__PURE__ */ React.createElement(Lock, { size: 16 }), type: "password", value: password, onChange: setPassword, placeholder: "Min. 6 characters" }), /* @__PURE__ */ React.createElement(InputField, { label: "Confirm Password", icon: /* @__PURE__ */ React.createElement(Lock, { size: 16 }), type: "password", value: confirm, onChange: setConfirm, placeholder: "Re-enter password" }))), error && /* @__PURE__ */ React.createElement("div", { className: "p-3 bg-red-50 border border-red-200 text-red-600 text-[13px] font-medium rounded-[10px]" }, error)) : /* @__PURE__ */ React.createElement("div", { className: "flex flex-col items-center gap-3 py-6 text-center" }, /* @__PURE__ */ React.createElement(CheckCircle2, { size: 48, className: "text-green-500" }), /* @__PURE__ */ React.createElement("div", { className: "text-[16px] font-bold text-slate-800" }, "Profile updated"), /* @__PURE__ */ React.createElement("div", { className: "text-[13px] text-slate-500" }, "Your changes have been saved."))
    );
  }

  // src/components/layout/Topbar.tsx
  var ROLE_LABELS = {
    system_admin: "System Admin",
    ops_director: "Ops Director",
    prod_supervisor: "Supervisor",
    qaqc_engineer: "QA/QC Engineer",
    wiring_technician: "Technician"
  };
  function Topbar({
    navItems = [],
    activeTab,
    onTabChange,
    onMenuClick,
    showMenuButton = false,
    activeProject = null,
    projectSelectionRequired = false
  }) {
    const navigate = useNavigate();
    const { user, logout } = useAuthStore();
    const [clock, setClock] = (0, import_react13.useState)(/* @__PURE__ */ new Date());
    const [showBioPanel, setShowBioPanel] = (0, import_react13.useState)(false);
    const [showProfile, setShowProfile] = (0, import_react13.useState)(false);
    const [deployMode, setDeployMode] = (0, import_react13.useState)(null);
    const bioPanelRef = (0, import_react13.useRef)(null);
    const { available: bioAvailable, checking: bioChecking, contextSupported } = useBiometricAvailable();
    const showBioSettings = !bioChecking && (bioAvailable || !contextSupported);
    (0, import_react13.useEffect)(() => {
      const t = setInterval(() => setClock(/* @__PURE__ */ new Date()), 1e3);
      return () => clearInterval(t);
    }, []);
    (0, import_react13.useEffect)(() => {
      if (user?.role !== "system_admin") return;
      adminApi.getDeploymentConfig().then((cfg) => setDeployMode(cfg.mode)).catch(() => setDeployMode("intranet"));
    }, [user?.role]);
    (0, import_react13.useEffect)(() => {
      if (!showBioPanel) return;
      const handler = (e) => {
        if (bioPanelRef.current && !bioPanelRef.current.contains(e.target))
          setShowBioPanel(false);
      };
      document.addEventListener("mousedown", handler);
      return () => document.removeEventListener("mousedown", handler);
    }, [showBioPanel]);
    const [hh, mm, ss] = clock.toLocaleTimeString("en-GB", { hour12: false, hour: "2-digit", minute: "2-digit", second: "2-digit" }).split(":");
    const dateStr = clock.toLocaleDateString("en-GB", {
      weekday: "short",
      day: "2-digit",
      month: "short",
      year: "numeric"
    });
    const handleLogout = async () => {
      await logout();
      navigate("/", { replace: true });
    };
    return /* @__PURE__ */ React.createElement("header", { className: "topbar", "aria-label": "Application header" }, /* @__PURE__ */ React.createElement("div", { className: "topbar-inner" }, /* @__PURE__ */ React.createElement("div", { className: "topbar-brand-card" }, showMenuButton && /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "topbar-menu-btn tablet-land:hidden",
        onClick: onMenuClick,
        title: "Open navigation",
        "aria-label": "Open navigation menu"
      },
      /* @__PURE__ */ React.createElement(Menu, { size: 18, strokeWidth: 1.75 })
    ), /* @__PURE__ */ React.createElement(CompanyLogo, { variant: "icon", size: "sm", className: "topbar-logo-img-wrap" }), /* @__PURE__ */ React.createElement("div", { className: "topbar-brand-text hidden xs:flex" }, /* @__PURE__ */ React.createElement("span", { className: "topbar-logo-name" }, "DWES"), /* @__PURE__ */ React.createElement("span", { className: "topbar-logo-sub hidden sm:block" }, "Digital Wiring Execution System"))), navItems.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "topbar-nav-card topbar-float topbar-float--dark", role: "tablist", "aria-label": "Module navigation" }, navItems.map((item) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: item.key,
        onClick: () => onTabChange?.(item.key),
        className: `topbar-nav-btn${activeTab === item.key ? " active" : ""}`,
        type: "button",
        title: item.label,
        role: "tab",
        "aria-selected": activeTab === item.key
      },
      item.icon,
      /* @__PURE__ */ React.createElement("span", { className: "hidden tablet-land:inline" }, item.label)
    ))), /* @__PURE__ */ React.createElement("div", { className: "topbar-controls" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `topbar-project-pill topbar-float topbar-float--light${projectSelectionRequired ? " topbar-project-pill--required" : ""}`,
        "aria-live": "polite",
        title: projectSelectionRequired ? "Project selection required before using the application" : `Active Project: ${activeProject?.code ?? ""}`
      },
      /* @__PURE__ */ React.createElement("div", { className: "topbar-project-icon", "aria-hidden": "true" }, /* @__PURE__ */ React.createElement(FolderKanban, { size: 16, strokeWidth: 1.75 })),
      /* @__PURE__ */ React.createElement("div", { className: "topbar-project-meta min-w-0" }, /* @__PURE__ */ React.createElement("span", { className: "topbar-project-label" }, "Project"), /* @__PURE__ */ React.createElement("span", { className: "topbar-project-value" }, projectSelectionRequired ? "Selection Required" : activeProject?.code ?? "Unassigned"))
    ), user?.role === "system_admin" && deployMode && /* @__PURE__ */ React.createElement("span", { className: `topbar-env topbar-float-pill${deployMode === "cloud" ? " topbar-env--cloud" : ""}` }, deployMode === "cloud" ? "Cloud Hosted" : "Local Intranet"), user && /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `topbar-user-card topbar-float topbar-float--light${user.role === "wiring_technician" ? " cursor-pointer hover:bg-white/90" : ""}`,
        title: `${user.full_name} \xB7 @${user.username} \xB7 ${ROLE_LABELS[user.role] ?? user.role}`,
        onClick: user.role === "wiring_technician" ? () => setShowProfile(true) : void 0,
        onKeyDown: user.role === "wiring_technician" ? (e) => {
          if (e.key === "Enter" || e.key === " ") setShowProfile(true);
        } : void 0,
        role: user.role === "wiring_technician" ? "button" : void 0,
        tabIndex: user.role === "wiring_technician" ? 0 : void 0
      },
      /* @__PURE__ */ React.createElement(Avatar, { name: user.full_name, role: user.role, size: 30 }),
      /* @__PURE__ */ React.createElement("div", { className: "topbar-user-meta min-w-0 hidden xs:block" }, /* @__PURE__ */ React.createElement("div", { className: "topbar-user-name" }, user.full_name), /* @__PURE__ */ React.createElement("div", { className: "topbar-user-role" }, /* @__PURE__ */ React.createElement("span", { className: "topbar-user-handle" }, "@", user.username), /* @__PURE__ */ React.createElement("span", { className: "topbar-user-role-sep" }, " \xB7 "), ROLE_LABELS[user.role] ?? user.role))
    ), /* @__PURE__ */ React.createElement("div", { className: "topbar-actions-capsule topbar-float topbar-float--dark", role: "group", "aria-label": "Header actions" }, showBioSettings && /* @__PURE__ */ React.createElement("div", { className: "topbar-bio-anchor", ref: bioPanelRef }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: () => setShowBioPanel((v) => !v),
        title: "Fingerprint sign-in settings",
        className: `topbar-capsule-btn topbar-capsule-btn--bio${showBioPanel ? " is-active" : ""}`,
        "aria-expanded": showBioPanel,
        "aria-haspopup": "dialog"
      },
      /* @__PURE__ */ React.createElement(Fingerprint, { size: 20, strokeWidth: 1.75 })
    ), showBioPanel && /* @__PURE__ */ React.createElement("div", { className: "bio-panel-dropdown", role: "dialog", "aria-label": "Fingerprint sign-in settings" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between px-5 py-4 border-b shrink-0" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("p", { className: "text-sm font-semibold" }, "Fingerprint sign-in"), /* @__PURE__ */ React.createElement("p", { className: "text-xs mt-0.5" }, "Enrolled devices on this server")), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        title: "Close fingerprint settings",
        onClick: () => setShowBioPanel(false),
        className: "p-1.5 min-h-[44px] min-w-[44px] flex items-center justify-center rounded-lg transition-colors"
      },
      /* @__PURE__ */ React.createElement(X, { size: 16 })
    )), /* @__PURE__ */ React.createElement("div", { className: "bio-panel-dropdown-body px-5 py-4" }, /* @__PURE__ */ React.createElement(BiometricSettings, null)))), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "topbar-capsule-btn topbar-capsule-btn--logout",
        onClick: handleLogout,
        type: "button",
        title: "Logout",
        "aria-label": "Logout"
      },
      /* @__PURE__ */ React.createElement(LogOut, { size: 20, strokeWidth: 1.75 })
    )), /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "topbar-clock-card topbar-float topbar-float--dark flex items-center gap-2.5 select-none shrink-0",
        "aria-label": `Current time: ${hh}:${mm}:${ss}, ${dateStr}`
      },
      /* @__PURE__ */ React.createElement("span", { className: "topbar-clock-live", "aria-hidden": "true" }),
      /* @__PURE__ */ React.createElement("div", { className: "flex flex-col items-end justify-center leading-none gap-[3px] min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "topbar-clock-digits" }, hh, /* @__PURE__ */ React.createElement("span", { className: "topbar-clock-sep" }, ":"), mm, /* @__PURE__ */ React.createElement("span", { className: "topbar-clock-sep" }, ":"), /* @__PURE__ */ React.createElement("span", { className: "topbar-clock-secs" }, ss)), /* @__PURE__ */ React.createElement("div", { className: "topbar-clock-date hidden xs:block" }, dateStr))
    ))), showProfile && /* @__PURE__ */ React.createElement(MyProfileModal, { onClose: () => setShowProfile(false) }));
  }

  // src/components/layout/Sidebar.tsx
  init_define_import_meta_env();

  // src/store/useUIStore.ts
  init_define_import_meta_env();
  var useUIStore = create()(
    persist(
      (set) => ({
        deviceProfile: "auto",
        setDeviceProfile: (profile) => set({ deviceProfile: profile }),
        sidebarExpanded: true,
        setSidebarExpanded: (expanded) => set({ sidebarExpanded: expanded }),
        toggleSidebar: () => set((state) => ({ sidebarExpanded: !state.sidebarExpanded }))
      }),
      {
        name: "dwes-ui-storage"
      }
    )
  );

  // src/components/layout/Sidebar.tsx
  function Sidebar({
    items,
    activeTab,
    onTabChange,
    mobileOpen = false,
    onMobileClose
  }) {
    const { sidebarExpanded, toggleSidebar } = useUIStore();
    if (items.length === 0) return null;
    const handleSelect = (key) => {
      onTabChange?.(key);
      onMobileClose?.();
    };
    return /* @__PURE__ */ React.createElement(React.Fragment, null, mobileOpen && /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "sidebar-backdrop",
        "aria-label": "Close navigation",
        onClick: onMobileClose
      }
    ), /* @__PURE__ */ React.createElement(
      "aside",
      {
        className: `dash-sidebar${sidebarExpanded ? "" : " dash-sidebar--collapsed"}${mobileOpen ? " dash-sidebar--mobile-open" : ""}`,
        "aria-label": "Dashboard navigation"
      },
      /* @__PURE__ */ React.createElement("div", { className: "dash-sidebar-head" }, /* @__PURE__ */ React.createElement("span", { className: `dash-sidebar-title${sidebarExpanded ? "" : " sr-only"}` }, "Workspace"), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          className: "dash-sidebar-toggle hidden tablet-land:flex items-center justify-center min-w-[44px] min-h-[44px]",
          onClick: toggleSidebar,
          title: sidebarExpanded ? "Collapse sidebar" : "Expand sidebar",
          "aria-expanded": sidebarExpanded
        },
        sidebarExpanded ? /* @__PURE__ */ React.createElement(PanelLeftClose, { size: 18 }) : /* @__PURE__ */ React.createElement(PanelLeftOpen, { size: 18 })
      )),
      /* @__PURE__ */ React.createElement("nav", { className: "dash-sidebar-nav" }, items.map((item) => /* @__PURE__ */ React.createElement(
        "button",
        {
          key: item.key,
          type: "button",
          className: `dash-sidebar-link${activeTab === item.key ? " is-active" : ""}`,
          onClick: () => handleSelect(item.key),
          title: item.label
        },
        /* @__PURE__ */ React.createElement("span", { className: "dash-sidebar-icon" }, item.icon),
        /* @__PURE__ */ React.createElement("span", { className: `dash-sidebar-label${sidebarExpanded ? "" : " sr-only"}` }, item.label)
      )))
    ));
  }

  // src/components/layout/AppShell.tsx
  function AppShell({
    children,
    navItems = [],
    activeTab,
    onTabChange,
    noPadding = false,
    sidebarNav = true
  }) {
    const [mobileNavOpen, setMobileNavOpen] = (0, import_react14.useState)(false);
    const { user } = useAuthStore();
    const { selectedProject, ownerUserId, initializeForUser } = useProjectSelectionStore();
    const useSidebar = sidebarNav && navItems.length > 0;
    const activeProject = user?.id && selectedProject && ownerUserId === user.id ? selectedProject : null;
    (0, import_react14.useEffect)(() => {
      if (!user?.id) return;
      initializeForUser(user.id);
    }, [initializeForUser, user?.id]);
    return /* @__PURE__ */ React.createElement("div", { className: "app-shell", "data-ui-polish": "saas" }, /* @__PURE__ */ React.createElement(
      Topbar,
      {
        navItems: useSidebar ? [] : navItems,
        activeTab,
        onTabChange,
        onMenuClick: useSidebar ? () => setMobileNavOpen((v) => !v) : void 0,
        showMenuButton: useSidebar,
        activeProject
      }
    ), /* @__PURE__ */ React.createElement("div", { className: "dash-layout" }, useSidebar && /* @__PURE__ */ React.createElement(
      Sidebar,
      {
        items: navItems,
        activeTab,
        onTabChange,
        mobileOpen: mobileNavOpen,
        onMobileClose: () => setMobileNavOpen(false)
      }
    ), /* @__PURE__ */ React.createElement("main", { className: noPadding ? "dash-main dash-main--flush" : "dash-main" }, children)));
  }

  // src/components/layout/PageContainer.tsx
  init_define_import_meta_env();
  function PageContainer({
    variant = "centered",
    children,
    className = "",
    ...rest
  }) {
    const variantClass = variant === "wide" ? "page-container--wide" : "page-container--centered";
    return /* @__PURE__ */ React.createElement("div", { className: `page-container ${variantClass} ${className}`.trim(), ...rest }, children);
  }

  // src/components/ui/DashboardShell.tsx
  function colorToVariant(color) {
    if (!color) return "default";
    if (color.includes("completed") || color.includes("success")) return "green";
    if (color.includes("danger") || color.includes("error")) return "red";
    if (color.includes("warning") || color.includes("one-open") || color.includes("pending")) return "amber";
    if (color.includes("progress") || color.includes("source") || color.includes("qaqc")) return "blue";
    return "default";
  }
  function DashboardShell({
    title,
    subtitle,
    badge,
    badgeVariant = "blue",
    kpis = [],
    tabs = [],
    activeTab,
    onTabChange,
    widthVariant = "centered",
    hideTabSectionHeader = false,
    children
  }) {
    const kpiGridClass = kpis.length >= 6 ? "kpi-grid-6" : kpis.length === 5 ? "kpi-grid-5" : "kpi-grid";
    const activeNav = tabs.find((t) => t.key === activeTab);
    return /* @__PURE__ */ React.createElement(AppShell, { navItems: tabs, activeTab, onTabChange, noPadding: true }, /* @__PURE__ */ React.createElement(PageContainer, { variant: widthVariant, className: "dashboard-shell-root flex flex-col gap-4 min-h-0" }, /* @__PURE__ */ React.createElement("section", { className: "dashboard-hero" }, /* @__PURE__ */ React.createElement("div", { className: "dashboard-hero-copy" }, /* @__PURE__ */ React.createElement("div", { className: "dashboard-hero-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "section-title" }, title), badge && /* @__PURE__ */ React.createElement("span", { className: `badge badge-${badgeVariant} dashboard-hero-badge` }, badge)), /* @__PURE__ */ React.createElement("p", { className: "section-sub dashboard-hero-subtitle" }, subtitle))), kpis.length > 0 && /* @__PURE__ */ React.createElement("div", { className: `${kpiGridClass} dashboard-kpis` }, kpis.map((kpi, i) => /* @__PURE__ */ React.createElement(
      KpiCard,
      {
        key: i,
        label: kpi.label,
        value: kpi.value,
        variant: colorToVariant(kpi.color),
        icon: kpi.icon
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "dashboard-content flex flex-col gap-4 pb-6" }, !hideTabSectionHeader && activeNav && /* @__PURE__ */ React.createElement(
      SectionHeader,
      {
        title: activeNav.label,
        description: activeNav.description ?? subtitle,
        icon: activeNav.icon
      }
    ), activeTab != null ? /* @__PURE__ */ React.createElement("div", { key: activeTab, className: "nav-tab-panel flex flex-col gap-4" }, children) : children)));
  }

  // src/components/ui/ProjectInfoCard.tsx
  init_define_import_meta_env();
  var STATUS_FRIENDLY = {
    completed: "Completed",
    active: "Active",
    in_review: "In Review",
    pending: "Pending",
    stopped: "Stopped",
    submitted_to_director: "Submitted",
    not_started: "Not Started",
    pass: "Pass",
    fail: "Fail",
    inspected: "Inspected"
  };
  function normalizeStatus(raw) {
    return (raw || "").toLowerCase().replace(/[\s-]+/g, "_");
  }
  function ProjectInfoCard({ title, subtitle, statusLabel, fields, actions }) {
    const code = subtitle?.split("\xB7")[0]?.trim() ?? "";
    const client = fields.find((f) => f.label === "Client")?.value;
    const stat = (() => {
      const progress = fields.find((f) => f.label === "Progress")?.value;
      if (progress != null) return String(progress);
      const cables = fields.find((f) => f.label === "Cables")?.value;
      if (cables != null) return `${cables} cables`;
      const panels = fields.find((f) => f.label === "Total Panels")?.value;
      if (panels != null) return `${panels} panels`;
      return null;
    })();
    const statusNorm = normalizeStatus(statusLabel);
    const friendlyStatus = STATUS_FRIENDLY[statusNorm] || statusLabel || "";
    return /* @__PURE__ */ React.createElement("article", { className: "proj-mini-card", "data-s": statusNorm }, code && /* @__PURE__ */ React.createElement("div", { className: "proj-mini-code" }, code), /* @__PURE__ */ React.createElement("div", { className: "proj-mini-name" }, title), client && /* @__PURE__ */ React.createElement("div", { className: "proj-mini-client" }, client), /* @__PURE__ */ React.createElement("div", { className: "proj-mini-foot" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-1 min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "proj-mini-dot" }), /* @__PURE__ */ React.createElement("span", { className: "proj-mini-slabel" }, friendlyStatus)), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-1.5 shrink-0" }, stat && /* @__PURE__ */ React.createElement("span", { className: "proj-mini-stat" }, stat), actions)));
  }

  // src/components/ui/CableSchematic.tsx
  init_define_import_meta_env();
  var COLOR_MAP = {
    // Full names (IEC / industrial)
    RED: "#DC2626",
    BLACK: "#1C1917",
    BLUE: "#2563EB",
    YELLOW: "#EAB308",
    GREEN: "#16A34A",
    WHITE: "#F1F5F9",
    GREY: "#6B7280",
    GRAY: "#6B7280",
    BROWN: "#92400E",
    ORANGE: "#EA580C",
    VIOLET: "#7C3AED",
    PURPLE: "#7C3AED",
    PINK: "#EC4899",
    CYAN: "#0891B2",
    TURQUOISE: "#0D9488",
    SILVER: "#94A3B8",
    "LIGHT BLUE": "#60A5FA",
    "DARK BLUE": "#1E3A8A",
    // Short codes
    R: "#DC2626",
    RD: "#DC2626",
    N: "#1C1917",
    BK: "#1C1917",
    L: "#2563EB",
    BU: "#2563EB",
    Y: "#EAB308",
    YL: "#EAB308",
    G: "#16A34A",
    GN: "#16A34A",
    W: "#F1F5F9",
    WH: "#F1F5F9",
    GY: "#6B7280",
    GR: "#6B7280",
    BR: "#92400E",
    BN: "#92400E",
    OR: "#EA580C",
    OG: "#EA580C",
    V: "#7C3AED",
    VI: "#7C3AED",
    PK: "#EC4899",
    CY: "#0891B2"
  };
  function parseLengthMeters(raw) {
    if (!raw) return null;
    const m = String(raw).replace(/,/g, ".").match(/(\d+\.?\d*)/);
    return m ? parseFloat(m[1]) : null;
  }
  function resolveColor(raw) {
    if (!raw) return { hex: "#94A3B8", isWhite: false, isUnknown: true };
    const n = raw.trim().toUpperCase();
    if (n.startsWith("#")) return { hex: n, isWhite: false, isUnknown: false };
    if (COLOR_MAP[n]) {
      return { hex: COLOR_MAP[n], isWhite: n === "WHITE" || n === "W" || n === "WH", isUnknown: false };
    }
    if (n.includes("/")) {
      const [a, b] = n.split("/").map((s) => s.trim());
      if (COLOR_MAP[a] && COLOR_MAP[b]) {
        return { hex: COLOR_MAP[a], hex2: COLOR_MAP[b], isWhite: false, isUnknown: false };
      }
    }
    return { hex: "#94A3B8", isWhite: false, isUnknown: true };
  }
  function parseMm2(raw) {
    if (!raw) return null;
    const m = raw.match(/(\d+\.?\d*)/);
    return m ? parseFloat(m[1]) : null;
  }
  function strokeForMm2(mm2) {
    if (mm2 === null) return 5;
    if (mm2 <= 1) return 3;
    if (mm2 <= 1.5) return 4;
    if (mm2 <= 2.5) return 5;
    if (mm2 <= 4) return 6;
    if (mm2 <= 6) return 8;
    if (mm2 <= 10) return 10;
    return 12;
  }
  function tr(s, n) {
    if (!s) return "";
    return s.length > n ? s.slice(0, n - 1) + "\u2026" : s;
  }
  var REDUCED_MOTION = typeof window !== "undefined" && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  var W = 480;
  var H = 130;
  var SRC_X = 58;
  var DST_X = 422;
  var LINE_Y = 62;
  var NODE_R = 22;
  var MAX_SPAN = DST_X - SRC_X - NODE_R * 2 - 6;
  function CableSchematic({
    cable,
    status,
    maxLengthMeters = 0,
    highlight = false
  }) {
    const { hex, hex2, isWhite, isUnknown } = resolveColor(cable.color);
    const mm2 = parseMm2(cable.size);
    const sw = strokeForMm2(mm2) + (highlight ? 1.5 : 0);
    const lengthM = parseLengthMeters(cable.length);
    const lengthRatio = lengthM && maxLengthMeters > 0 ? Math.max(0.35, Math.min(1, lengthM / maxLengthMeters)) : 1;
    const spanPx = Math.round(MAX_SPAN * lengthRatio);
    const lx1 = SRC_X + NODE_R + 3;
    const lx2 = lx1 + spanPx;
    const mid = Math.round((lx1 + lx2) / 2);
    const { src: srcDone, dst: dstDone, issue: hasIssue, note: issueNote } = status;
    const bothDone = srcDone && dstDone;
    const opacity = highlight ? 1 : bothDone ? 1 : srcDone || dstDone ? 0.72 : 0.38;
    const dashLen = sw * 4;
    const gapLen = sw * 3;
    const dashArr = bothDone || REDUCED_MOTION ? "" : `${dashLen} ${gapLen}`;
    const animFrom = `${dashLen + gapLen}`;
    const strokeHex = isWhite ? "#94A3B8" : hex;
    const srcFill = srcDone ? hex : "#F8FAFC";
    const dstFill = dstDone ? hex : "#F8FAFC";
    const srcRing = srcDone ? hex : "#CBD5E1";
    const dstRing = dstDone ? hex : "#CBD5E1";
    const checkClr = isWhite ? "#475569" : "#FFFFFF";
    const ferrTxt = tr(cable.ferrule, 16);
    const sizeText = [cable.size, cable.length].filter(Boolean).join(" \xB7 ");
    const colorLabel = cable.color ? isUnknown ? `${cable.color} (?)` : cable.color : "";
    const metaLine = [colorLabel, sizeText].filter(Boolean).join(" \xB7 ");
    const srcLabel = tr(cable.source_terminal || cable.terminal, 16) || tr(cable.source, 18);
    const srcDev = tr(cable.source_device || cable.device, 18);
    const dstLabel = tr(cable.dest_terminal, 16) || tr(cable.destination, 18);
    const dstDev = tr(cable.dest_device, 18);
    const ckPath = (cx, cy) => `M${cx - 7} ${cy + 1} L${cx - 2} ${cy + 7} L${cx + 8} ${cy - 7}`;
    return /* @__PURE__ */ React.createElement("div", { className: `wire-schematic-wrap${hasIssue ? " wire-schematic-issue" : ""}` }, hasIssue && /* @__PURE__ */ React.createElement("div", { className: "wire-schematic-issue-banner" }, /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true" }, "\u26A0"), /* @__PURE__ */ React.createElement("span", null, issueNote ? issueNote : "Issue flagged")), /* @__PURE__ */ React.createElement(
      "svg",
      {
        viewBox: `0 0 ${W} ${H}`,
        className: "wire-schematic-svg",
        role: "img",
        "aria-label": `Cable schematic: ${cable.source || "\u2014"} \u2192 ${cable.destination || "\u2014"}`
      },
      /* @__PURE__ */ React.createElement(
        "text",
        {
          x: SRC_X,
          y: LINE_Y - NODE_R - 7,
          textAnchor: "middle",
          fontSize: "9",
          fontWeight: "700",
          letterSpacing: "0.06em",
          fill: srcDone ? "#16A34A" : "#94A3B8"
        },
        "SRC"
      ),
      /* @__PURE__ */ React.createElement(
        "text",
        {
          x: DST_X,
          y: LINE_Y - NODE_R - 7,
          textAnchor: "middle",
          fontSize: "9",
          fontWeight: "700",
          letterSpacing: "0.06em",
          fill: dstDone ? "#EA580C" : "#94A3B8"
        },
        "DST"
      ),
      /* @__PURE__ */ React.createElement(
        "line",
        {
          x1: lx1,
          y1: LINE_Y,
          x2: lx2,
          y2: LINE_Y,
          stroke: strokeHex,
          strokeWidth: sw,
          strokeLinecap: "round",
          strokeDasharray: dashArr,
          opacity,
          filter: highlight ? "drop-shadow(0 0 4px currentColor)" : void 0,
          color: strokeHex
        },
        !REDUCED_MOTION && !bothDone && dashArr && /* @__PURE__ */ React.createElement(
          "animate",
          {
            attributeName: "stroke-dashoffset",
            values: `${animFrom};0`,
            dur: "1.1s",
            repeatCount: "indefinite"
          }
        )
      ),
      hex2 && /* @__PURE__ */ React.createElement(
        "line",
        {
          x1: lx1,
          y1: LINE_Y + sw * 0.35,
          x2: lx2,
          y2: LINE_Y + sw * 0.35,
          stroke: hex2,
          strokeWidth: Math.max(2, sw * 0.45),
          strokeLinecap: "round",
          opacity: opacity * 0.85
        }
      ),
      /* @__PURE__ */ React.createElement(
        "circle",
        {
          cx: SRC_X,
          cy: LINE_Y,
          r: NODE_R,
          fill: srcFill,
          stroke: srcRing,
          strokeWidth: "2.5"
        }
      ),
      srcDone && /* @__PURE__ */ React.createElement(
        "path",
        {
          d: ckPath(SRC_X, LINE_Y),
          stroke: checkClr,
          strokeWidth: "2.5",
          strokeLinecap: "round",
          strokeLinejoin: "round",
          fill: "none"
        }
      ),
      /* @__PURE__ */ React.createElement(
        "circle",
        {
          cx: DST_X,
          cy: LINE_Y,
          r: NODE_R,
          fill: dstFill,
          stroke: dstRing,
          strokeWidth: "2.5"
        }
      ),
      dstDone && /* @__PURE__ */ React.createElement(
        "path",
        {
          d: ckPath(DST_X, LINE_Y),
          stroke: checkClr,
          strokeWidth: "2.5",
          strokeLinecap: "round",
          strokeLinejoin: "round",
          fill: "none"
        }
      ),
      ferrTxt && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
        "rect",
        {
          x: mid - 56,
          y: LINE_Y - 13,
          width: 112,
          height: 22,
          rx: "5",
          fill: "#FFFFFF",
          stroke: "#E2E8F0",
          strokeWidth: "1.2"
        }
      ), /* @__PURE__ */ React.createElement(
        "text",
        {
          x: mid,
          y: LINE_Y + 5,
          textAnchor: "middle",
          fontSize: "10.5",
          fontWeight: "700",
          fill: "#1E293B",
          fontFamily: "ui-monospace, SFMono-Regular, monospace"
        },
        ferrTxt
      )),
      metaLine && /* @__PURE__ */ React.createElement(React.Fragment, null, colorLabel && /* @__PURE__ */ React.createElement(
        "circle",
        {
          cx: mid - 46,
          cy: LINE_Y + 21,
          r: 4.5,
          fill: hex,
          stroke: isWhite ? "#94A3B8" : hex,
          strokeWidth: "1"
        }
      ), /* @__PURE__ */ React.createElement(
        "text",
        {
          x: colorLabel ? mid - 38 : mid,
          y: LINE_Y + 25,
          textAnchor: colorLabel ? "start" : "middle",
          fontSize: "9",
          fill: isUnknown ? "#94A3B8" : "#64748B",
          fontFamily: "ui-monospace, SFMono-Regular, monospace"
        },
        metaLine
      )),
      srcLabel && /* @__PURE__ */ React.createElement(
        "text",
        {
          x: SRC_X,
          y: LINE_Y + NODE_R + 16,
          textAnchor: "middle",
          fontSize: "9",
          fontWeight: "600",
          fill: "#334155",
          fontFamily: "ui-monospace, SFMono-Regular, monospace"
        },
        srcLabel
      ),
      srcDev && /* @__PURE__ */ React.createElement(
        "text",
        {
          x: SRC_X,
          y: LINE_Y + NODE_R + 28,
          textAnchor: "middle",
          fontSize: "8",
          fill: "#94A3B8"
        },
        srcDev
      ),
      dstLabel && /* @__PURE__ */ React.createElement(
        "text",
        {
          x: DST_X,
          y: LINE_Y + NODE_R + 16,
          textAnchor: "middle",
          fontSize: "9",
          fontWeight: "600",
          fill: "#334155",
          fontFamily: "ui-monospace, SFMono-Regular, monospace"
        },
        dstLabel
      ),
      dstDev && /* @__PURE__ */ React.createElement(
        "text",
        {
          x: DST_X,
          y: LINE_Y + NODE_R + 28,
          textAnchor: "middle",
          fontSize: "8",
          fill: "#94A3B8"
        },
        dstDev
      )
    ));
  }

  // src/components/ui/CompletionReport.tsx
  init_define_import_meta_env();
  function ProgressBar({ value, tone, height = 8 }) {
    return /* @__PURE__ */ React.createElement(
      "progress",
      {
        className: "cr-progress w-full",
        "data-tone": tone,
        "data-h": String(height),
        value: Math.min(value, 100),
        max: 100
      }
    );
  }
  function StatBlock({ value, label, tone }) {
    const textColors = {
      blue: "text-blue-700",
      orange: "text-orange-600",
      green: "text-green-700",
      red: "text-red-600",
      gray: "text-slate-600"
    };
    return /* @__PURE__ */ React.createElement("div", { className: "flex flex-col items-center px-3 py-2.5 rounded-xl bg-slate-50 border border-slate-200 min-w-[72px]" }, /* @__PURE__ */ React.createElement("div", { className: `text-[20px] font-bold tabular-nums leading-none ${tone ? textColors[tone] : "text-slate-800"}` }, value), /* @__PURE__ */ React.createElement("div", { className: "text-[9px] font-bold uppercase tracking-widest text-slate-400 mt-1 select-none" }, label));
  }
  function fmt(dt) {
    if (!dt) return "--";
    return new Date(dt).toLocaleString(void 0, { month: "short", day: "numeric", hour: "2-digit", minute: "2-digit" });
  }
  function CompletionReport({ data: data2, onDownloadXlsx, compact = false }) {
    const { technician, project, assignment: a, rollup } = data2;
    const isCompleted = a.status === "completed" || a.status === "approved";
    return /* @__PURE__ */ React.createElement("div", { className: "flex flex-col gap-0 rounded-2xl border border-slate-200 bg-white overflow-hidden shadow-sm" }, /* @__PURE__ */ React.createElement("div", { className: "px-5 py-4 bg-gradient-to-r from-slate-800 to-slate-700 flex items-center gap-3" }, /* @__PURE__ */ React.createElement(CompanyLogo, { variant: "white", size: "sm", className: "shrink-0" }), isCompleted ? /* @__PURE__ */ React.createElement(CheckCircle, { size: 20, className: "text-emerald-400 shrink-0" }) : /* @__PURE__ */ React.createElement(AlertCircle, { size: 20, className: "text-amber-400 shrink-0" }), /* @__PURE__ */ React.createElement("div", { className: "flex-1 min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "text-[15px] font-bold text-white truncate select-none" }, a.panel_name), /* @__PURE__ */ React.createElement("div", { className: "text-[11px] font-mono text-slate-400 mt-0.5 select-none" }, project.code)), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 shrink-0" }, a.report_submitted && /* @__PURE__ */ React.createElement("span", { className: "text-[10px] font-bold bg-emerald-600 text-white px-2 py-0.5 rounded-full select-none" }, "SUBMITTED"), onDownloadXlsx && /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: onDownloadXlsx,
        className: "flex items-center gap-1.5 h-7 px-3 rounded-lg bg-white/10 hover:bg-white/20 text-white text-[11px] font-semibold transition-colors"
      },
      /* @__PURE__ */ React.createElement(FileSpreadsheet, { size: 12 }),
      /* @__PURE__ */ React.createElement("span", null, "XLSX")
    ))), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-1 tablet-port:grid-cols-2 divide-y tablet-port:divide-y-0 tablet-port:divide-x divide-slate-100" }, /* @__PURE__ */ React.createElement("div", { className: "px-5 py-3 flex items-center gap-3" }, /* @__PURE__ */ React.createElement("div", { className: "w-8 h-8 rounded-full bg-blue-100 flex items-center justify-center shrink-0" }, /* @__PURE__ */ React.createElement(User, { size: 16, className: "text-blue-700" })), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "text-[13px] font-bold text-slate-800 truncate" }, technician.full_name), /* @__PURE__ */ React.createElement("div", { className: "text-[11px] text-slate-400 mt-0.5 select-none" }, "@", technician.username, technician.employee_id && /* @__PURE__ */ React.createElement(React.Fragment, null, " \xB7 ", /* @__PURE__ */ React.createElement("span", { className: "font-mono" }, technician.employee_id))))), /* @__PURE__ */ React.createElement("div", { className: "px-5 py-3 flex items-center gap-3" }, /* @__PURE__ */ React.createElement("div", { className: "w-8 h-8 rounded-full bg-slate-100 flex items-center justify-center shrink-0" }, /* @__PURE__ */ React.createElement(Building2, { size: 16, className: "text-slate-600" })), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "text-[13px] font-bold text-slate-800 truncate" }, project.name), /* @__PURE__ */ React.createElement("div", { className: "text-[11px] text-slate-400 mt-0.5 select-none" }, project.client && /* @__PURE__ */ React.createElement(React.Fragment, null, project.client, " \xB7 "), /* @__PURE__ */ React.createElement("span", { className: "font-mono" }, project.code))))), /* @__PURE__ */ React.createElement("div", { className: "border-t border-slate-100" }), /* @__PURE__ */ React.createElement("div", { className: "px-5 py-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 mb-3" }, /* @__PURE__ */ React.createElement(Cable, { size: 14, className: "text-slate-400" }), /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold uppercase tracking-widest text-slate-400 select-none" }, "Cable Progress")), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3 mb-1" }, /* @__PURE__ */ React.createElement("div", { className: "flex-1" }, /* @__PURE__ */ React.createElement(ProgressBar, { value: a.kpi, tone: "overall", height: 10 })), /* @__PURE__ */ React.createElement("div", { className: "text-[18px] font-bold text-blue-700 tabular-nums shrink-0 min-w-[3.5ch]" }, a.kpi, "%")), /* @__PURE__ */ React.createElement("div", { className: "text-[11px] text-slate-500 mb-3 select-none" }, a.cables_both_done, " of ", a.cables_total, " cables fully completed (", a.kpi, "%)"), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-4 gap-2 mb-4" }, /* @__PURE__ */ React.createElement(StatBlock, { value: a.cables_both_done, label: "Complete", tone: "green" }), /* @__PURE__ */ React.createElement(StatBlock, { value: a.cables_src_done, label: "Src Done", tone: "blue" }), /* @__PURE__ */ React.createElement(StatBlock, { value: a.cables_dst_done, label: "Dst Done", tone: "orange" }), /* @__PURE__ */ React.createElement(StatBlock, { value: a.cables_pending, label: "Pending", tone: "gray" })), !compact && /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-2 gap-3" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ React.createElement("span", { className: "text-[10px] font-bold uppercase tracking-wider text-blue-600 select-none" }, "Source"), /* @__PURE__ */ React.createElement("span", { className: "text-[10px] font-bold tabular-nums text-blue-700" }, a.cables_src_done, "/", a.cables_total)), /* @__PURE__ */ React.createElement(ProgressBar, { value: a.src_pct, tone: "source", height: 6 })), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ React.createElement("span", { className: "text-[10px] font-bold uppercase tracking-wider text-orange-600 select-none" }, "Destination"), /* @__PURE__ */ React.createElement("span", { className: "text-[10px] font-bold tabular-nums text-orange-600" }, a.cables_dst_done, "/", a.cables_total)), /* @__PURE__ */ React.createElement(ProgressBar, { value: a.dst_pct, tone: "destination", height: 6 })))), /* @__PURE__ */ React.createElement("div", { className: "border-t border-slate-100" }), /* @__PURE__ */ React.createElement("div", { className: "px-5 py-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 mb-3" }, /* @__PURE__ */ React.createElement(Clock, { size: 14, className: "text-slate-400" }), /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold uppercase tracking-widest text-slate-400 select-none" }, "Time Tracking")), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-3 gap-3" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "text-[10px] text-slate-400 uppercase tracking-wider mb-0.5 select-none" }, "Started"), /* @__PURE__ */ React.createElement("div", { className: "text-[12px] font-semibold text-slate-700" }, fmt(a.started_at))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "text-[10px] text-slate-400 uppercase tracking-wider mb-0.5 select-none" }, "Completed"), /* @__PURE__ */ React.createElement("div", { className: "text-[12px] font-semibold text-slate-700" }, fmt(a.completed_at))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "text-[10px] text-slate-400 uppercase tracking-wider mb-0.5 select-none" }, "Duration"), /* @__PURE__ */ React.createElement("div", { className: "text-[14px] font-bold text-slate-800" }, a.duration_human || "--")))), /* @__PURE__ */ React.createElement("div", { className: "border-t border-slate-100" }), /* @__PURE__ */ React.createElement("div", { className: "px-5 py-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 mb-3" }, /* @__PURE__ */ React.createElement(BarChart3, { size: 14, className: "text-slate-400" }), /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold uppercase tracking-widest text-slate-400 select-none" }, "Project Rollup")), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement("div", { className: "flex-1" }, /* @__PURE__ */ React.createElement(ProgressBar, { value: rollup.project_kpi, tone: "rollup", height: 8 })), /* @__PURE__ */ React.createElement("span", { className: "text-[13px] font-bold text-green-700 tabular-nums shrink-0" }, rollup.project_kpi, "%")), /* @__PURE__ */ React.createElement("div", { className: "mt-1.5 text-[11px] text-slate-500 select-none" }, rollup.completed_frames, " of ", rollup.total_frames, " panel", rollup.total_frames !== 1 ? "s" : "", " completed in ", project.name)), /* @__PURE__ */ React.createElement("div", { className: "px-5 py-2 bg-slate-50 border-t border-slate-100" }, /* @__PURE__ */ React.createElement("div", { className: "text-[10px] text-slate-400 select-none" }, "Report generated ", new Date(data2.generated_at).toLocaleString(), a.review_status && /* @__PURE__ */ React.createElement(React.Fragment, null, " \xB7 Review: ", /* @__PURE__ */ React.createElement("span", { className: "font-semibold capitalize" }, a.review_status.replace(/_/g, " "))))));
  }

  // src/components/ui/ReportPreviewModal.tsx
  init_define_import_meta_env();
  var import_react16 = __toESM(require_react_shim(), 1);

  // src/hooks/useReadOnlyPoll.ts
  init_define_import_meta_env();
  var import_react15 = __toESM(require_react_shim(), 1);
  function useReadOnlyPoll(fetchFn, intervalMs = 4e3) {
    const inFlightRef = (0, import_react15.useRef)(false);
    const fetchRef = (0, import_react15.useRef)(fetchFn);
    fetchRef.current = fetchFn;
    (0, import_react15.useEffect)(() => {
      const tick = async () => {
        if (document.hidden || inFlightRef.current) return;
        inFlightRef.current = true;
        try {
          await fetchRef.current();
        } catch {
        } finally {
          inFlightRef.current = false;
        }
      };
      const id = setInterval(tick, intervalMs);
      const onVis = () => {
        if (!document.hidden) void tick();
      };
      document.addEventListener("visibilitychange", onVis);
      return () => {
        clearInterval(id);
        document.removeEventListener("visibilitychange", onVis);
      };
    }, [intervalMs]);
  }

  // src/components/ui/ReportPreviewModal.tsx
  function fmtDateTime(dt) {
    if (!dt) return "--";
    return new Date(dt).toLocaleString(void 0, {
      year: "numeric",
      month: "short",
      day: "numeric",
      hour: "2-digit",
      minute: "2-digit"
    });
  }
  function fmtTime(dt) {
    if (!dt) return "";
    return new Date(dt).toLocaleString(void 0, {
      month: "short",
      day: "numeric",
      hour: "2-digit",
      minute: "2-digit"
    });
  }
  function ReportPreviewModal({
    assignmentId,
    projectCode,
    frameId,
    panelName,
    onClose,
    onSubmit,
    submitting = false,
    loadReport = supervisorApi.completionReport,
    showExport = true
  }) {
    const [report, setReport] = (0, import_react16.useState)(null);
    const [failed, setFailed] = (0, import_react16.useState)(false);
    const [exporting, setExporting] = (0, import_react16.useState)(false);
    const fetchReport = (0, import_react16.useCallback)(async () => {
      const data2 = await loadReport(assignmentId);
      setReport(data2);
    }, [assignmentId, loadReport]);
    (0, import_react16.useEffect)(() => {
      fetchReport().catch(() => setFailed(true));
    }, [fetchReport]);
    useReadOnlyPoll(fetchReport, 6e3);
    const exportXlsx = async () => {
      setExporting(true);
      try {
        const blob = await supervisorApi.panelReportXlsx(projectCode, frameId);
        const url = URL.createObjectURL(blob);
        const anchor = document.createElement("a");
        anchor.href = url;
        anchor.download = `${panelName}_report.xlsx`;
        anchor.click();
        URL.revokeObjectURL(url);
      } catch {
      } finally {
        setExporting(false);
      }
    };
    const a = report?.assignment;
    const completionPct = a && a.cables_total > 0 ? Math.round(a.cables_both_done / a.cables_total * 100) : 0;
    return /* @__PURE__ */ React.createElement(
      Modal,
      {
        title: "Report Preview",
        onClose,
        size: "xl",
        footer: /* @__PURE__ */ React.createElement(React.Fragment, null, showExport && /* @__PURE__ */ React.createElement("button", { onClick: exportXlsx, disabled: exporting || !report, className: "btn-secondary", type: "button" }, /* @__PURE__ */ React.createElement(Download, { size: 16 }), /* @__PURE__ */ React.createElement("span", null, exporting ? "Exporting\u2026" : "Export XLSX")), onSubmit ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { onClick: onClose, className: "btn-secondary", type: "button", disabled: submitting }, "Cancel"), /* @__PURE__ */ React.createElement(
          "button",
          {
            onClick: () => void onSubmit(),
            disabled: submitting || !report,
            className: "btn-primary",
            type: "button"
          },
          submitting ? "Submitting\u2026" : "Submit Report"
        )) : /* @__PURE__ */ React.createElement("button", { onClick: onClose, className: "btn-primary", type: "button" }, "Close"))
      },
      !report && !failed && /* @__PURE__ */ React.createElement("div", { className: "empty-state" }, /* @__PURE__ */ React.createElement("p", { className: "empty-text" }, "Loading report\u2026")),
      failed && !report && /* @__PURE__ */ React.createElement("div", { className: "form-error" }, "Could not load report data."),
      report && a && /* @__PURE__ */ React.createElement("div", { className: "flex flex-col gap-4" }, /* @__PURE__ */ React.createElement("div", { className: "rounded-2xl bg-gradient-to-r from-slate-800 to-slate-700 px-5 py-4 flex flex-wrap items-center gap-4" }, /* @__PURE__ */ React.createElement(CompanyLogo, { variant: "white", size: "sm", className: "shrink-0" }), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3 min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("div", { className: "w-10 h-10 rounded-full bg-white/10 flex items-center justify-center shrink-0" }, /* @__PURE__ */ React.createElement(User, { size: 20, className: "text-white" })), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "text-[16px] font-bold text-white truncate" }, report.technician.full_name), /* @__PURE__ */ React.createElement("div", { className: "text-[12px] text-slate-300 truncate select-none" }, "Technician", report.technician.employee_id && /* @__PURE__ */ React.createElement(React.Fragment, null, " \xB7 ", /* @__PURE__ */ React.createElement("span", { className: "font-mono" }, report.technician.employee_id))))), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3 min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("div", { className: "w-10 h-10 rounded-full bg-white/10 flex items-center justify-center shrink-0" }, /* @__PURE__ */ React.createElement(Building2, { size: 20, className: "text-white" })), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "text-[15px] font-bold text-white truncate" }, report.project.name), /* @__PURE__ */ React.createElement("div", { className: "text-[12px] text-slate-300 truncate select-none" }, "Panel: ", /* @__PURE__ */ React.createElement("span", { className: "font-semibold text-slate-100" }, a.panel_name)))), /* @__PURE__ */ React.createElement("span", { className: "flex items-center gap-1.5 bg-emerald-500/15 border border-emerald-400/40 text-emerald-300 text-[11px] font-bold uppercase tracking-wider px-2.5 py-1 rounded-full select-none shrink-0" }, /* @__PURE__ */ React.createElement("span", { className: "w-2 h-2 rounded-full bg-emerald-400 animate-pulse" }), "Live")), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-1 tablet-port:grid-cols-2 tablet-land:grid-cols-3 gap-3" }, /* @__PURE__ */ React.createElement("div", { className: "kpi-card", "data-variant": "blue" }, /* @__PURE__ */ React.createElement("div", { className: "kpi-icon-wrap" }, /* @__PURE__ */ React.createElement(Clock, { size: 20 })), /* @__PURE__ */ React.createElement("div", { className: "kpi-content" }, /* @__PURE__ */ React.createElement("span", { className: "kpi-value-base kpi-value-blue" }, a.duration_human || "--"), /* @__PURE__ */ React.createElement("span", { className: "kpi-label" }, "Total Hours (technician)"))), /* @__PURE__ */ React.createElement("div", { className: "kpi-card", "data-variant": "default" }, /* @__PURE__ */ React.createElement("div", { className: "kpi-icon-wrap" }, /* @__PURE__ */ React.createElement(Cable, { size: 20 })), /* @__PURE__ */ React.createElement("div", { className: "kpi-content" }, /* @__PURE__ */ React.createElement("span", { className: "kpi-value-base kpi-value" }, a.cables_total), /* @__PURE__ */ React.createElement("span", { className: "kpi-label" }, "Total Cable Assignment"))), /* @__PURE__ */ React.createElement("div", { className: "kpi-card", "data-variant": "green" }, /* @__PURE__ */ React.createElement("div", { className: "kpi-icon-wrap" }, /* @__PURE__ */ React.createElement(CheckCircle, { size: 20 })), /* @__PURE__ */ React.createElement("div", { className: "kpi-content" }, /* @__PURE__ */ React.createElement("span", { className: "kpi-value-base kpi-value-green" }, completionPct, "%"), /* @__PURE__ */ React.createElement("span", { className: "kpi-label" }, "Completion")))), /* @__PURE__ */ React.createElement("div", { className: "rounded-2xl border border-slate-200 bg-white px-5 py-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold uppercase tracking-widest text-slate-400 select-none" }, "Cable Completion Status"), /* @__PURE__ */ React.createElement("span", { className: "text-[14px] font-bold text-slate-800 tabular-nums" }, a.cables_both_done, "/", a.cables_total, " ", /* @__PURE__ */ React.createElement("span", { className: "font-medium text-slate-500" }, "cables completed"))), /* @__PURE__ */ React.createElement("progress", { className: "cr-progress w-full", "data-tone": "overall", "data-h": "10", value: Math.min(completionPct, 100), max: 100 })), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-1 tablet-port:grid-cols-2 gap-3" }, /* @__PURE__ */ React.createElement("div", { className: "rounded-2xl border border-slate-200 bg-white px-5 py-4" }, /* @__PURE__ */ React.createElement("div", { className: "text-[11px] font-bold uppercase tracking-widest text-slate-400 mb-1 select-none" }, "Start Time"), /* @__PURE__ */ React.createElement("div", { className: "text-[15px] font-bold text-slate-800" }, fmtDateTime(a.started_at))), /* @__PURE__ */ React.createElement("div", { className: "rounded-2xl border border-slate-200 bg-white px-5 py-4" }, /* @__PURE__ */ React.createElement("div", { className: "text-[11px] font-bold uppercase tracking-widest text-slate-400 mb-1 select-none" }, "End Time"), /* @__PURE__ */ React.createElement("div", { className: "text-[15px] font-bold text-slate-800" }, fmtDateTime(a.completed_at)))), /* @__PURE__ */ React.createElement("div", { className: "rounded-2xl border border-slate-200 bg-white px-5 py-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 mb-3" }, /* @__PURE__ */ React.createElement(PauseCircle, { size: 16, className: "text-amber-600" }), /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold uppercase tracking-widest text-slate-400 select-none" }, "Break Reason Log")), (report.break_log?.length ?? 0) === 0 ? /* @__PURE__ */ React.createElement("div", { className: "text-[13px] text-slate-400 select-none" }, "No breaks recorded.") : /* @__PURE__ */ React.createElement("ul", { className: "flex flex-col gap-2" }, report.break_log.map((entry, index) => /* @__PURE__ */ React.createElement("li", { key: index, className: "flex items-center justify-between gap-3 rounded-xl bg-amber-50 border border-amber-100 px-3 py-2" }, /* @__PURE__ */ React.createElement("span", { className: "text-[13px] font-medium text-slate-700 min-w-0 truncate", title: entry.reason }, entry.reason), /* @__PURE__ */ React.createElement("span", { className: "text-[12px] text-slate-500 tabular-nums shrink-0" }, fmtTime(entry.at)))))), /* @__PURE__ */ React.createElement("div", { className: "rounded-2xl border border-slate-200 bg-white px-5 py-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 mb-3" }, /* @__PURE__ */ React.createElement(FileText, { size: 16, className: "text-blue-600" }), /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold uppercase tracking-widest text-slate-400 select-none" }, "Technician Notes / Remarks")), (report.technician_notes?.length ?? 0) === 0 ? /* @__PURE__ */ React.createElement("div", { className: "text-[13px] text-slate-400 select-none" }, "No notes recorded for this panel.") : /* @__PURE__ */ React.createElement("ul", { className: "flex flex-col gap-2" }, report.technician_notes.map((note, index) => /* @__PURE__ */ React.createElement("li", { key: index, className: "flex items-start gap-2.5 rounded-xl bg-slate-50 border border-slate-100 px-3 py-2" }, note.issue ? /* @__PURE__ */ React.createElement(AlertCircle, { size: 16, className: "text-red-500 shrink-0 mt-0.5" }) : /* @__PURE__ */ React.createElement(FileText, { size: 16, className: "text-slate-400 shrink-0 mt-0.5" }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold text-slate-400 uppercase tracking-wider select-none" }, "Cable #", note.cable_index), /* @__PURE__ */ React.createElement("p", { className: "text-[13px] text-slate-700 mt-0.5 break-words" }, note.note)))))), /* @__PURE__ */ React.createElement("div", { className: "text-[11px] text-slate-400 text-right select-none" }, "Live preview \xB7 refreshes every 6s \xB7 last updated ", new Date(report.generated_at).toLocaleTimeString()))
    );
  }

  // src/components/ui/VerificationModal.tsx
  init_define_import_meta_env();
  var import_react17 = __toESM(require_react_shim(), 1);
  var FIELD_LABELS = {
    ferrule: "Ferrule",
    source: "Source",
    destination: "Destination",
    path: "Path",
    source_device: "Src Device",
    source_terminal: "Src Terminal",
    dest_device: "Dst Device",
    dest_terminal: "Dst Terminal",
    color: "Color",
    size: "Size",
    length: "Length",
    ref: "Ref",
    remarks: "Remarks",
    sign: "Sign",
    sno: "S.No",
    panel: "Panel",
    rack: "Rack"
  };
  var TABLE_COLS = ["ferrule", "source", "destination", "color", "size", "length", "ref"];
  function VerificationModal({ projectCode, frameId, onClose, onVerified }) {
    const [loading, setLoading] = (0, import_react17.useState)(true);
    const [loadError, setLoadError] = (0, import_react17.useState)("");
    const [meta, setMeta] = (0, import_react17.useState)(null);
    const [cables, setCables] = (0, import_react17.useState)([]);
    const [mapping, setMapping] = (0, import_react17.useState)({});
    const [issues, setIssues] = (0, import_react17.useState)({});
    const [errorCount, setErrorCount] = (0, import_react17.useState)(0);
    const [okCount, setOkCount] = (0, import_react17.useState)(0);
    const [editCell, setEditCell] = (0, import_react17.useState)(null);
    const [editValue, setEditValue] = (0, import_react17.useState)("");
    const [remapField, setRemapField] = (0, import_react17.useState)(null);
    const [remapping, setRemapping] = (0, import_react17.useState)(false);
    const [confirming, setConfirming] = (0, import_react17.useState)(false);
    const [confirmError, setConfirmError] = (0, import_react17.useState)("");
    const [sourceFile, setSourceFile] = (0, import_react17.useState)(null);
    const [comparing, setComparing] = (0, import_react17.useState)(false);
    const [mismatches, setMismatches] = (0, import_react17.useState)({});
    const [mismatchCount, setMismatchCount] = (0, import_react17.useState)(0);
    const fileInputRef = (0, import_react17.useRef)(null);
    const editRef = (0, import_react17.useRef)(null);
    (0, import_react17.useEffect)(() => {
      setLoading(true);
      setLoadError("");
      projectsApi.verifyData(projectCode, frameId).then((d) => {
        const cableList = Array.isArray(d.cables) ? d.cables : [];
        setMeta({
          panel_name: d.panel_name,
          original_filename: d.original_filename,
          sheet_name: d.sheet_name,
          excel_headers: d.excel_headers || [],
          compare_status: d.compare_status,
          has_source_excel: d.has_source_excel,
          mapping: d.mapping || {}
        });
        setCables(cableList);
        setMapping(d.mapping || {});
        const v = d.validation || { issues: {}, error_count: 0, ok_count: cableList.length };
        setIssues(v.issues || {});
        setErrorCount(v.error_count || 0);
        setOkCount(v.ok_count ?? cableList.length);
        setLoading(false);
      }).catch((e) => {
        setLoadError(e?.response?.data?.message || e?.message || "Failed to load cable data. Check backend is running.");
        setLoading(false);
      });
    }, [projectCode, frameId]);
    (0, import_react17.useEffect)(() => {
      if (editCell && editRef.current) editRef.current.focus();
    }, [editCell]);
    (0, import_react17.useEffect)(() => {
      if (!remapField) return;
      const close = () => setRemapField(null);
      const t = setTimeout(() => document.addEventListener("click", close), 0);
      return () => {
        clearTimeout(t);
        document.removeEventListener("click", close);
      };
    }, [remapField]);
    const applyValidation = (v) => {
      setIssues(v?.issues || {});
      setErrorCount(v?.error_count || 0);
      setOkCount(v?.ok_count ?? 0);
    };
    const startEdit = (idx, field) => {
      setEditCell({ idx, field });
      setEditValue(String(cables[idx]?.[field] ?? ""));
    };
    const saveEdit = async () => {
      if (!editCell) return;
      const { idx, field } = editCell;
      const prev = String(cables[idx]?.[field] ?? "");
      setEditCell(null);
      if (prev === editValue) return;
      const val = editValue;
      setCables((cs) => {
        const n = [...cs];
        n[idx] = { ...n[idx], [field]: val };
        return n;
      });
      try {
        const r = await projectsApi.patchCable(projectCode, frameId, idx, field, val);
        setCables((cs) => {
          const n = [...cs];
          n[idx] = r.cable;
          return n;
        });
        applyValidation(r.validation);
      } catch {
        setCables((cs) => {
          const n = [...cs];
          n[idx] = { ...n[idx], [field]: prev };
          return n;
        });
      }
    };
    const handleRemap = async (field, header) => {
      setRemapField(null);
      setRemapping(true);
      try {
        const r = await projectsApi.remapColumn(projectCode, frameId, field, header);
        setCables(r.cables);
        setMapping(r.mapping);
        applyValidation(r.validation);
      } catch {
      } finally {
        setRemapping(false);
      }
    };
    const handleSourceFileChange = async (e) => {
      const file = e.target.files?.[0];
      if (!file) return;
      setSourceFile(file);
      setComparing(true);
      setMismatches({});
      setMismatchCount(0);
      const fd = new FormData();
      fd.append("file", file);
      try {
        const r = await projectsApi.compareSourceFile(projectCode, frameId, fd);
        setMismatches(r.mismatches || {});
        setMismatchCount(r.mismatch_count || 0);
      } catch {
      } finally {
        setComparing(false);
        if (fileInputRef.current) fileInputRef.current.value = "";
      }
    };
    const handleConfirm = async () => {
      setConfirming(true);
      setConfirmError("");
      try {
        await projectsApi.verifyConfirm(projectCode, frameId);
        onVerified();
      } catch (e) {
        setConfirmError(e?.response?.data?.message || "Verification failed \u2014 try again.");
      } finally {
        setConfirming(false);
      }
    };
    const isVerified = meta?.compare_status === "validated" || meta?.compare_status === "verified";
    const mappedEntries = Object.entries(mapping).filter(([, v]) => v);
    const totalIssues = errorCount + mismatchCount;
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "modal-overlay z-[210]",
        onClick: (e) => {
          if (e.target === e.currentTarget) onClose();
        }
      },
      /* @__PURE__ */ React.createElement("div", { className: "modal-box-full relative" }, /* @__PURE__ */ React.createElement("div", { className: "px-6 pt-5 pb-4 border-b border-slate-200 shrink-0" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3 mb-3" }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: onClose,
          title: "Close",
          className: "w-8 h-8 rounded-lg flex items-center justify-center text-slate-400 hover:text-slate-700 hover:bg-slate-100 transition-colors shrink-0"
        },
        /* @__PURE__ */ React.createElement(X, { size: 16 })
      ), /* @__PURE__ */ React.createElement("div", { className: "flex-1 min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "text-[15px] font-bold text-slate-900 truncate" }, "Verify: ", meta?.panel_name || (loading ? "\u2026" : "Frame")), /* @__PURE__ */ React.createElement("div", { className: "text-[11px] font-mono text-slate-400 truncate mt-0.5" }, meta?.original_filename, meta?.sheet_name && ` \xB7 Sheet: ${meta.sheet_name}`, !loading && cables.length > 0 && ` \xB7 ${cables.length} cables`)), /* @__PURE__ */ React.createElement("div", { className: `flex items-center gap-1.5 px-3 py-1.5 rounded-xl text-[12px] font-bold select-none shrink-0 border ${loading ? "bg-slate-50 text-slate-400 border-slate-200" : isVerified ? "bg-emerald-50 text-emerald-700 border-emerald-200" : "bg-slate-50 text-slate-600 border-slate-200"}` }, isVerified ? /* @__PURE__ */ React.createElement(CheckCircle2, { size: 14 }) : /* @__PURE__ */ React.createElement(Info, { size: 14 }), loading ? "Loading\u2026" : isVerified ? "Verified" : `Draft`)), !loading && !loadError && /* @__PURE__ */ React.createElement("div", { className: "flex flex-wrap gap-2 items-center min-h-[28px]" }, /* @__PURE__ */ React.createElement(
        "input",
        {
          ref: fileInputRef,
          type: "file",
          accept: ".xlsx,.xls",
          title: "Select original Excel file for comparison",
          className: "hidden",
          onChange: handleSourceFileChange
        }
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: () => fileInputRef.current?.click(),
          disabled: comparing,
          title: "Optionally select the original Excel to compare against parsed data",
          className: "inline-flex items-center gap-1.5 px-2.5 py-1 rounded-lg border text-[11px] font-semibold transition-colors select-none\n                  border-blue-200 bg-blue-50 text-blue-700 hover:bg-blue-100 hover:border-blue-300 disabled:opacity-50"
        },
        /* @__PURE__ */ React.createElement(FolderOpen, { size: 12 }),
        comparing ? "Comparing\u2026" : "Select Source File"
      ), sourceFile && !comparing && /* @__PURE__ */ React.createElement("div", { className: `inline-flex items-center gap-1.5 px-2 py-0.5 rounded-lg text-[11px] font-semibold border select-none ${mismatchCount === 0 ? "bg-emerald-50 text-emerald-700 border-emerald-200" : "bg-amber-50 text-amber-700 border-amber-200"}` }, /* @__PURE__ */ React.createElement(FileSpreadsheet, { size: 10 }), /* @__PURE__ */ React.createElement("span", { className: "truncate max-w-[180px]" }, sourceFile.name), /* @__PURE__ */ React.createElement("span", null, "\u2014"), /* @__PURE__ */ React.createElement("span", null, mismatchCount === 0 ? "No mismatches" : `${mismatchCount} mismatch${mismatchCount !== 1 ? "es" : ""}`)), mappedEntries.length > 0 && /* @__PURE__ */ React.createElement("span", { className: "text-slate-200 select-none text-[14px]" }, "|"), mappedEntries.map(([field, excelCol]) => /* @__PURE__ */ React.createElement("div", { key: field, className: "relative", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("div", { className: `inline-flex items-center gap-1 rounded-lg px-2 py-0.5 border text-[10px] font-semibold cursor-default select-none ${remapField === field ? "bg-blue-100 border-blue-300" : "bg-slate-50 border-slate-200"}` }, /* @__PURE__ */ React.createElement("span", { className: "text-slate-500" }, FIELD_LABELS[field] || field), /* @__PURE__ */ React.createElement("span", { className: "text-slate-300" }, "\u2190"), /* @__PURE__ */ React.createElement("span", { className: "font-mono text-blue-700" }, excelCol), meta?.has_source_excel && /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: (e) => {
            e.stopPropagation();
            setRemapField(remapField === field ? null : field);
          },
          className: "text-slate-300 hover:text-blue-500 transition-colors ml-0.5 cursor-pointer",
          title: "Remap this column",
          disabled: remapping
        },
        /* @__PURE__ */ React.createElement(RefreshCw, { size: 10, className: remapping ? "animate-spin" : "" })
      )), remapField === field && /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "absolute top-full left-0 mt-1 z-20 bg-white border border-slate-200 rounded-xl shadow-xl overflow-hidden min-w-[200px]",
          onClick: (e) => e.stopPropagation()
        },
        /* @__PURE__ */ React.createElement("div", { className: "px-3 py-2 bg-slate-50 border-b border-slate-100 text-[10px] font-bold uppercase tracking-wider text-slate-500 select-none" }, 'Remap "', FIELD_LABELS[field] || field, '" to:'),
        /* @__PURE__ */ React.createElement("div", { className: "overflow-y-auto max-h-[240px] p-1" }, (meta?.excel_headers || []).map((h) => /* @__PURE__ */ React.createElement(
          "button",
          {
            key: h,
            type: "button",
            onClick: () => handleRemap(field, h),
            className: `w-full text-left px-2.5 py-1.5 rounded-lg text-[11px] transition-colors ${mapping[field] === h ? "bg-blue-50 text-blue-700 font-semibold" : "text-slate-700 hover:bg-slate-50"}`
          },
          h
        ))),
        /* @__PURE__ */ React.createElement("div", { className: "border-t border-slate-100 p-1" }, /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: () => handleRemap(field, ""),
            className: "w-full flex items-center gap-1 px-2.5 py-1.5 rounded-lg text-[11px] text-red-500 hover:bg-red-50 transition-colors"
          },
          /* @__PURE__ */ React.createElement(RotateCcw, { size: 10 }),
          " Remove mapping"
        ))
      ))), !meta?.has_source_excel && mappedEntries.length === 0 && /* @__PURE__ */ React.createElement("span", { className: "text-[10px] text-slate-400 italic select-none" }, 'No source Excel stored \u2014 use "Select Source File" to compare, or edit cells directly.'))), /* @__PURE__ */ React.createElement("div", { className: "flex-1 overflow-auto" }, loading ? /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-center py-20 text-slate-400 text-[13px]" }, "Loading cable data\u2026") : loadError ? /* @__PURE__ */ React.createElement("div", { className: "flex flex-col items-center justify-center py-20 gap-3" }, /* @__PURE__ */ React.createElement(AlertTriangle, { size: 32, className: "text-red-400" }), /* @__PURE__ */ React.createElement("div", { className: "text-[13px] font-semibold text-red-600 text-center max-w-md" }, loadError), /* @__PURE__ */ React.createElement("div", { className: "text-[11px] text-slate-400 text-center" }, "Ensure the backend server is running, then close and re-open the verification modal.")) : cables.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "flex flex-col items-center justify-center py-20 gap-2 text-slate-400" }, /* @__PURE__ */ React.createElement(Info, { size: 32 }), /* @__PURE__ */ React.createElement("div", { className: "text-[13px]" }, "No cable data found for this frame.")) : /* @__PURE__ */ React.createElement("table", { className: "w-full border-collapse text-[12px]" }, /* @__PURE__ */ React.createElement("thead", { className: "sticky top-0 z-10 shadow-[0_1px_0_#E2E8F0]" }, /* @__PURE__ */ React.createElement("tr", null, /* @__PURE__ */ React.createElement("th", { className: "px-3 py-2 text-left text-[10px] font-bold uppercase tracking-wider text-slate-400 w-[44px] select-none border-r border-slate-100 bg-slate-50" }, "#"), TABLE_COLS.map((col) => {
        const hasIssue = Object.values(issues).some((ci) => col in ci);
        const hasMismatch = !hasIssue && Object.values(mismatches).some((cm) => col in cm);
        return /* @__PURE__ */ React.createElement(
          "th",
          {
            key: col,
            className: `px-3 py-2 text-left text-[10px] font-bold uppercase tracking-wider select-none ${hasIssue ? "text-red-500 bg-red-50" : hasMismatch ? "text-amber-600 bg-amber-50" : "text-slate-400 bg-slate-50"}`
          },
          FIELD_LABELS[col],
          hasIssue && /* @__PURE__ */ React.createElement("span", { className: "ml-1 opacity-60" }, "\u26A0"),
          hasMismatch && /* @__PURE__ */ React.createElement("span", { className: "ml-1 opacity-60" }, "\u2260")
        );
      }))), /* @__PURE__ */ React.createElement("tbody", { className: "divide-y divide-slate-100" }, cables.map((cable, idx) => {
        const rowIssues = issues[idx] || {};
        const rowMismatches = mismatches[idx] || {};
        const hasRowError = Object.keys(rowIssues).length > 0;
        const hasRowMismatch = !hasRowError && Object.keys(rowMismatches).length > 0;
        return /* @__PURE__ */ React.createElement(
          "tr",
          {
            key: idx,
            className: `transition-colors hover:bg-blue-50/20 ${hasRowError ? "bg-red-50/30" : hasRowMismatch ? "bg-amber-50/30" : idx % 2 === 0 ? "bg-white" : "bg-slate-50/30"}`
          },
          /* @__PURE__ */ React.createElement("td", { className: "px-3 py-1.5 text-[11px] tabular-nums text-slate-400 border-r border-slate-100 select-none bg-slate-50/50" }, cable.sno ?? idx + 1),
          TABLE_COLS.map((col) => {
            const issue = rowIssues[col];
            const mismatch = !issue ? rowMismatches[col] : void 0;
            const isEditing = editCell?.idx === idx && editCell?.field === col;
            const val = String(cable[col] ?? "");
            return /* @__PURE__ */ React.createElement(
              "td",
              {
                key: col,
                className: `px-2 py-1 ${isEditing ? "" : "cursor-pointer"} ${issue ? "bg-red-50 border-l-2 border-red-300" : mismatch ? "bg-amber-50 border-l-2 border-amber-300" : ""}`,
                title: issue || mismatch || void 0,
                onClick: () => {
                  if (!isEditing) startEdit(idx, col);
                }
              },
              isEditing ? /* @__PURE__ */ React.createElement(
                "input",
                {
                  ref: editRef,
                  type: "text",
                  value: editValue,
                  title: `Edit ${FIELD_LABELS[col] || col}`,
                  placeholder: `Enter ${FIELD_LABELS[col] || col}`,
                  onChange: (e) => setEditValue(e.target.value),
                  onBlur: saveEdit,
                  onKeyDown: (e) => {
                    if (e.key === "Enter") saveEdit();
                    if (e.key === "Escape") setEditCell(null);
                  },
                  className: "w-full bg-blue-50 border border-blue-400 rounded px-1.5 py-0.5 text-[12px] outline-none focus:ring-2 focus:ring-blue-300 min-w-[100px]"
                }
              ) : /* @__PURE__ */ React.createElement("span", { className: `block truncate ${issue ? "text-red-600 font-medium" : mismatch ? "text-amber-700 font-medium" : val ? "text-slate-700" : "text-slate-300 italic"} ${col === "ferrule" ? "font-mono text-[11px]" : ""}` }, val || (issue ? `\u26A0 ${issue}` : "\u2014"))
            );
          })
        );
      })))), /* @__PURE__ */ React.createElement("div", { className: "px-6 py-4 border-t border-slate-200 bg-white shrink-0 rounded-b-2xl" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex-1 min-w-0" }, !loadError && cables.length > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between mb-1.5" }, /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-semibold text-slate-500 select-none" }, cables.length, " cables", totalIssues > 0 ? ` \xB7 ${errorCount > 0 ? `${errorCount} validation issue${errorCount !== 1 ? "s" : ""}` : ""}${errorCount > 0 && mismatchCount > 0 ? ", " : ""}${mismatchCount > 0 ? `${mismatchCount} source mismatch${mismatchCount !== 1 ? "es" : ""}` : ""}` : " \xB7 Ready to verify"), /* @__PURE__ */ React.createElement("span", { className: `text-[11px] font-bold tabular-nums select-none ${totalIssues === 0 ? "text-emerald-600" : "text-amber-600"}` }, okCount, "/", cables.length)), /* @__PURE__ */ React.createElement(
        "progress",
        {
          className: "cr-progress w-full",
          "data-tone": totalIssues === 0 ? "rollup" : "destination",
          "data-h": "8",
          value: okCount,
          max: Math.max(cables.length, 1)
        }
      ), totalIssues > 0 && /* @__PURE__ */ React.createElement("div", { className: "text-[11px] text-slate-400 mt-1 select-none" }, "Issues are for review only \u2014 you can still confirm and mark as verified.")) : /* @__PURE__ */ React.createElement("div", { className: "text-[12px] text-slate-400 select-none" }, loadError ? "Could not load cables \u2014 confirm is unavailable." : cables.length === 0 && !loading ? "No cables found. You can still confirm." : "")), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 shrink-0" }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "btn-secondary"
        },
        "Cancel"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: handleConfirm,
          disabled: confirming || !!loadError,
          className: "btn-primary disabled:opacity-40 disabled:cursor-not-allowed"
        },
        /* @__PURE__ */ React.createElement(ShieldCheck, { size: 16 }),
        /* @__PURE__ */ React.createElement("span", null, confirming ? "Confirming\u2026" : isVerified ? "Re-confirm" : "Confirm & Verify")
      ))), confirmError && /* @__PURE__ */ React.createElement("div", { className: "form-error mt-2" }, confirmError)))
    );
  }

  // src/components/ui/Button.tsx
  init_define_import_meta_env();
  var VARIANT_CLASS = {
    "primary": "btn-primary",
    "primary-lg": "btn-primary-lg",
    "secondary": "btn-secondary",
    "danger": "btn-danger",
    "ghost": "btn-ghost",
    "sm": "btn-sm",
    "icon": "btn-icon",
    "success": "btn-success",
    "warning": "btn-warning"
  };
  function Button({
    variant = "secondary",
    children,
    className = "",
    disabled,
    isLoading,
    ...props
  }) {
    const loadingClass = isLoading ? "btn--loading" : "";
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        className: `${VARIANT_CLASS[variant]} ${loadingClass} ${className}`.trim(),
        disabled: disabled || isLoading,
        "aria-busy": isLoading || void 0,
        ...props
      },
      isLoading ? /* @__PURE__ */ React.createElement("span", { className: "btn-spinner", "aria-hidden": "true" }) : null,
      children
    );
  }

  // src/components/ui/Card.tsx
  init_define_import_meta_env();
  function Card({ children, className = "", hoverable = false, onClick }) {
    const baseStyle = {};
    const hoverStyle = hoverable ? {
      cursor: "pointer"
    } : {};
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `card ${className}${hoverable ? " card--hoverable" : ""}`,
        style: { ...baseStyle, ...hoverStyle },
        onClick
      },
      children
    );
  }
  function CardHeader({ children, className = "" }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `card-header ${className}`
      },
      children
    );
  }
  function CardTitle({ children }) {
    return /* @__PURE__ */ React.createElement("h2", { className: "card-title" }, children);
  }
  function CardBody({ children, className = "" }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `card-body ${className}`
      },
      children
    );
  }
  function CardBodySm({ children, className = "" }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `card-body-sm ${className}`
      },
      children
    );
  }
  function CardContent({ children, className = "" }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className
      },
      children
    );
  }

  // src/components/ui/DataTable.tsx
  init_define_import_meta_env();
  var import_react18 = __toESM(require_react_shim(), 1);
  function readStore(key) {
    if (!key) return null;
    try {
      return JSON.parse(localStorage.getItem(`dt:${key}`) || "null");
    } catch {
      return null;
    }
  }
  function writeStore(key, value) {
    if (!key) return;
    try {
      localStorage.setItem(`dt:${key}`, JSON.stringify(value));
    } catch {
    }
  }
  function cellText(col, row) {
    const v = col.exportValue?.(row) ?? col.accessor?.(row);
    return v == null ? "" : String(v);
  }
  function csvEscape(s) {
    return /[",\n]/.test(s) ? `"${s.replace(/"/g, '""')}"` : s;
  }
  function DataTable({
    columns,
    rows,
    rowKey,
    title,
    toolbar,
    loading = false,
    emptyMessage = "No records found.",
    emptyIcon,
    searchable = true,
    searchPlaceholder = "Search\u2026",
    selectable = false,
    onSelectionChange,
    bulkActions,
    columnToggle = true,
    resizable = true,
    stickyHeader = true,
    pagination = true,
    pageSize: pageSizeProp = 25,
    exportable = false,
    exportFileName = "export.csv",
    onRowClick,
    rowActions,
    storageKey,
    dense = false,
    className = "",
    maxBodyHeight
  }) {
    const persisted = (0, import_react18.useMemo)(() => readStore(storageKey), [storageKey]);
    const [query, setQuery] = (0, import_react18.useState)("");
    const [sort, setSort] = (0, import_react18.useState)(null);
    const [page, setPage] = (0, import_react18.useState)(0);
    const [pageSize] = (0, import_react18.useState)(persisted?.pageSize ?? pageSizeProp);
    const [selected, setSelected] = (0, import_react18.useState)(/* @__PURE__ */ new Set());
    const [menuOpen, setMenuOpen] = (0, import_react18.useState)(false);
    const [hidden, setHidden] = (0, import_react18.useState)(
      () => new Set(persisted?.hidden ?? columns.filter((c) => c.defaultHidden).map((c) => c.key))
    );
    const [widths, setWidths] = (0, import_react18.useState)(
      () => persisted?.widths ?? {}
    );
    const menuRef = (0, import_react18.useRef)(null);
    (0, import_react18.useEffect)(() => {
      writeStore(storageKey, { hidden: [...hidden], widths, pageSize });
    }, [storageKey, hidden, widths, pageSize]);
    (0, import_react18.useEffect)(() => {
      if (!onSelectionChange) return;
      const map = new Map(rows.map((r) => [rowKey(r), r]));
      onSelectionChange([...selected].map((k) => map.get(k)).filter(Boolean));
    }, [selected, rows]);
    (0, import_react18.useEffect)(() => {
      if (!menuOpen) return;
      const onDown = (e) => {
        if (menuRef.current && !menuRef.current.contains(e.target)) setMenuOpen(false);
      };
      const onKey = (e) => {
        if (e.key === "Escape") setMenuOpen(false);
      };
      document.addEventListener("mousedown", onDown);
      document.addEventListener("keydown", onKey);
      return () => {
        document.removeEventListener("mousedown", onDown);
        document.removeEventListener("keydown", onKey);
      };
    }, [menuOpen]);
    const visibleCols = (0, import_react18.useMemo)(
      () => columns.filter((c) => !hidden.has(c.key)),
      [columns, hidden]
    );
    const filtered = (0, import_react18.useMemo)(() => {
      const q = query.trim().toLowerCase();
      if (!q) return rows;
      return rows.filter(
        (row) => columns.some((col) => {
          const v = col.accessor?.(row);
          return v != null && String(v).toLowerCase().includes(q);
        })
      );
    }, [rows, columns, query]);
    const sorted = (0, import_react18.useMemo)(() => {
      if (!sort) return filtered;
      const col = columns.find((c) => c.key === sort.key);
      if (!col) return filtered;
      const val = (row) => col.sortValue?.(row) ?? col.accessor?.(row) ?? "";
      const dir = sort.dir === "asc" ? 1 : -1;
      return [...filtered].sort((a, b) => {
        const av = val(a), bv = val(b);
        if (typeof av === "number" && typeof bv === "number") return (av - bv) * dir;
        return String(av).localeCompare(String(bv), void 0, { numeric: true }) * dir;
      });
    }, [filtered, sort, columns]);
    const total = sorted.length;
    const pageCount = pagination ? Math.max(1, Math.ceil(total / pageSize)) : 1;
    const safePage = Math.min(page, pageCount - 1);
    const paged = (0, import_react18.useMemo)(() => {
      if (!pagination) return sorted;
      const start = safePage * pageSize;
      return sorted.slice(start, start + pageSize);
    }, [sorted, pagination, safePage, pageSize]);
    (0, import_react18.useEffect)(() => {
      if (page > pageCount - 1) setPage(0);
    }, [pageCount, page]);
    (0, import_react18.useEffect)(() => {
      setPage(0);
    }, [query]);
    const pageKeys = (0, import_react18.useMemo)(() => paged.map(rowKey), [paged, rowKey]);
    const allOnPageSelected = pageKeys.length > 0 && pageKeys.every((k) => selected.has(k));
    const someOnPageSelected = pageKeys.some((k) => selected.has(k));
    const toggleAllOnPage = (0, import_react18.useCallback)(() => {
      setSelected((prev) => {
        const next = new Set(prev);
        if (allOnPageSelected) pageKeys.forEach((k) => next.delete(k));
        else pageKeys.forEach((k) => next.add(k));
        return next;
      });
    }, [allOnPageSelected, pageKeys]);
    const toggleRow = (0, import_react18.useCallback)((k) => {
      setSelected((prev) => {
        const next = new Set(prev);
        if (next.has(k)) next.delete(k);
        else next.add(k);
        return next;
      });
    }, []);
    const clearSelection = (0, import_react18.useCallback)(() => setSelected(/* @__PURE__ */ new Set()), []);
    const onSortClick = (col) => {
      if (col.sortable === false) return;
      setSort((prev) => {
        if (!prev || prev.key !== col.key) return { key: col.key, dir: "asc" };
        if (prev.dir === "asc") return { key: col.key, dir: "desc" };
        return null;
      });
    };
    const resizeCol = (key, startX, startW) => {
      const onMove = (e) => {
        const col = columns.find((c) => c.key === key);
        const min = col?.minWidth ?? 64;
        setWidths((w) => ({ ...w, [key]: Math.max(min, startW + (e.clientX - startX)) }));
      };
      const onUp = () => {
        document.removeEventListener("mousemove", onMove);
        document.removeEventListener("mouseup", onUp);
        document.body.style.userSelect = "";
      };
      document.body.style.userSelect = "none";
      document.addEventListener("mousemove", onMove);
      document.addEventListener("mouseup", onUp);
    };
    const hasFixedWidths = resizable && (Object.keys(widths).length > 0 || columns.some((c) => c.width));
    const exportCsv = () => {
      const cols = visibleCols;
      const header = cols.map((c) => csvEscape(c.header)).join(",");
      const body = sorted.map((row) => cols.map((c) => csvEscape(cellText(c, row))).join(",")).join("\n");
      const blob = new Blob([`${header}
${body}`], { type: "text/csv;charset=utf-8;" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = exportFileName;
      a.click();
      URL.revokeObjectURL(url);
    };
    const colWidth = (c) => widths[c.key] ?? c.width;
    const selectedRows = (0, import_react18.useMemo)(() => {
      const map = new Map(rows.map((r) => [rowKey(r), r]));
      return [...selected].map((k) => map.get(k)).filter(Boolean);
    }, [selected, rows, rowKey]);
    const showToolbar = title || toolbar || searchable || columnToggle || exportable;
    return /* @__PURE__ */ React.createElement("div", { className: `dt-root ${className}` }, showToolbar && /* @__PURE__ */ React.createElement("div", { className: "dt-toolbar" }, title && /* @__PURE__ */ React.createElement("div", { className: "dt-title" }, title), searchable && /* @__PURE__ */ React.createElement("div", { className: "dt-search" }, /* @__PURE__ */ React.createElement(Search, { size: 16, className: "dt-search-icon" }), /* @__PURE__ */ React.createElement(
      "input",
      {
        value: query,
        onChange: (e) => setQuery(e.target.value),
        placeholder: searchPlaceholder,
        "aria-label": "Search table",
        className: "dt-search-input"
      }
    ), query && /* @__PURE__ */ React.createElement("button", { type: "button", className: "dt-search-clear", onClick: () => setQuery(""), "aria-label": "Clear search" }, /* @__PURE__ */ React.createElement(X, { size: 14 }))), /* @__PURE__ */ React.createElement("div", { className: "dt-toolbar-actions" }, toolbar, exportable && /* @__PURE__ */ React.createElement("button", { type: "button", className: "dt-btn", onClick: exportCsv, title: "Export CSV" }, /* @__PURE__ */ React.createElement(Download, { size: 16 }), /* @__PURE__ */ React.createElement("span", { className: "dt-btn-label" }, "Export")), columnToggle && /* @__PURE__ */ React.createElement("div", { className: "dt-menu-anchor", ref: menuRef }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: `dt-btn ${menuOpen ? "is-active" : ""}`,
        onClick: () => setMenuOpen((o) => !o),
        "aria-haspopup": "true",
        "aria-expanded": menuOpen,
        title: "Columns"
      },
      /* @__PURE__ */ React.createElement(Columns3, { size: 16 }),
      /* @__PURE__ */ React.createElement("span", { className: "dt-btn-label" }, "Columns")
    ), menuOpen && /* @__PURE__ */ React.createElement("div", { className: "dt-menu", role: "menu" }, /* @__PURE__ */ React.createElement("div", { className: "dt-menu-head" }, "Toggle columns"), columns.map((col) => {
      const canHide = col.hideable !== false;
      const isVisible = !hidden.has(col.key);
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          key: col.key,
          type: "button",
          role: "menuitemcheckbox",
          "aria-checked": isVisible,
          disabled: !canHide,
          className: "dt-menu-item",
          onClick: () => {
            if (!canHide) return;
            setHidden((h) => {
              const next = new Set(h);
              if (next.has(col.key)) next.delete(col.key);
              else next.add(col.key);
              return next;
            });
          }
        },
        /* @__PURE__ */ React.createElement("span", { className: `dt-check ${isVisible ? "is-on" : ""}` }, isVisible && /* @__PURE__ */ React.createElement(Check, { size: 12 })),
        /* @__PURE__ */ React.createElement("span", { className: "dt-menu-label" }, col.header)
      );
    }))))), selectable && selected.size > 0 && /* @__PURE__ */ React.createElement("div", { className: "dt-selbar" }, /* @__PURE__ */ React.createElement("span", { className: "dt-selbar-count" }, selected.size, " selected"), /* @__PURE__ */ React.createElement("div", { className: "dt-selbar-actions" }, bulkActions?.(selectedRows), /* @__PURE__ */ React.createElement("button", { type: "button", className: "dt-btn dt-btn-ghost", onClick: clearSelection }, /* @__PURE__ */ React.createElement(X, { size: 16 }), " Clear"))), /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `dt-scroll ${stickyHeader ? "is-sticky" : ""}`,
        style: maxBodyHeight ? { maxHeight: typeof maxBodyHeight === "number" ? `${maxBodyHeight}px` : maxBodyHeight } : void 0
      },
      /* @__PURE__ */ React.createElement("table", { className: `dt-table ${dense ? "is-dense" : ""}`, style: hasFixedWidths ? { tableLayout: "fixed" } : void 0 }, /* @__PURE__ */ React.createElement("colgroup", null, selectable && /* @__PURE__ */ React.createElement("col", { style: { width: 44 } }), visibleCols.map((c) => /* @__PURE__ */ React.createElement("col", { key: c.key, style: colWidth(c) ? { width: colWidth(c) } : void 0 })), rowActions && /* @__PURE__ */ React.createElement("col", { style: { width: 1 } })), /* @__PURE__ */ React.createElement("thead", null, /* @__PURE__ */ React.createElement("tr", null, selectable && /* @__PURE__ */ React.createElement("th", { className: "dt-th dt-th-check" }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          className: `dt-check ${allOnPageSelected ? "is-on" : someOnPageSelected ? "is-partial" : ""}`,
          onClick: toggleAllOnPage,
          "aria-label": allOnPageSelected ? "Deselect all" : "Select all"
        },
        allOnPageSelected ? /* @__PURE__ */ React.createElement(Check, { size: 12 }) : someOnPageSelected ? /* @__PURE__ */ React.createElement("span", { className: "dt-dash" }) : null
      )), visibleCols.map((col) => {
        const active = sort?.key === col.key;
        const sortable = col.sortable !== false;
        return /* @__PURE__ */ React.createElement(
          "th",
          {
            key: col.key,
            className: `dt-th ${sortable ? "is-sortable" : ""} ${active ? "is-sorted" : ""} dt-al-${col.align ?? "left"}`,
            onClick: sortable ? () => onSortClick(col) : void 0,
            "aria-sort": active ? sort.dir === "asc" ? "ascending" : "descending" : void 0
          },
          /* @__PURE__ */ React.createElement("span", { className: "dt-th-inner" }, /* @__PURE__ */ React.createElement("span", { className: "dt-th-label" }, col.header), sortable && /* @__PURE__ */ React.createElement("span", { className: "dt-sort-ic" }, active ? sort.dir === "asc" ? /* @__PURE__ */ React.createElement(ChevronUp, { size: 14 }) : /* @__PURE__ */ React.createElement(ChevronDown, { size: 14 }) : /* @__PURE__ */ React.createElement(ArrowDownUp, { size: 14, className: "dt-sort-idle" }))),
          resizable && /* @__PURE__ */ React.createElement(
            "span",
            {
              className: "dt-resize",
              onMouseDown: (e) => {
                e.stopPropagation();
                const th = e.currentTarget.parentElement;
                resizeCol(col.key, e.clientX, colWidth(col) ?? th.offsetWidth);
              },
              onClick: (e) => e.stopPropagation(),
              role: "separator",
              "aria-label": `Resize ${col.header}`
            }
          )
        );
      }), rowActions && /* @__PURE__ */ React.createElement("th", { className: "dt-th dt-th-actions", "aria-label": "Actions" }))), /* @__PURE__ */ React.createElement("tbody", null, loading ? Array.from({ length: 6 }).map((_, i) => /* @__PURE__ */ React.createElement("tr", { key: `sk-${i}`, className: "dt-row" }, selectable && /* @__PURE__ */ React.createElement("td", { className: "dt-td" }, /* @__PURE__ */ React.createElement("span", { className: "dt-skel dt-skel-check" })), visibleCols.map((c) => /* @__PURE__ */ React.createElement("td", { key: c.key, className: "dt-td" }, /* @__PURE__ */ React.createElement("span", { className: "dt-skel" }))), rowActions && /* @__PURE__ */ React.createElement("td", { className: "dt-td" }, /* @__PURE__ */ React.createElement("span", { className: "dt-skel dt-skel-btn" })))) : paged.length === 0 ? /* @__PURE__ */ React.createElement("tr", null, /* @__PURE__ */ React.createElement("td", { className: "dt-empty", colSpan: visibleCols.length + (selectable ? 1 : 0) + (rowActions ? 1 : 0) }, /* @__PURE__ */ React.createElement("div", { className: "dt-empty-inner" }, emptyIcon ?? /* @__PURE__ */ React.createElement(Search, { size: 32, className: "dt-empty-icon" }), /* @__PURE__ */ React.createElement("span", null, emptyMessage)))) : paged.map((row, idx) => {
        const k = rowKey(row);
        const isSel = selected.has(k);
        return /* @__PURE__ */ React.createElement(
          "tr",
          {
            key: k,
            className: `dt-row ${onRowClick ? "is-clickable" : ""} ${isSel ? "is-selected" : ""}`,
            onClick: onRowClick ? () => onRowClick(row) : void 0
          },
          selectable && /* @__PURE__ */ React.createElement("td", { className: "dt-td dt-td-check", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement(
            "button",
            {
              type: "button",
              className: `dt-check ${isSel ? "is-on" : ""}`,
              onClick: () => toggleRow(k),
              "aria-label": isSel ? "Deselect row" : "Select row"
            },
            isSel && /* @__PURE__ */ React.createElement(Check, { size: 12 })
          )),
          visibleCols.map((col) => /* @__PURE__ */ React.createElement("td", { key: col.key, className: `dt-td dt-al-${col.align ?? "left"}` }, col.render ? col.render(row, idx) : String(col.accessor?.(row) ?? ""))),
          rowActions && /* @__PURE__ */ React.createElement("td", { className: "dt-td dt-td-actions", onClick: (e) => e.stopPropagation() }, rowActions(row))
        );
      })))
    ), (pagination || exportable) && !loading && /* @__PURE__ */ React.createElement("div", { className: "dt-footer" }, /* @__PURE__ */ React.createElement("span", { className: "dt-range" }, total === 0 ? "0 records" : `${safePage * pageSize + 1}\u2013${Math.min((safePage + 1) * pageSize, total)} of ${total}`), pagination && pageCount > 1 && /* @__PURE__ */ React.createElement("div", { className: "dt-pager" }, /* @__PURE__ */ React.createElement("button", { type: "button", className: "dt-page-btn", disabled: safePage === 0, onClick: () => setPage((p) => p - 1), "aria-label": "Previous page" }, /* @__PURE__ */ React.createElement(ChevronLeft, { size: 16 })), /* @__PURE__ */ React.createElement("span", { className: "dt-page-info" }, "Page ", safePage + 1, " of ", pageCount), /* @__PURE__ */ React.createElement("button", { type: "button", className: "dt-page-btn", disabled: safePage >= pageCount - 1, onClick: () => setPage((p) => p + 1), "aria-label": "Next page" }, /* @__PURE__ */ React.createElement(ChevronRight, { size: 16 })))), loading && /* @__PURE__ */ React.createElement("div", { className: "dt-loading-note" }, /* @__PURE__ */ React.createElement(Loader, { size: 14 }), " Loading\u2026"));
  }

  // src/components/ui/EmptyState.tsx
  init_define_import_meta_env();
  function EmptyState({
    icon,
    title,
    description,
    action,
    className = "",
    variant = "default"
  }) {
    const variantClasses = {
      default: "empty-state",
      compact: "empty-state-compact",
      centered: "empty-state-centered"
    };
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `${variantClasses[variant]} ${className}`
      },
      icon && /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "empty-state-icon"
        },
        icon
      ),
      /* @__PURE__ */ React.createElement("h3", { className: "empty-state-title" }, title),
      description && /* @__PURE__ */ React.createElement("p", { className: "empty-state-description" }, description),
      action && /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "empty-state-action"
        },
        action
      )
    );
  }
  function EmptyStateIllustration({ type, className = "" }) {
    const illustrations = {
      search: /* @__PURE__ */ React.createElement(
        "svg",
        {
          className: "empty-state-svg",
          viewBox: "0 0 200 200",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg"
        },
        /* @__PURE__ */ React.createElement("circle", { cx: "100", cy: "100", r: "80", fill: "currentColor", fillOpacity: "0.05" }),
        /* @__PURE__ */ React.createElement("circle", { cx: "100", cy: "100", r: "60", fill: "currentColor", fillOpacity: "0.1" }),
        /* @__PURE__ */ React.createElement("circle", { cx: "100", cy: "100", r: "40", fill: "currentColor", fillOpacity: "0.15" }),
        /* @__PURE__ */ React.createElement("circle", { cx: "100", cy: "100", r: "20", fill: "currentColor", fillOpacity: "0.2" })
      ),
      filter: /* @__PURE__ */ React.createElement(
        "svg",
        {
          className: "empty-state-svg",
          viewBox: "0 0 200 200",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg"
        },
        /* @__PURE__ */ React.createElement("rect", { x: "40", y: "60", width: "120", height: "80", rx: "8", fill: "currentColor", fillOpacity: "0.05" }),
        /* @__PURE__ */ React.createElement("rect", { x: "60", y: "80", width: "80", height: "40", rx: "4", fill: "currentColor", fillOpacity: "0.1" })
      ),
      error: /* @__PURE__ */ React.createElement(
        "svg",
        {
          className: "empty-state-svg",
          viewBox: "0 0 200 200",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg"
        },
        /* @__PURE__ */ React.createElement("circle", { cx: "100", cy: "100", r: "70", fill: "currentColor", fillOpacity: "0.05" }),
        /* @__PURE__ */ React.createElement("path", { d: "M100 60 L100 100", stroke: "currentColor", strokeWidth: "8", strokeLinecap: "round", fillOpacity: "0.2" }),
        /* @__PURE__ */ React.createElement("circle", { cx: "100", cy: "130", r: "8", fill: "currentColor", fillOpacity: "0.2" })
      ),
      success: /* @__PURE__ */ React.createElement(
        "svg",
        {
          className: "empty-state-svg",
          viewBox: "0 0 200 200",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg"
        },
        /* @__PURE__ */ React.createElement("circle", { cx: "100", cy: "100", r: "70", fill: "currentColor", fillOpacity: "0.05" }),
        /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M70 100 L90 120 L130 80",
            stroke: "currentColor",
            strokeWidth: "8",
            strokeLinecap: "round",
            strokeLinejoin: "round",
            fillOpacity: "0.2"
          }
        )
      ),
      "no-data": /* @__PURE__ */ React.createElement(
        "svg",
        {
          className: "empty-state-svg",
          viewBox: "0 0 200 200",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg"
        },
        /* @__PURE__ */ React.createElement("rect", { x: "50", y: "50", width: "100", height: "100", rx: "8", fill: "currentColor", fillOpacity: "0.05" }),
        /* @__PURE__ */ React.createElement("line", { x1: "50", y1: "80", x2: "150", y2: "80", stroke: "currentColor", strokeWidth: "4", fillOpacity: "0.1" }),
        /* @__PURE__ */ React.createElement("line", { x1: "50", y1: "110", x2: "150", y2: "110", stroke: "currentColor", strokeWidth: "4", fillOpacity: "0.1" }),
        /* @__PURE__ */ React.createElement("line", { x1: "50", y1: "140", x2: "150", y2: "140", stroke: "currentColor", strokeWidth: "4", fillOpacity: "0.1" })
      ),
      "no-connection": /* @__PURE__ */ React.createElement(
        "svg",
        {
          className: "empty-state-svg",
          viewBox: "0 0 200 200",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg"
        },
        /* @__PURE__ */ React.createElement("circle", { cx: "100", cy: "100", r: "70", fill: "currentColor", fillOpacity: "0.05" }),
        /* @__PURE__ */ React.createElement("line", { x1: "60", y1: "100", x2: "140", y2: "100", stroke: "currentColor", strokeWidth: "8", strokeLinecap: "round", fillOpacity: "0.2" }),
        /* @__PURE__ */ React.createElement("line", { x1: "100", y1: "60", x2: "100", y2: "140", stroke: "currentColor", strokeWidth: "8", strokeLinecap: "round", fillOpacity: "0.2" })
      )
    };
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `empty-state-illustration ${className}`
      },
      illustrations[type] || illustrations["no-data"]
    );
  }

  // src/components/ui/Input.tsx
  init_define_import_meta_env();
  var import_react19 = __toESM(require_react_shim(), 1);
  var Input = import_react19.default.forwardRef(
    ({ label, icon, error, className = "", ...props }, ref) => {
      return /* @__PURE__ */ import_react19.default.createElement("div", { className: "form-group w-full" }, label && /* @__PURE__ */ import_react19.default.createElement("label", { className: "form-label" }, label), /* @__PURE__ */ import_react19.default.createElement("div", { className: "relative flex items-center" }, icon && /* @__PURE__ */ import_react19.default.createElement("div", { className: "absolute left-3 flex items-center justify-center text-slate-400 pointer-events-none" }, import_react19.default.isValidElement(icon) ? import_react19.default.cloneElement(icon, { size: 18, strokeWidth: 1.5 }) : icon), /* @__PURE__ */ import_react19.default.createElement(
        "input",
        {
          ref,
          className: `form-input ${icon ? "pl-10" : ""} ${error ? "border-red-500 focus:border-red-500" : ""} ${className}`,
          ...props
        }
      )), error && /* @__PURE__ */ import_react19.default.createElement("span", { className: "form-error" }, error));
    }
  );
  Input.displayName = "Input";

  // src/components/ui/Skeleton.tsx
  init_define_import_meta_env();
  function Skeleton({
    className = "",
    variant = "rectangular",
    width,
    height,
    animation = "pulse"
  }) {
    const baseStyle = {
      width: width ?? "100%",
      height: height ?? "1em"
    };
    const variantClasses = {
      text: "skeleton-text",
      circular: "skeleton-circular",
      rectangular: "skeleton-rectangular",
      rounded: "skeleton-rounded"
    };
    const animationClasses = {
      pulse: "animate-pulse",
      wave: "animate-wave",
      none: ""
    };
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `skeleton ${variantClasses[variant]} ${animationClasses[animation]} ${className}`,
        style: baseStyle,
        "aria-hidden": "true"
      }
    );
  }
  function SkeletonCard({ children, className = "" }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `skeleton-card ${className}`
      },
      children
    );
  }
  function SkeletonTable({ rows = 5, columns = 4, className = "" }) {
    return /* @__PURE__ */ React.createElement("div", { className: `skeleton-table ${className}` }, /* @__PURE__ */ React.createElement("div", { className: "skeleton-table-header" }, Array.from({ length: columns }).map((_, i) => /* @__PURE__ */ React.createElement(Skeleton, { key: `header-${i}`, variant: "text", height: "1.5em" }))), Array.from({ length: rows }).map((_, rowIndex) => /* @__PURE__ */ React.createElement("div", { key: `row-${rowIndex}`, className: "skeleton-table-row" }, Array.from({ length: columns }).map((_2, colIndex) => /* @__PURE__ */ React.createElement(Skeleton, { key: `cell-${rowIndex}-${colIndex}`, variant: "text", height: "1em" })))));
  }
  function SkeletonKpi({ className = "" }) {
    return /* @__PURE__ */ React.createElement("div", { className: `skeleton-kpi ${className}` }, /* @__PURE__ */ React.createElement("div", { className: "skeleton-kpi-icon" }, /* @__PURE__ */ React.createElement(Skeleton, { variant: "circular", width: "40px", height: "40px" })), /* @__PURE__ */ React.createElement("div", { className: "skeleton-kpi-content" }, /* @__PURE__ */ React.createElement(Skeleton, { variant: "text", width: "60%", height: "1em" }), /* @__PURE__ */ React.createElement(Skeleton, { variant: "text", width: "40%", height: "1.5em" })));
  }
  function SkeletonList({ items = 3, className = "" }) {
    return /* @__PURE__ */ React.createElement("div", { className: `skeleton-list ${className}` }, Array.from({ length: items }).map((_, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "skeleton-list-item" }, /* @__PURE__ */ React.createElement(Skeleton, { variant: "circular", width: "40px", height: "40px" }), /* @__PURE__ */ React.createElement("div", { className: "skeleton-list-content" }, /* @__PURE__ */ React.createElement(Skeleton, { variant: "text", width: "70%", height: "1em" }), /* @__PURE__ */ React.createElement(Skeleton, { variant: "text", width: "50%", height: "0.875em" })))));
  }

  // src/components/ui/Form.tsx
  init_define_import_meta_env();
  function FormLabel({ children, htmlFor }) {
    return /* @__PURE__ */ React.createElement("label", { className: "form-label", htmlFor }, children);
  }
  function FormGroup({ children, className = "" }) {
    return /* @__PURE__ */ React.createElement("div", { className: `form-group ${className}` }, children);
  }
  function FormInput({ className = "", ...props }) {
    return /* @__PURE__ */ React.createElement("input", { className: `form-input ${className}`, ...props });
  }
  function FormSelect({ children, className = "", ...props }) {
    return /* @__PURE__ */ React.createElement("select", { className: `form-select ${className}`, ...props }, children);
  }
  function FormTextarea({ className = "", ...props }) {
    return /* @__PURE__ */ React.createElement("textarea", { className: `form-textarea ${className}`, ...props });
  }
  function FormError({ children }) {
    return /* @__PURE__ */ React.createElement("p", { className: "form-error" }, children);
  }
  return __toCommonJS(entry_exports);
})();
/*! Bundled license information:

react-router/dist/development/chunk-4ZMWKKQ3.mjs:
react-router/dist/development/index.mjs:
  (**
   * react-router v7.18.0
   *
   * Copyright (c) Remix Software Inc.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE.md file in the root directory of this source tree.
   *
   * @license MIT
   *)
*/
window.DWES=DWES.__dsMainNs?Object.assign({},DWES,DWES.__dsMainNs,{__dsMainNs:undefined}):DWES;
