import * as React from 'react';

/**
 * SectionHeader — from dwes@0.0.0.
 */
export interface SectionHeaderProps {
  [key: string]: unknown;
}

export declare const SectionHeader: React.ComponentType<SectionHeaderProps>;
