OverflowActionMenu from dwes. Use via `window.DWES.OverflowActionMenu` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<ThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).
